# Receipt — A-S1c: Phase 1 Repository Auditor — the D→M surfacing contract (repo-scoped excerpt surfacing + deterministic-layer-first ordering + honest contradiction handling) — Fork Branch A, Slice 1c

- **Date:** 2026-07-25
- **Branch base (merge-base):** SugarBee.ai/LaunchGraph product repo, branch `claude/sugarbee-project-handoff-ic47uc`, based at **`e8f6f99`** (the A-S1b receipt commit; code tip `6509b35`). Tree clean and `0 0` ahead/behind origin at shaping; the builder re-verified the base at start and it had not moved. Both packet commits sit directly on that base. `git merge-base HEAD origin/main` → **`fatal: Not a valid object name origin/main`** — **this repo has no default branch**; see Open boundaries. `git merge-base HEAD origin/claude/launchgraph-product-scope-43pgdx` → `a00d194` (retired branch tip). Pushed to the feature branch as a normal fast-forward **`e8f6f99..7908d5d`**; origin now `0 0` with HEAD.

## Scope

- **In:** the three coupled changes that make the deterministic layer's contract with the model layer honest, on both the offline and the online path.
  1. **C1 — contradiction is an outcome, not just a label** (`src/model/inference.ts`). In `runInferenceContract`, a `contradictory` classification now **forces `outcome: 'fail'`**, bypassing `presentation.outcomeForVerdict` (documented as consulted only for non-contradictory judgments). Justified by spec §4.2 rule 4 — *"the fact wins"*: the deterministic fact carries `establishesVerdict: 'fail'`, so `fail` **is** the fact-wins outcome, and `contradictory` is the classification that records "needs human review." The alternative (`unknown`) would make engine rule 5 **dead code for every model-derived finding**. Also resolves the deferred P-003-S1 spec-clarification item "rule-5 behavior for contradictory non-fail outcomes is unspecified."
  2. **C2 — the deterministic layer settles first, in both paths** (`src/checks/lg006.ts`, `src/scan/scanner.ts`). The `!hasCancellationSignalAnywhere` disjunct is hoisted **above** the judgment branch in `interpretLg006`, and `scanWithModel` **skips the `model.infer` call entirely** when the check is already settled. Closes the §4.3 layer-assignment violation (LG-006's layer no longer varies with model configuration).
  3. **C3 — the surfaced excerpt set follows the question's scope, not the handler file** (`src/checks/lg006.ts`; additive `isCodeFile` export in `src/scan/collect.ts`, consumed by `src/scan/webhook.ts`). `request.excerpts` = every located handler (whole file) **plus** up to **5** additional **code** files elsewhere in the repo carrying a cancellation signal, path-sorted, each windowed **−10 / +30** lines around the first match, with a note naming why it was surfaced; elision is disclosed. `fact:lg006.no-cancellation-branch` (handler-scoped, and false on delegating repos) is **retired** in favour of **`fact:lg006.no-code-cancellation-signal`** (`establishesVerdict: 'fail'`), emitted when no *code* file in the repository carries a cancellation signal. `hasCancellationBranch` survives but gates only the focused branch excerpt — no fact, no outcome.
  - **Two reviewer-requested pre-close fixes** also landed (both in Commit 2, via amend): `isCodeFile`'s docstring corrected (it falsely claimed to be the single source of truth for code-extension matching — three private copies survive, see Residue), and the **elision disclosure moved into `request.question`**, because `Evidence.note` is **not transmitted** by `buildUntrustedDataEnvelope` and the disclosure was therefore invisible to the model where it had been placed.
- **Out (explicit):** **LG-005 — OUT, that is A-S2**; no LG-005 detector, registry wiring, presentation, fixture, or test, and likewise none for LG-007/009/011/012/013. **No new fixture** — eval stays at 11; the online path is not exercised by eval, so the `FakeModelClient` integration tests are the right artifact (a `delegating-ok/` offline fixture stays a residue candidate). **No engine change, no schema change** (emit-only). No report-surface change — `src/cli/reportMd.ts` already groups `requires_confirmation` and renders `contradictory`; verified, not modified. No import-resolution / reachability analysis (A-S1b's reviewer withdrew that approach; not revived). `lg003.ts:51`'s third copy of the **webhook** predicate — still deferred. `detectStack` / `scanner.supported` / LG-015's applicability gate — untouched. `collect.ts`'s exclusion set — untouched. SEC-5 delimiter-token hardening; `golden/` (AT-16); `hostile/` (AT-18/20/21/22); spec-header staleness; `report.repo.commit`; the Express/Vite/Fly (Gravito) recipe. **No real model call** — `FakeModelClient` drove every test. No merge, no PR, no deploy, no secrets.

## Commits

- **`f3a981f`** — *Contradiction is an outcome, not just a label (D→M contract) — Commit 1* — 4 files, 104 insertions / 3 deletions: `src/model/inference.ts` (29), `tests/model/inference.test.ts` (47), `tests/engine.test.ts` (23, the new DC-5 test), `tests/scan/scanModel.test.ts` (8, the single budgeted assertion change).
- **`7908d5d`** — *Surface the delegated path; the D layer settles first — Commit 2* — 6 files, 491 insertions / 64 deletions: `src/checks/lg006.ts` (223), `tests/checks/lg006.test.ts` (174), `tests/scan/scanModel.test.ts` (111), `src/scan/collect.ts` (25, additive), `src/scan/scanner.ts` (13), `src/scan/webhook.ts` (9, consumes `isCodeFile`, private `CODE_EXT_RE` deleted).
- Commit 2 was **amended twice** — `f41beae`, then `7908d5d` — for the two reviewer-requested pre-close fixes above. **Commit 1 was unchanged throughout**, so its isolation proof stands as taken.
- Branch tip `7908d5d`; ≤2 commits honored.

## QA proof

*(All figures qa-verified independently at `7908d5d`.)*

- **Suite:** `npm test` → **253 passed, 0 failed, 0 skipped, 25 files** (base `e8f6f99` was **233 / 25**) → **+20 tests, no new file**. `npm run typecheck` (`tsc --noEmit`) **clean**.
- **Eval:** `npm run eval` → **11 fixtures, 11-of-11 decisions correct, 0 blocker false positives**.
- **Commit-1 isolation:** fresh worktree @ **`f3a981f`** + `npm ci` → **236 passed / 25 files**, typecheck clean, eval **11/11, 0 blocker FPs** → **GREEN**.
- **Emit-only preserved:** `src/decision/engine.ts` blob **`54b0990…`** and `src/schema/index.ts` blob **`c1ae295…`** byte-identical to base. Also byte-identical to base: `src/model/client.ts`, `src/model/realClient.ts`, `src/checks/registry.ts`, `src/checks/detectorKit.ts`, `src/checks/lg003.ts`, `src/checks/lg004.ts`, `src/checks/lg010.ts`, `src/checks/lg015.ts`, `src/cli/reportMd.ts`, `bin/`, all of `src/cli/**`, and `tests/scan/webhook.test.ts` (the zero-assertion-change behavior-preservation proof for the `isCodeFile` migration, per Trap 3).
- **Zero fixture movement:** the `fixtures/` tree object is **`a981446bac76039147d93efedd14a092c2aeadc1`** at **both** `e8f6f99` and `7908d5d` — identical. No rebaseline was needed and none was authorized.
- **Fixture-corpus byte-identity — recorded as PER-FIXTURE NORMALIZED HASHES, not a whole-corpus aggregate.** A whole-corpus aggregate is **scan-path dependent**, because `repo.root` is embedded in every serialized report. The builder's earlier aggregate figure (`fe3de836…` / 68332 bytes) was wrong for exactly that reason and is **retracted**. qa validated the replacement adversarially — raw hashes differ across scan paths, normalized hashes are invariant and agree at both commits and in both configurations:

  | normalized sha (16) / bytes | fixture |
  |---|---|
  | `121cdb5f3fbac3c8` / 5685 | `broken-lg-001` |
  | `5fc7363e19b591a5` / 6688 | `broken-lg-002` |
  | `7c8d4e370f64b73f` / 6846 | `broken-lg-003` |
  | `a7a408a8376b20ea` / 7690 | `broken-lg-004` |
  | `805070be66c27a60` / 7490 | `broken-lg-006` |
  | `f8c5e604a7273ca5` / 6076 | `broken-lg-008` |
  | `bda9ac207baa5fa5` / 6029 | `broken-lg-010` |
  | `41cbe2bb9a8a4a03` / 6534 | `broken-lg-014` |
  | `3a94e4b9294749d1` / 5405 | `broken-lg-015` |
  | `0881320866025be6` / 5362 | `clean-min` |
  | `9f6e2380a768126f` / 4204 | `unsupported` |

- **Assertion-change budget: exactly 1 for the whole packet** — Commit 1's single §8 entry (`tests/scan/scanModel.test.ts`, `expect(finding?.classification)` `'contradictory'` → `'requires_confirmation'`), and **ZERO in Commit 2**. Five of the six §8-authorized test-*input* re-bases were used (`lg006.test.ts:212/:225/:240` onto a delegating repo; `scanModel.test.ts:257` onto the existing `delegatingCancellationRepo()`; `:280` onto a new prose-only-signal repo). **No budget breach.**
- **`collect.ts` additive-only:** the `isCodeFile` export is purely additive; the exclusion set is **byte-identical**, `IGNORED_DIRS` re-verified. The `node_modules` exclusion still holds LG-006's blocker (the `stripe` package's own typings name `customer.subscription.deleted`) — and it is now **load-bearing twice over**, since C3 surfaces repo-wide code files into prompts.
- **SEC-5 blast radius bounded:** **zero non-code excerpts reach a prompt** (DC-6, asserted on a repo whose `README.md` names the event); widened excerpts are still redacted and still SEC-5 envelope-wrapped.
- **CEILING INVARIANT:** holds — and now **provably by construction**, see Corrections #2. No unqualified `ready` across all 11 fixtures and every online probe.
- **AT-23 determinism:** surfaced non-handler files are path-sorted, capped and windowed from the first matching line; two fixed-clock runs byte-identical. **AT-26:** the contradictory-derived `fail` carries non-empty evidence (verified, not assumed). **AT-27 stays closed** — the M-layer disjunct still degrades to `unknown` ("model layer disabled"), and the branch-absent case now provably makes **zero `infer` calls**.
- **Safety grep: clean.** Zero secret patterns across both commit trees, the packet, this receipt, and memory. No `process.env` / network / credential read in model or detector code. No new runtime deps. **No real model API call** — `FakeModelClient` only. TEST-DATA POLICY satisfied on every surface.
- **UI smoke:** N/A (detector/contract packet, no UI). Report surface verified read-only: the `requires_confirmation` finding renders in `reportMd`'s needs-confirmation grouping, with `reportMd.ts` unmodified.

## Review

- **Verdict: PASS.** Two must-fix-before-close items were raised and fixed inside the packet via amend (the `isCodeFile` docstring overclaim, and the elision disclosure being placed on a field `buildUntrustedDataEnvelope` does not transmit); qa re-proved the suite at `7908d5d` after both.
- **Adjudications the packet required (§7), decided explicitly:**
  1. **`fail` vs `unknown` for a contradictory judgment — `fail` upheld** on spec §4.2 rule 4 ("the fact wins"). The fact carries `establishesVerdict: 'fail'`; `contradictory` is the classification recording "needs human review." `unknown` would render engine rule 5 dead code for every model-derived finding and drop the disagreement out of the section §7 rule 4 designed for it.
  2. **`fact:lg006.no-code-cancellation-signal` — honest replacement, but the code/non-code split introduces a real new species of wrong claim.** See Corrections #3: the packet's own direction-check ("worst case is under-warn") was **wrong**, and both reviewer and qa measured it.
  3. **The 5-file cap — kept, but its stated justification was demolished.** The reviewer constructed the hazard: five ordinary `app/**/page.tsx` files carrying an analytics label, with the real `lib/events.ts` elided. **Six mentions can all be UI copy**, so "a repo mentioning cancellation in six code files plainly handles cancellation" does not hold. The elision disclosure as landed therefore makes **no completeness argument** — do not reintroduce one.
- **Codex second-eyes: NOT AVAILABLE — and no second-eyes pass ran.** `codex` is not on PATH in this environment and no plugin is present. The review was single-model analysis plus empirical probes. Recorded so the proof standard is not overstated.
- **Product Trajectory Check:** the D→M contract is the seam A-S2/LG-005 consumes; it is now honest on both paths, but **not fully settled** — the reviewer named two extraction seams for A-S2 (Residue #6) and a live correctness gap (Residue #1/#2). Engine + schema untouched; registry stays the single source of severity/blocker/provider; LG-006 still carries no `externalVerification` on any branch (correct — non-external), so the LG-012 external path (A-S7) is untrodden.

## Corrections that must land in the record

1. **The unqualified-`ready` claim is RETRACTED.** The builder reported that pre-C1, a bare repo plus a model `pass@0.9` produced unqualified `ready`. **It does not reproduce.** qa enumerated **all 32 combinations** of the five structural signals governing the ceiling, as real directory scans: **0 unqualified `ready`, 0 ceiling violations**. The claim came from an isolated `decide()` with LG-015's finding absent — the same artifact class as A-S1b's. The builder's own diagnosis: *"I overclaimed a decision-level consequence from a finding-level defect."* **The accurate framing of what C1 fixes: a contradictory judgment fired no decision rule at all and rendered as a plain pass.**
2. **The CEILING INVARIANT is STRUCTURAL, not incidental — this CORRECTS A-S1b's receipt wording.** A-S1b recorded qa's caveat that the ceiling holds only because LG-015 *"incidentally"* always attaches a pending-external marker. qa now proves it holds **by construction**: `supported = hasNextDep || hasAppRouter || hasPages`, and `hasAppSignal = supported's disjuncts || vercel.json || metadataBase`, so **`supported ⇒ hasAppSignal`** and LG-015 can never be `not_applicable` on a supported stack. Rule 6 therefore always pre-empts rule 7. *(Receipts are append-only — `A-S1b.md` is NOT edited; the correction is recorded here and in `build-os/memory/`.)*
3. **The A-S1b false blocker is NARROWED, NOT CLOSED — and the earlier "under-warn only" characterization was WRONG.** Both reviewer and qa measured this, and qa **explicitly withdrew its own softer framing**. On a **correct** repo whose cancellation handling lives in a file `isCodeFile` does not match, the D layer attaches the false `fail`-establishing fact **and** withholds the very file that would disprove it. When the model **agrees** with that false fact — the **likely** branch, since it is shown a branchless handler and told no code file handles cancellation — the result is `inferred`@0.85 → **`not_ready` on a correct repository**. Only when the model *disagrees* do you get `requires_confirmation` → `ready_with_warnings`. Measured on `.mts`, `.cts`, `.sql`, and `.prisma`.

## Residue

**Deferred / follow-up packets:**

1. **`.mts`/`.cts` is the SHARPER instance, not `.sql`/`.prisma`** (qa's addition). `isCodeFile` is `/\.(?:ts|tsx|js|jsx|mjs|cjs)$/` — it has `mjs|cjs` but **not** `mts|cts`. A repo using ESM/CJS TypeScript module extensions is ordinary TypeScript, so this is a **materially more likely** false blocker than the migration-file case. **Do not let Fix A's docstring stand as coverage** — it frames `.mts` as a *future consistency* risk for lg003/lg010/lg015, not as a **live correctness gap in LG-006 today**.
2. **Frame the follow-up correctly** (qa's second addition): the live issue is **a false `establishesVerdict: 'fail'` fact asserted about repositories that are correct** — not merely "the extension list needs widening." The `.ts` row separates the two classes: with `.ts` the model saw `lib/events.ts` and still said `fail` — a wrong model on a **complete** surface, explicitly disclaimed by DC-2. With `.mts`/`.cts`/`.sql`/`.prisma` the surface is **incomplete *and* a false fact is attached**. The second class is what A-S1c exists to close, and **it remains open for those extensions**.
3. **The elision honesty guard is a regression pin, not a semantic guarantee.** It pins the phrasings "plainly handles" / "clearly handles" only; a reword to "six mentions indicate the repo handles cancellation" would slip past it. qa confirmed the guard **discriminates** (it fires on counterfactual text) rather than being vacuous.
4. **The 5-file cap's stated justification was demolished** — the reviewer built the hazard from five ordinary `app/**/page.tsx` files carrying an analytics label with the real `lib/events.ts` elided. Six mentions can all be UI copy. The disclosure as landed makes **no completeness argument**; do not reintroduce one.
5. **Three surviving private `CODE_EXT_RE` copies** — `lg003.ts:35`, `lg015.ts:39`, `lg010.ts:62`. The previously-deferred `lg003.ts:51` webhook-predicate item is **folded into this one** as a single "un-migrated predicate copies" item. The predicate is unified **at the sites this packet touches**, not repo-wide.
6. **Two trajectory seams the reviewer named for A-S2.** (a) The repo-wide-grep + cap + window logic is **inline in `lg006.ts`**; LG-005 will restate it, and LG-005's signal (`event.id` dedup) is a **far more common string** than a cancellation event name, so a naive grep surfaces noisier files and hits the cap far more often. Extract a parameterized `surfaceDelegatedCandidates(fileset, { handlers, signalRe, cap, window })` **before the second copy exists**. (b) `scanWithModel` now **hardcodes the LG-006 `surface → isSettled → interpret` triple**; five more D+M checks means five more inline triples. A small `ModelCheck` interface is the next seam.
7. **`node_modules` exclusion is now load-bearing twice over** — C3 surfaces repo-wide code files into prompts, so the exclusion protects both LG-006's blocker (Stripe's own typings name the event) and the **prompt surface**. qa re-verified `IGNORED_DIRS` byte-identical.
8. **No Codex second-eyes pass ran** — again. `codex` is not on PATH and no plugin is present; single-model review plus empirical probes. Recorded so the proof standard is not overstated.
9. **Carried forward — this repo still has NO DEFAULT BRANCH.** No `main`/`master`, locally or on the remote. "Never merge without go" has **no target**; a PR has **nowhere to land**. Needs a user decision; explicitly not this packet's work.
10. **Carried forward from A-S1b, still open and not superseded above:** the LG-006 **comment-suppression recall cost**; the **anchor-excerpt fidelity** note (the fail evidence reuses the single-line anchor rather than the whole-handler excerpt); `fixtures/broken-lg-006/app/api/stripe/webhook/route.ts:1-2`'s **import depth** (five levels where four is correct); and **AT-06's model-assisted half** (still proven only by `FakeModelClient` integration tests — AT-06 is **not** closed end-to-end).
- **Also still open, non-blocking, unchanged by this packet:** SEC-5 delimiter-token hardening; spec-header staleness; `report.repo.commit` null; banner locator artifact on fixture scans; §9.3's 15/15-recall and 18/18-decision thresholds (eval stays at 11 fixtures); AT-16 `golden/` + AT-20/21/22 hostile fixtures; LG-008 optional warning for a lone committed DB URL; LG-003 provider-agnostic webhook presence; LG-010 last-two-labels domain heuristic (no PSL); LG-014 implicit/dashboard-only Sentry mechanisms; LG-001 source-path scanning; harness greedy-matching debt.

**Known risks / carry-forward obligations still binding:**
- **MODEL-LAYER SAFETY BOUNDARY** — credential boundary at `bin/sugarbee.ts` only; `RealModelClient` bin-only, built-in `fetch`, no SDK, no `process.env` in model or detector code; `FakeModelClient` drives every test; SEC-5 delimited untrusted data; cite-or-discard; at best `inferred`, capped 0.9, never `confirmed`. Preserved — and now **stricter**, since only code files may enter `request.excerpts`.
- **EMIT-ONLY ENGINE + SCHEMA** — blob SHAs unchanged; re-verified at close.
- **REGISTRY IS THE SINGLE SOURCE** — severity/blocker/provider via `makeFinding`; `blocker` never hardcoded.
- **externalVerification OBLIGATION** — LG-006 carries none on any branch (correct, non-external); **LG-012 remains the last pending external check (A-S7)**.
- **TEST-DATA POLICY** — <20 contiguous alphanumerics on every key-shaped fake, no Sentry-DSN shape, on every surface including this receipt and memory; never allowlist a secret.
- **CEILING INVARIANT** `hasAppSignal ⊇ scanner.supported` — now proven **structural** (Corrections #2). Add the regression assertion when `golden/`/AT-16 lands.
- **`node_modules` EXCLUSION IS LOAD-BEARING** — twice over, see Residue #7.

## Open boundaries (awaiting explicit go)

- **Push:** executed under the standing feature-branch authorization AFTER qa green + reviewer pass + safety grep, landing as a normal fast-forward **`e8f6f99..7908d5d`**. Origin and HEAD are in sync.
- **NO merge to any default branch and NO pull request** — both await explicit approval, and per Residue #9 there is currently **no default branch to merge into**.
- No deploys, no secrets touched or allowlisted, no provider access. **No real model API call occurred** — `FakeModelClient` drove every test; the model layer's runtime egress only activates when a real model is configured at the bin, which did not happen.
