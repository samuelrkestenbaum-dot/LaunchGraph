# Receipt — P-004: Phase 1 Repository Auditor — `launchgraph` scan/eval CLI (Fork Branch B)

- **Date:** 2026-07-23
- **Branch base (merge-base):** LaunchGraph repo, branch `claude/launchgraph-product-scope-43pgdx`, based at `006cadd` (P-003-S4 tip) — verified via `git merge-base` at go (exact match, tree clean). Both packet commits sit directly on that base and were pushed to the feature branch as a normal fast-forward (`006cadd..a4ffbf5` on origin).

## Scope
- **In:** The runnable `launchgraph` CLI wrapping the proven detector library — `scan [path]` and `eval` — with deterministic exit codes, a canonical JSON report AND a human-readable report (banner + report.md), explicit unsupported-stack behavior, and writes confined to the out dir.
  - **New source:** `src/cli/{args,io,run,reportMd,banner}.ts`, `bin/launchgraph.ts`, `tests/cli/{args,scan,report}.test.ts`.
  - **Architecture:** `run(argv, io): number` — pure, synchronous, returns the exit code; ALL effects flow through an injected `Io {stdout, stderr, now, cwd, writeFile}`. Tests invoke `run()` programmatically with a fake Io (fixed clock, capturing writes) — no subprocess, no build step. The bin (`bin/launchgraph.ts`) runs via `tsx`. The clock is injected via `Io.now` (fixed in tests for determinism). The scanner's `dir` param works on any directory (the "fixtureDir" name was a misnomer), so `scan` reuses the composed §9 pipeline verbatim.
  - **Exit-code + output contract (§11.4):** `decision.value` drives the code in BOTH modes — `ready`/`ready_with_warnings` → 0, `not_ready` → 1, scan/parse error → 2, `not_evaluated` → 3. `--json` → `serializeReport` to stdout only, ZERO files written. default → `report.json` + `report.md` into `--out` (else `<path>/.launchgraph/`) + a human banner to stdout; writes confined to the out dir (SEC-6). Flags: `scan [path]`, `--json`, `--out`, `--offline`, `--checks`, `--app`; plus `eval` (wraps `runEvaluation` over the fixtures root, resolved module-relative via `import.meta.url` — no host env; exits 0 iff `allDecisionsCorrect && blockerFalsePositives === 0`). `--offline` is honest flag-only (all 8 current checks are Layer D).
  - **Additive / behavior-preserving edit to a frozen file:** `src/scan/scanner.ts` gained an optional `checks?: readonly string[]` option (filters findings by `checkId` before `decide`; `undefined` = byte-identical; the `stackSupported` short-circuit stays intact so `--checks` cannot break AT-24; the 143 prior tests unaffected).
  - **Config:** `package.json` — added `bin.launchgraph` + `scan`/`eval` scripts + `tsx` 4.19.2 as a **DEV** dependency (runtime dependencies remain ZERO/absent). `README.md` — added a CLI section and corrected the Status section (see fix-then-pass below).
  - **Reuse (import, never reimplement):** `createScanner`, `decide`, `serializeReport`, `exitCodeForDecision`/`SCAN_ERROR_EXIT_CODE`, `runEvaluation`, `validateReport`.
- **Out (explicit):** The model layer / any M or D+M detector (`--offline` is honest flag-only; the "mark M-checks unknown" half of AT-27 is OWED to the model-layer packet). The Express/Vite/Fly (Gravito) recipe — recorded as the first named post-golden-path recipe + eval target in the receipt/memory ONLY, NOT implemented (`detectStack` stays Next.js-only). Remediation packages; `hostile/` + real `golden/` fixtures (AT-16/20/22 still deferred). Merge or PR. Provider access. No new RUNTIME deps. Reading `.git/HEAD` for `report.repo.commit` was deferred (banner `@<commit>` is cosmetic; no AT requires it).

## Commits
- 3116db7 Add launchgraph scan CLI core (args, injected Io, run)
- a4ffbf5 Add report.md/banner renderers, eval command, runnable bin
- Commit 2 was **amended once, pre-push**, to fold in the reviewer's one must-fix README item plus a tsx README-link typo (original hash before amend was `182fc44`). Docs/specs and frozen library source untouched except the one additive `scanner.ts` change; the only other pre-existing files changed are `README.md` + `package.json` + `package-lock.json`. No fixtures added or modified.

## QA proof
- Suite:        `npm test` @ HEAD `a4ffbf5` → **181 passed, 0 failed, 0 skipped**, 20 files (new: cli/args 14, cli/scan 14, cli/report 10; the 143 prior detector/engine/scan tests unchanged); `tsc --noEmit` clean. `npm run eval` → **10/10 decisions correct, 0 blocker false-positives, exit 0**.
- Regression:   `PRODUCT_SCOPE.md`, `specs/`, and ALL frozen library source (schema, all checks, engine, serialize, harness, collect, redact) UNCHANGED; `scanner.ts` additive-only; only other pre-existing files changed are `README.md` + `package.json` + `package-lock.json`; no fixtures added/modified; 143 prior tests green. Exit codes re-derived by running `run()` + the real bin: `broken-lg-001` → 1, `broken-lg-010` → 0, `clean-min` → 0, `unsupported` → 3, parse errors → 2. `--json` === `serializeReport` byte-for-byte with ZERO writes. AT-25 `validateReport` clean on the emitted `report.json`. AT-23 two fixed-clock runs → byte-identical `report.json` AND `report.md`. `--checks LG-001` → only LG-001 findings. `--offline` byte-identical to online.
- Commit-1 iso: fresh worktree @ `3116db7`, `npm ci` with the ORIGINAL lockfile (no tsx) → **171 passed / 19 files**, typecheck clean; Commit-2 artifacts (`reportMd.ts`, `banner.ts`, the bin, the tsx dep, the scan/eval scripts) absent → green in isolation.
- Safety grep:  **Scanner-safety grep — the provider live/test key prefix patterns, the webhook-secret prefix pattern, the email-provider key prefix pattern, AND all Sentry-DSN shapes (plus the old fixed-string literal) are ZERO** across the worktree AND both commit trees; no new fixture and no committed `.md` snapshot. SEC: no `process.env` / `child_process` / `exec` / `spawn` / `fetch` / network in `src/cli` or `bin`; writes only via `Io.writeFile` into `--out` (SEC-6 confinement enforced by the injected-Io design and asserted); `collect.ts`/`redact.ts` unchanged. Redaction (SEC-4): `report.md`/`report.json` show the redaction marker, NEVER the raw fake key — the renderers read only already-redacted evidence excerpts. Runtime deps still ZERO (all 153 non-root lockfile packages `dev:true`; tsx dev-only). Re-verified independently by the main session pre-push (scanner-safety zero incl. Sentry-DSN shapes + the old literal) before the fast-forward push.
- UI smoke:     N/A (CLI packet; the human banner + report.md are exercised via cli/report tests and the real bin).
- Acceptance tests: **AT-24 closed** (unsupported stack → `not_evaluated` / exit 3, with ZERO fail/warning findings in the human output — the 1 `unknown` + 7 `not_applicable` pass through in `report.json`, honestly labeled; the banner and report.md render NONE of them, so the human surface shows zero findings). AT-25 (report.json validates), AT-23 (byte-identical under fixed clock) reinforced end-to-end; partial AT-27 (`--offline` deterministic, no network). AT-27's "mark M-checks unknown" half remains OWED to the model-layer packet.

## Review
- Verdict: **fix-then-pass → satisfied** — one must-fix-now, resolved before push; the reviewer's flip-to-pass condition met.
- Codex second-eyes: not available (no CLI/plugin).
- Product Trajectory Check: the `run()`/`Io` seam is a clean, pure, synchronous entry point the model layer will plug into. The library reuse is genuine — no reimplemented decision/exit/serialize logic (the exit code comes from the harness's `exitCodeForDecision`); the registry stays the single source of check metadata; the ceiling watch-item is undisturbed.
- The ONE must-fix-now (RESOLVED before push): a README internal contradiction — the new CLI section said the auditor "ships" while the pre-existing Status section still said "Pre-implementation … awaits explicit go." The Status section was rewritten (per the reviewer's exact prescribed prose) to state Phase 1 is under active implementation with the library + CLI in place, the model layer + remaining checks pending, the `ready_with_warnings` ceiling over the wired subset (LG-001/002/003/004/008/010/014/015), and "nothing merged/deployed/published without explicit go." The fix was folded into Commit 2 via amend; the suite was independently re-verified green (181) + typecheck clean + scanner-safe.
- NOTABLE ADJUDICATIONS (all ten substantive challenges PASS):
  1. Library reuse genuine — no reimplemented decision/exit/serialize logic.
  2. `scanner.ts` `checks?` additive/behavior-preserving; the ceiling watch-item undisturbed.
  3. Exit-code contract correct in BOTH modes.
  4. **AT-24 interpretation FAITHFUL** — §2.3 targets "junk findings" (spurious defects); honestly-labeled `not_applicable`/`pass` are not junk, and the banner/report.md render none of them, so the human output shows zero findings on an unsupported stack. The pass-through findings are pre-existing frozen-scanner behavior, present only in `report.json`, honestly labeled.
  5. `--json` contract correct.
  6. SEC-6 output confinement enforced by the injected-Io design + asserted.
  7. Redaction preserved (renderers read only already-redacted evidence excerpts). The flagged "banner locator artifact" (scanning a FIXTURE dir, LG-002 evidence lists the manifest file because the fixture manifest embeds the seeded key string; excerpt still redacted) is genuinely cosmetic, cannot occur on a real repo, and would require touching a frozen detector — out of scope.
  8. `--offline` honest, no overclaim.
  9. SEC clean (no cred intake, no execution, no network).
  10. Scope + secrets clean, tsx dev-only, ceiling undisturbed, registry single source.

## Residue
- Deferred / follow-up packets (all non-blocking, NONE gating):
  1. **Spec header staleness** — `specs/phase-1-repository-auditor.md` header still says "not yet implemented"; defensible as the spec's own authorship status, but worth reconciling in a later doc pass now that implementation is underway.
  2. **`report.repo.commit` stays null this packet** — the banner's `@<commit>` is cosmetic (no AT requires it); reading `.git/HEAD` statically is deferred to avoid touching the frozen scanner beyond the additive option.
  3. **Banner locator artifact on fixture scans** — cosmetic, real-repo-impossible (see review adjudication 7).
  4. **`eval` runs over the 10 present fixtures** — golden/, hostile/, and the model-layer broken fixtures don't exist yet; §9.3's 15/15-recall and 18/18-decision thresholds remain a PROGRAM-level gate owed as fixtures/detectors land.
  5. **AT-27 "mark M-checks unknown" half is OWED to the model-layer packet** — `--offline` is honest flag-only this packet because all 8 wired checks are Layer D.
  6. The tsx README-link typo (pointed at the wrong package's domain) was ALSO fixed in the same amend (now the tsx homepage) — no residue.
  - Carried forward from S3/S4 (still open, non-blocking): **LG-008** optional warning for a lone committed DB URL; **LG-003** provider-agnostic webhook presence; **LG-010** last-two-labels domain heuristic (no PSL — can only under-warn); **LG-014** implicit/dashboard-only Sentry mechanisms.
- Carry-forward obligations still binding:
  - **externalVerification obligation** — proven in code for LG-003/010/014/015; **LG-012 is the last pending external check, and it needs the model layer** (Layer D+M).
  - **TEST-DATA POLICY** (<20 contiguous alphanumerics for every key-shaped fake, no Sentry-DSN shapes, every surface incl. receipts/memory; never allowlist a secret).
  - **CEILING WATCH-ITEM** (`hasAppSignal ⊇ scanner.supported`; add the AT-16 regression when `golden/` lands). Registry stays the single source of check metadata.
  - **CLI entry-point seam** — the `run()`/`Io` seam is the pure, synchronous entry point the model layer plugs into.
- **Coverage after this packet: 8 of 15 detectors wired into a runnable CLI** — LG-001/002/003/004/008/010/014/015. Remaining 7 — LG-005/006/009/011/013 (Layer D+M), LG-007 (Layer M), LG-012 (Layer D+M, external) — ALL require the model layer (the FORK's Branch A).
- **GRAVITO — FIRST NAMED POST-GOLDEN-PATH RECIPE + REAL-WORLD EVALUATION TARGET** (recorded per the user's instruction, product-direction note, NOT a Build OS action item this packet): Gravito is a Vite + Express + Fly.io TypeScript SaaS (NOT Next.js/Vercel), so LaunchGraph's current Next.js-only `detectStack` would honestly return `not_evaluated` (unsupported stack) on it. An Express/Vite/Fly recipe is the first obvious post-golden-path expansion, and Gravito becomes a named eval target once that recipe exists — but the golden path (one runnable Next.js path) is completed FIRST; the recipe was deliberately NOT built this packet. The separate Gravito governance visibility item (the live substrate-readiness check still reports Claude, Manus, surplus_recovery as missing surfaces) remains its own distinct item.

## Open boundaries (awaiting explicit go)
- Push to the designated feature branch was executed under standing authorization AFTER qa green + reviewer fix-then-pass-satisfied + a two-repo scanner-safety grep (incl. Sentry-DSN shapes + the old literal), landing as a fast-forward `006cadd..a4ffbf5`.
- NO merge to any default branch and NO pull request — both await explicit approval.
- No deploys, no secrets touched/allowlisted, no provider access.
