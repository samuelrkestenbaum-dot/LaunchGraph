# Receipt — A-S1b: Phase 1 Repository Auditor — shared webhook locator + LG-006 deterministic-disjunct fidelity fix + `broken-lg-006` fixture (AT-06 via eval) — Fork Branch A, Slice 1b

- **Date:** 2026-07-25
- **Branch base (merge-base):** SugarBee.ai/LaunchGraph product repo, branch `claude/sugarbee-project-handoff-ic47uc`, based at **`ed71d6e`** (the Build-OS memory-relocation commit; verified clean tree, `0 0` ahead/behind origin at shaping). Both packet commits sit directly on that base. `git merge-base HEAD origin/claude/launchgraph-product-scope-43pgdx` → `a00d194` (retired branch tip). **`origin/main` does not exist** — see Open boundaries. Pushed to the feature branch as a normal fast-forward **`ed71d6e..6509b35`**; origin now `0 0` with HEAD.

## Scope

- **In:** the three coupled pre-LG-005 items from the P-005 residue, landed as one slice.
  1. **Shared webhook-handler locator extracted** — new `src/scan/webhook.ts` holding the single path predicate + an all-handlers `locateWebhookHandlers(fileset)`. `src/checks/lg004.ts` and `src/checks/lg006.ts` both consume it; the byte-identical restatement is gone. Per packet §7 the shared contract is **all-handlers (`.filter()` semantics)** — LG-006 moved off `.find()`, LG-004 kept what it already shipped. LG-004's local predicate was **byte-identical** to LG-006's copy, so LG-004 cannot regress from the swap. Determinism rests on `collect.ts:270` sorting collected files by path (qa confirmed, per §7d).
  2. **LG-006 deterministic-disjunct fidelity fix** (P-005 residue TOP ITEM, spec §3 + §4.3) — a **fully missing** cancellation branch is now a deterministic `confirmed` / confidence 1.0 **blocker `fail`** that stands under `--offline`. `unknown` / `inferred` is reserved for the branch-present-but-downgrade-unproven case, which is where the spec genuinely requires model path confirmation. AT-27 stays closed under the §8 restatement (LG-006 is D+M, not M; AT-27 binds its M-layer portion, which still degrades to `unknown` with the documented reason `model layer disabled` in every case whose outcome depends on it).
  3. **`fixtures/broken-lg-006/`** added (inert, `private: true`, no scripts, excluded by vitest/tsconfig) + `expected.json`; **AT-06's deterministic half proven via `npm run eval`**. Coupled and mandatory per §6: **`fixtures/broken-lg-004` was repaired** — it had a genuine latent LG-006 defect (no cancellation branch at all), so what was wrong was its manifest's "seeds exactly LG-004" claim, not the check. A `customer.subscription.deleted` branch reaching an entitlement downgrade was added without adding `constructEvent` or a `stripe-signature`/HMAC pair, so LG-004 still fails and still cites `webhook/route.ts`.
  - Also landed in the fix-then-pass round (see Review): the offline `unknown` summary reworded to a repo-scoped claim, and an **authorized comment-only** correction to `src/scan/scanner.ts:196-207`.
- **Out (explicit):** **LG-005 — OUT, that is A-S2**; no LG-005 detector, registry wiring, fixture, or presentation. Any other remaining detector (LG-007/009/011/012/013). **No engine or schema change** (emit-only preserved). No change to `detectStack` / `scanner.supported` / LG-015's applicability gate. No change to `src/checks/detectorKit.ts` (kept frozen — which is *why* the locator went in `src/scan/webhook.ts`). `golden/` (AT-16), `hostile/` (AT-18/20/21/22), SEC-5 delimiter-token hardening, spec-header staleness, `report.repo.commit`, Express/Vite/Fly (Gravito) recipe. **No real model call** — `FakeModelClient` drove every test. No merge, no PR, no deploy, no secrets.

## Commits

- **`7683d74`** — *Extract the shared webhook-handler locator — Commit 1* — 5 files, 330 insertions / 82 deletions: new `src/scan/webhook.ts` (53), new `tests/scan/webhook.test.ts` (175), `src/checks/lg004.ts` (16), `src/checks/lg006.ts` (162), `tests/checks/lg006.test.ts` (6).
- **`6509b35`** — *LG-006 deterministic disjunct + broken-lg-006 fixture — Commit 2* — 15 files, 468 insertions / 41 deletions: `src/checks/lg006.ts` (152), `src/scan/scanner.ts` (9, comment-only), `tests/checks/lg006.test.ts` (102), `tests/scan/scanModel.test.ts` (141), the 8 new `fixtures/broken-lg-006/*` files, and the `broken-lg-004` repair (`app/api/stripe/webhook/route.ts`, `lib/handle.ts`, `expected.json`).
- Commit 2 was **amended from `6fa0060`** after the reviewer's fix-then-pass round. **Commit 1's tree was unchanged by the amend**, so its isolation proof stands as taken.
- Branch tip `6509b35`; ≤2 commits honored.

## QA proof

*(All figures qa-verified independently at `6509b35`.)*

- **Suite:** `npm test` → **233 passed, 0 failed, 0 skipped, 25 files** (base `ed71d6e` was **213 / 24**) → **+20 tests, +1 file**. `npm run typecheck` (`tsc --noEmit`) **clean**.
- **Eval:** `npm run eval` → **11 fixtures, 11/11 decisions correct, 0 blocker false positives**, exit 0 (base was 10/10 over 10 fixtures).
- **Commit-1 isolation:** fresh worktree @ **`7683d74`** + `npm ci` → **223 passed / 25 files**, typecheck clean, `npm run eval` **10/10, 0 blocker FPs** → **GREEN**. The amend did not touch Commit 1's tree.
- **Behavior-preservation proof for Commit 1:** all 10 pre-existing fixtures' **full serialized reports byte-identical to base** — corpus `sha256 c1f187c5…`, 64694 bytes, identical at `ed71d6e` and `7683d74`. Emitted Findings therefore unchanged by the refactor.
- **Emit-only preserved:** `src/decision/engine.ts` blob **`54b0990…`** and `src/schema/index.ts` blob **`c1ae295…`** are **byte-identical across `ed71d6e`, `7683d74`, and `6509b35`** (independently re-verified at close). Also byte-identical to base: `src/checks/detectorKit.ts`, `src/checks/registry.ts`, `src/checks/lg015.ts`, `src/model/realClient.ts`, `src/scan/collect.ts`.
- **AT-23 determinism:** two fixed-clock `--offline` scans produce **byte-identical** serialized reports on **`broken-lg-006`** and on **`broken-lg-004`**. No set/hash iteration, no wall clock, no filesystem-order dependence in the locator.
- **AT-26:** the new deterministic `fail` carries non-empty evidence including a §5 `kind: 'absence'` entry.
- **CEILING INVARIANT holds** — `hasAppSignal ⊇ scanner.supported` undisturbed; **no unqualified `ready`** across all 11 fixtures + 3 probe repos. `detectStack`/`supported`/LG-015's gate untouched.
- **Safety grep:** scanner-safety grep **clean — zero secret patterns** across both commit trees, all packet files, and the new fixture (`broken-lg-006` uses `process.env` only; no committed key literals at all). No new runtime deps. No `process.env`/network/credential read in model or detector code. TEST-DATA POLICY satisfied on every surface.
- **Assertion-change budget:** the changed pre-existing assertion set stayed at **exactly the 4 enumerated in §8** plus **3 mechanical `handlerPath` → `handlerPaths`** renames authorized by §5 — **no budget breach**.
- **UI smoke:** N/A (detector + fixture packet; behavior exercised via the locator/lg006/scanModel tests, `npm run eval`, and the real bin under `--offline`).
- **Acceptance tests:** **AT-06 — deterministic half CLOSED via eval** on `broken-lg-006` (`LG-006` `fail` / `blocker` from the registry / `confirmed` / confidence 1 / evidence citing `webhook/route.ts`, and **no other blocker findings**). The **model-assisted half remains proven by the existing `FakeModelClient` integration tests** — AT-06 is **not** closed end-to-end by one fixture. **AT-27 stays closed** under the §8 restatement. AT-23/AT-26 hold.

## Review

- **Verdict: PASS — as fixed** (fix-then-pass; one must-fix-now round, fixed and re-proven).
- **The fix-then-pass round, recorded plainly:** Commit 2 **as first written manufactured a false blocker.** A *correct* repo — handler verifies the signature then delegates (`await handleStripeEvent(event)`, the cancellation branch living in `lib/events.ts`) — emitted `fail` / `confirmed` / `1` / blocker → **`not_ready`**, where base gave `ready_with_warnings`. qa reproduced it across all three trees. This is precisely the "may under-warn, must never manufacture a false blocker" convention the packet's own §7b invoked.
  - **Fix as landed:** the blocker is gated on **`hasCancellationSignalAnywhere` (repo-wide)** rather than on handler-file scope. `hasCancellationBranch` is **unchanged** and still drives the branch excerpt and the model-request supporting fact.
  - The reviewer **explicitly withdrew its own suggested tighter fix** (handler + first-order imports) after the builder demonstrated it reproduces the identical false blocker on path-alias imports (`@/lib/events` — the default `create-next-app` tsconfig), barrel re-exports, and two-hop delegation.
  - Also landed in the round: the offline `unknown` summary reworded to a repo-scoped claim, and a **comment-only** correction at `src/scan/scanner.ts:196-207` — qa proved comment-only three ways, including **byte-identical `tsc --removeComments` output**.
- **Codex second-eyes: NOT AVAILABLE — and no second-eyes pass ran.** `codex` is not on PATH in this environment and no Codex plugin is present. The review was single-model analysis plus empirical probes. Recorded so the proof standard is not overstated.
- **Adjudications the packet specifically required:** the `.filter()` decision (§7) — upheld, and confirmed to be a **safety requirement of part 2, not a tidiness preference of part 1**, hence correctly in Commit 1. The AT-27 restatement (§8) — upheld as honest; LG-006's M-layer portion still degrades to `unknown` with the documented reason, and the branch-absent case produces, needs, and fabricates **no** model result. The `broken-lg-004` repair (§6) — upheld: the fixture carried a real latent LG-006 defect; its manifest was the wrong artifact.
- **Product Trajectory Check:** the shared locator is the seam A-S2/LG-005 needs and now exists as a single contract with one traversal semantics — LG-005 plugs in as its third consumer. Engine + schema untouched; registry stays the single source of severity/blocker/provider; LG-006 carries **no** `externalVerification` on any branch (correct — it is non-external per §3), so the LG-012 external path (A-S7) is untrodden.

## Residue

**Deferred / follow-up packets:**

1. **LG-006 deterministic-disjunct fidelity gap — HALF closed, NOT closed.** The **offline/CI path is closed and proven**. **The online path still substitutes model judgment for a required deterministic signal (spec §4.3).** Measured consequences: branch-absent + model `fail`@0.85 → `not_ready` (fine); + model `fail`@<0.7 → engine Rule 4 demotes it to a warning → `ready_with_warnings`; + model **`pass`** → `outcomeForVerdict` maps to outcome `pass`, so engine **Rule 5 (which filters on `outcome === 'fail'`) never fires**, the finding contributes nothing — not even a warning — and the human report renders **"LG-006: pass"** on a repo where nothing can revoke access. Additionally the check's **layer assignment now varies with model configuration** (Layer D offline, Layer M online), contradicting §4.3's "layer assignment is fixed." **Unqualified `ready` is NOT reachable in a full scan** — qa proved the earlier `ready` result was an artifact of an isolated `decide()` probe missing LG-015 — but note qa's caveat: the only thing preventing `ready` is **LG-015's unrelated external-verification marker**, incidental rather than a designed guard, which would disappear if a future packet let LG-015 resolve its external verification. **Deliberately deferred:** the unconditional fix makes `fact:lg006.no-cancellation-branch` dead code and costs LG-006 its only contradiction-guard test, with no branch-present deterministic fact to replace it — a design question about **what the D layer surfaces to the model**, needing its own shaped packet. Expanding it here would have breached the §8 four-assertion budget by five.
2. **Shared-locator extraction — also NOT fully closed.** `lg003.ts:51` carries a **third** byte-identical copy of the webhook predicate. The P-005 residue's framing ("restated in lg004 and lg006") **undercounted its consumers**.
3. **NEW — the "never manufacture a false blocker" guarantee is offline-only.** On the *correct delegating* repo scanned **online**, LG-006 yields `fail` / `inferred` / 0.85 / blocker → `not_ready`. Two causes: `request.excerpts` carries only handler files, so the model never sees `lib/events.ts`; and `lg006.ts:240` still gates the fail-establishing supporting fact on the narrow handler-file signal. **Pre-existing — identical at `ed71d6e`, not introduced here** — and dormant unless a real model is configured at the bin. Fixing it means changing what the D layer surfaces to the model, which LG-005 will need anyway. **Bundle with item 1.**
4. **NEW — comment-suppression / recall cost.** `SUB_DELETED_RE` runs over raw file content and the new gate scans **all collected files, not just code**. A `README.md`, a `CLAUDE.md` agent spec, a commented-out branch, or a SQL migration comment merely *naming* `customer.subscription.deleted` flips the blocker to `unknown`. Direction-safe (under-warn only, never a false blocker), consistent with the LG-010 no-PSL and LG-001 source-path precedents, and it degrades to `ready_with_warnings` rather than silence — but the reviewer notes the suppression is **wider in SugarBee's actual target population**, since AI-built SaaS repos are heavy on generated READMEs and agent spec files. Disclosed in-code at `lg006.ts:186-192`.
5. **NEW standing constraint — `node_modules` exclusion is LOAD-BEARING.** LG-006's deterministic blocker survives *only* because `collect.ts:47` excludes `node_modules`: the `stripe` package's own typings name `customer.subscription.deleted`. **If a "scan vendored dependencies" mode is ever added, LG-006's blocker dies silently on every Stripe repo.**
6. **NEW (minor).** LG-006's fail evidence reuses the single-line anchor (`startLine:1, endLine:1`) rather than the whole-handler excerpt §7 specified; AT-26 still passes. And `fixtures/broken-lg-006/app/api/stripe/webhook/route.ts:1-2` imports at five levels where four is correct — harmless (fixtures are never compiled) but a future LG-005 consumer could trip on it.
7. **NEW (process) — no Codex second-eyes pass ran on this packet** (no `codex` on PATH, no plugin). Single-model review plus empirical probes. Recorded so the proof standard is not overstated.
8. **AT-06 is only HALF closed by eval** — the deterministic half. The model-assisted half stays proven by the existing `FakeModelClient` integration tests. **Do not claim AT-06 closed end-to-end by one fixture.**
9. **Carried forward:** **this repo has no default branch** — no `main`/`master`, locally or on the remote; only the two `claude/*` branches. "Never merge without go" currently has **no target**, and an eventual PR has nowhere to land. Needs a user decision at some point; explicitly not this packet's work.
10. **Frozen-status changes, disclosed deliberately:** **`lg004.ts` lost its "frozen / byte-identical" status** in Commit 1 (authorized by the residue extraction item; the compensating proof is the byte-identical serialized-report corpus), and **`broken-lg-004` was edited** despite prior receipts asserting it byte-identical — a deliberate, disclosed rebaseline of qa's regression baseline.
- **Carried forward (still open, non-blocking, unchanged by this packet):** SEC-5 delimiter-token hardening; spec-header staleness ("not yet implemented"); `report.repo.commit` null; banner locator artifact on fixture scans; §9.3's 15/15-recall and 18/18-decision thresholds (this packet took eval from 10 → 11 fixtures; it does **not** close §9.3); AT-16 `golden/` + AT-20/21/22 hostile fixtures; LG-008 optional warning for a lone committed DB URL; LG-003 provider-agnostic webhook presence; LG-010 last-two-labels domain heuristic (no PSL); LG-014 implicit/dashboard-only Sentry mechanisms; LG-001 source-path scanning.

**Known risks / carry-forward obligations still binding:**
- **MODEL-LAYER SAFETY BOUNDARY** — credential boundary at `bin/sugarbee.ts` only; `RealModelClient` bin-only, built-in `fetch`, no SDK, no `process.env` in model/detector code; `FakeModelClient` drives all tests; SEC-5 delimited untrusted data; cite-or-discard; at best `inferred`, capped 0.9. Preserved.
- **EMIT-ONLY ENGINE + SCHEMA** — blob SHAs unchanged; re-verified at close.
- **REGISTRY IS THE SINGLE SOURCE** — severity/blocker/provider via `makeFinding`; `blocker` never hardcoded.
- **externalVerification OBLIGATION** — LG-006 carries none on any branch (correct, non-external); **LG-012 remains the last pending external check (A-S7)**.
- **TEST-DATA POLICY** — <20 contiguous alphanumerics on every key-shaped fake, no Sentry-DSN shape, every surface incl. receipts and memory; never allowlist a secret.
- **CEILING INVARIANT** `hasAppSignal ⊇ scanner.supported` — undisturbed; add the regression assertion when `golden/`/AT-16 lands.

## Open boundaries (awaiting explicit go)

- **Push:** executed under the standing feature-branch authorization AFTER qa green + reviewer pass + safety grep, landing as a normal fast-forward **`ed71d6e..6509b35`**. Origin and HEAD are in sync (`0 0`).
- **NO merge to any default branch and NO pull request** — both await explicit approval, and per residue item 9 there is currently **no default branch to merge into**.
- No deploys, no secrets touched or allowlisted, no provider access. **No real model API call occurred** — `FakeModelClient` drove every test; the model layer's runtime egress only activates when a real model is configured at the bin, which did not happen.
