# H-003 — LG-006 delegated-opacity guard + the shared local-runtime-import seam

- **Packet id:** H-003 (hardening series `H-NNN` — third packet; **H-002's number was
  CONSUMED by the tiny-edit addendum `6d7cc80`**, see "Fold-in 1" below — that is why
  this packet is H-003, not H-002)
- **Title:** Extract the local-runtime-import predicate into a shared seam
  (`src/scan/imports.ts`) and close the LG-006 delegated-opacity defect (the same class
  H-001 repaired in LG-005, reviewer-found at H-001, carried as residue TOP ITEM)
- **Date:** 2026-07-30
- **Reviewer verdict:** **PASS** — the **first clean pass of the hardening series**
  (H-001 was fix-then-pass). **Single-model review — say so:** no second-model pass ran
  on H-003's own diff. Three record items below.
- **qa:** **GREEN 12/12**, all numbers independently re-derived.

## Scope

**In:**
- **C1** — extract the local-runtime-import predicate out of `lg005.ts` into
  `src/scan/imports.ts` (the **THIRD shared seam** after `webhook.ts` and `surface.ts`,
  taken per the **second-consumer precedent**: LG-006 is the second consumer, so the
  predicate moves to a seam before it is consumed twice). LG-005 migrates
  **byte-behavior-identically**.
- **C2** — the **LG-006 delegated-opacity guard**, purely additive.

**Explicitly out:**
- Any cure for **surfaced-delegate over-demotion** (record item 3 below) — the cure is
  import resolution by another name, **withdrawn at A-S1b**, correctly not attempted.
- Editing `specs/evidence-allocation-and-phase-boundaries.md` (the §7
  correction-convention question remains the next packet's problem, per H-001 residue).
- Any semantic change to LG-005 (C1 is migration only, proven byte-behavior-identical).
- Fixtures (eighth consecutive zero-movement packet).

## Commits and base

- **Base: `4934927`** ("Add CI: run tests, typecheck, and eval on every push and PR").
  Verified at close: `git merge-base HEAD 4934927` → `4934927`, and `4934927` is ALSO
  the remote tip of `claude/sugarbee-project-handoff-tdfylf` (`git ls-remote`) — **the
  branch is pushed through the base; H-003's two commits are LOCAL.**
- **C1 `5bfacc8`** — *Extract the local-runtime-import predicate into
  src/scan/imports.ts* — 3 files, +261/−100 (`src/scan/imports.ts` NEW 128 lines;
  `src/checks/lg005.ts` −100; `tests/scan/imports.test.ts` NEW, 19 tests).
  - The **four regexes moved verbatim and stay module-private**; the exported surface is
    `hasLocalRuntimeImport` + `isLocalSpecifier` + the new `topLevelDirectories`.
  - **Migration proof, byte-level:** `tests/checks/lg005.test.ts` **blob-identical**
    base↔tip; **all 4 regexes byte-identical**; the LG-005 `InferenceRequest`
    **sha256-equal base↔tip** (`62555ab4fa7d6f58…`).
- **C2 `1135a6c`** — *LG-006: demote a model-asserted fail to unknown when a handler
  carries local runtime imports* — 3 files, +255/−10; **the guard itself is purely
  additive in `lg006.ts` (+71/−0)**.
  - `handlersWithLocalRuntimeImports` is computed **handlers-only, AFTER request
    assembly** (so transmitted bytes cannot depend on it).
  - Demotion at **`lg006.ts:530-531` iff ALL FOUR conjuncts hold**: the contract result
    is a finding **∧** outcome `fail` **∧** **`classification === 'inferred'`** **∧**
    armed handlers > 0.
  - **Why the `inferred` conjunct exists (this is the design point):** LG-006 —
    unlike LG-005 — carries a **live fail-establishing fact path**
    (`fact:lg006.no-code-cancellation-signal`). On that path a model `pass` classifies
    `contradictory` and **`inference.ts:140` forces `fail`** (engine rule 5 →
    `requires_confirmation`). **Demoting a `contradictory` fail would let a heuristic
    override a deterministic fact.** The `inferred` conjunct structurally excludes it.
  - The **Layer-D blocker (`lg006.ts:442-467`, no-signal-anywhere confirmed fail — the
    check's offline teeth)** and the **offline AT-27 `unknown` return** are **provably
    unreachable by the guard**.
  - **Recall cost disclosed in-source twice** (as at H-001: the online inferred-fail
    blocker is reachable only on handlers whose side-effect path is inline).

## QA proof (GREEN 12/12 — exact counts, all independently re-derived)

- `npm test` at tip: **430 tests / 31 files** (base `4934927` was **404/30**; C1 adds
  `imports.test.ts`'s 19, C2 adds 7).
- **Commit-1 isolation:** **423 / 31** at `5bfacc8` in a **fresh `npm ci`** worktree —
  GREEN.
- `npm run typecheck`: **exit 0**.
- `npm run eval`: **11 fixtures / 11-of-11 decisions / 0 blocker false positives**.
- **All 11 offline report hashes AND byte sizes byte-identical to the A-S3 baseline**
  (convention: `<ROOT>` token + fixed clock `2026-07-23T00:00:00.000Z`, first 16 hex) —
  including `broken-lg-006` **`a2e9caf5144d4559` / 8678 bytes**, decision unchanged.
- **Diff confinement: exactly 6 files** across the two commits, with **blob-SHA
  identity** base↔tip on `surface.ts`, `webhook.ts`, `collect.ts`, `scanner.ts`, all of
  `src/model/*`, `lg009.ts`, `engine.ts`, the schema, `questionPin.test.ts`, and
  `lg005.test.ts`.
- **`fixtures/` tree `a981446bac76039147d93efedd14a092c2aeadc1` — EIGHTH consecutive
  zero-fixture-movement packet** (A-S1c, A-S2, A-S3, T-001, T-002, EV-001, H-001,
  H-003).
- **Assertion budget: 0 of 2 spent** — 5 pre-existing repo-call line re-bases in
  `scanModel.test.ts`, itemized; **zero `expect` lines changed**.
- **Safety grep: clean** — sole hit is the pre-existing `STRIPE_WEBHOOK_SECRET`
  env-ref idiom inside a test-fixture handler string, mirroring base.
- **Negative controls red on base:** the repro FAILED on base with
  `expected 'fail' to be 'unknown'` at `lg006.test.ts:484` — **the false blocker
  itself**; the shape proof ran GREEN on base (proves the shape, not the fix).
- **Invariance pins (a)–(d) GREEN on BOTH base and tip.**

## Reviewer — PASS, three record items (also carried in residue)

1. **Fact-agreeing-fail demotion corner (probe P6).** A model `fail` that **AGREES**
   with a live `fact:lg006.no-code-cancellation-signal` classifies **`inferred`**
   (facts force a classification only on **disagreement** — `inference.ts:96`) and
   therefore **IS demoted** by the guard. **Sound on the merits:** the armed-handler
   configuration is precisely the one where the fact's implication can be false —
   per-event dynamic dispatch can downgrade without any file naming the event. **Not
   stated in-source — recorded here.**
2. **The combined-shape sentence, stated as ONE sentence:** *a prose-suppressed D
   blocker plus an armed handler means LG-006 has NO blocker-capable online path at all
   on that configuration* — model `fail` → demoted `unknown`; model `pass` →
   `contradictory` fail → rule-5 warning; offline → AT-27 `unknown`. **Both halves are
   disclosed in-source; the INTERSECTION was not — this receipt is the disclosure.**
3. **Surfaced-delegate over-demotion.** When the delegate IS surfaced (it names the
   event) and the model still says `fail`, the guard still demotes. **Direction:
   under-warn** — identical to LG-005-since-H-001. The cure is import resolution by
   another name (**withdrawn at A-S1b**) — **correctly not attempted here.**

**Second eyes on H-003's own diff: NOT obtained** (single-model review; Codex re-review
of PR #1 requires a push plus an explicit `@codex review` comment, and H-003's commits
are local). Context for weighting: **Codex has found SIX real defects across three
reviews of this repo, versus ZERO found by the green suite.**

## Fold-ins (owed debts, all landed in this close)

1. **H-002 addendum — tiny-edit lane, no receipt of its own; recorded here.**
   Commit **`6d7cc80`** (*"H-002: side-effect-only imports arm the delegated-opacity
   guard"*) repaired a **Codex P2 re-review finding**: side-effect-only imports
   (`import './x'`) did not arm the H-001 guard. **`SIDE_EFFECT_IMPORT_RE` added —
   quoted specifiers only**, correctly, because **static import declarations cannot
   take template literals**. Suite **402 → 404**; negative control **red pre-fix**;
   **all 11 offline hashes unmoved**; the Codex thread was **replied to and resolved**.
   **Series consequence: H-002 is consumed by that fix — hence THIS packet is H-003.**
2. **Push-state supersession.** The branch is **PUSHED through `4934927`**: H-001's
   `72e42db` + `4dfea24`, the H-001 close `3143100`, H-002's `6d7cc80`, and the CI
   workflow `4934927`. **Memory's "local and unpushed / awaiting push go" notes (H-001
   close) are SUPERSEDED.** Authorization trail: the pushes ran under the project's
   **standing authorization (HANDOFF §8: qa green + reviewer pass + safety grep,
   fast-forward only)** plus the **user's session directives**. Also new:
   **`.github/workflows/test.yml` exists — CI (test + typecheck + eval) on every
   push/PR; the repo's FIRST CI ever.**
3. **PR #1 babysit.** The session subscribed to PR #1; **six quiet checks**, then the
   **Codex re-review cycle** (which produced the H-002 finding). **ALL Codex threads
   resolved except the deliberately-open upstream P2** (`project-bootstrap.sh:209`
   cache gate — **owed upstream; never write to canonical ClaudeOrchestrator**).
   Check-in timer armed (~3h re-arming). **Codex score to date: SIX real defects
   across three reviews vs zero from the green suite.**
4. **Capability-context reconciliation** (from the user's 2026-07-30 scratchpad note;
   live-evidence-only claims; the note authorizes nothing):
   - Adopt the **four-state capability ladder — listed → connected → callable →
     invoked** — as the status vocabulary, **refining T-001's ACTIVE /
     DURABLY-CONFIGURED semantics** and the router's no-route-to-unverified rule.
     Recorded in `current_state.md` stable facts; **`tool_router.md` is MANAGED and was
     NOT touched.**
   - **Per-container framing:** capabilities absent here are **"unavailable in this
     container," not globally absent**. Serena is **repository-scoped via a committed
     `.mcp.json`** — LaunchGraph has none (verified) — so **T-002's "serena absent"
     claim is hereby sharpened to per-container.**
   - **ui-ux-pro-max: LISTED in this session, not invoked.**
   - Existing containers **do not reliably hot-load newly merged repo MCPs**; connector
     rosters **can flap**.
   - **Mutating connector calls (Apollo etc.) remain STOP-gated.**
   - **Do NOT change repo config to mirror other repos** — assess each repo
     independently.
5. **Field-test state (task-tracked).** Harness **built and e2e-verified** at
   `scratchpad/field-test/` (proxy clone, offline scan, TSV scoring); **corpus of 11
   forks awaiting USER clicks**. **Field finding FT-1:** the SugarBee **self-scan**
   returns **`not_ready` with 6 confirmed blockers, ALL evidence drawn from `fixtures/`
   and `tests/`** — **fixture/test-directory contamination manufactures false confirmed
   blockers on any repo carrying deliberately-bad example code.** Candidate packet: the
   **test-directory scan boundary** — a design decision the spec never made (residue
   doctrine: *"where the scan boundary belongs is a design decision, not a patch"*).
   Also noted: the runner's **check-id column bug** (reads the wrong key).
6. **EV-001/H-001-era open items still standing:** **G1d** (repo default → `main`) and
   **G5** (three stale ClaudeOrchestrator branches) remain **USER-ONLY actions**; the
   field test's **kill criterion stands**.

## Residue (deferred / follow-ups / risks)

- The three reviewer record items above (fact-agreeing-fail demotion corner; the
  combined-shape sentence; surfaced-delegate over-demotion) — carried in
  `build-os/memory/residue.md`.
- **FT-1 fixture/test-directory contamination** — candidate scan-boundary packet
  (design decision, not a patch); plus the field-test runner's check-id column bug.
- The upstream Codex P2 (`project-bootstrap.sh:209`) stays deliberately open on PR #1 —
  owed upstream, never fixed from here.
- The evidence-allocation spec's §7 correction convention remains the next packet's
  problem (now three repairs behind: §7.1, §7.2, and the H-002/H-003 class extension).
- Out-of-class specifier residue on the guard (bare npm, `#imports`, tsconfig aliases)
  carries forward from H-001, now shared by both consumers via `imports.ts`.

## Open boundaries (awaiting explicit go)

- **`5bfacc8` and `1135a6c` are LOCAL — not pushed.** Remote tip is the base
  `4934927`. **No push, no merge, no deploy without explicit go.** (The standing
  HANDOFF §8 authorization covers fast-forward pushes after qa green + reviewer pass +
  safety grep; the push itself is left to the orchestrator/user lane — the archivist
  never pushes.)
- This receipt and the memory updates are **NOT committed** — the user/orchestrator
  handles commit, per the standing "hold all `build-os/` commits until close"
  constraint. **Managed-path churn check run before these writes: clean.**
- PR #1 remains a **review vehicle, not a merge proposal**. No secrets touched; no
  real model API call occurred in H-003 (byte-identity proofs construct requests; they
  do not transmit them).
