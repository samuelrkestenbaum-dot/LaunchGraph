# Receipt — EV-001: Allocate the evidence method per check; restate the Phase-1 boundary

- **Date:** 2026-07-28
- **Branch:** `claude/sugarbee-project-handoff-tdfylf`; local tip **`258cc70`**. `origin/main` is at **`8652bac`** — **three commits unpushed** (`c7baff9`, `9706d87`, `258cc70`).
- **Branch base (merge-base):** **`9706d87`** (*Close T-002: receipt and Build OS memory*), a **strict ancestor** of the tip. `git merge-base HEAD origin/HEAD` → `8652bac`. Fast-forward lineage, no fork.
- **Guide:** canonical ClaudeOrchestrator at `/home/user/ClaudeOrchestrator`, tip **`0a63f66`** — **read only, never written.** Verified still `0a63f66` at close.
- **Working tree at close:** clean (`git status --short` empty) before these build-os writes.

---

## NEW SERIES — `EV-NNN`, and the rule that forced it

**`EV-NNN` = evidence-method allocation and phase-boundary doctrine: packets that produce a durable decision artifact and ship no product code.**

The series was **forced by existing namespace rules, not chosen by preference**:

- **`P-0NN` is FROZEN** — it collides with canonical ClaudeOrchestrator's `P-001…P-025`.
- **`A-S*` is reserved for the detector fork**, under the standing rule that **a non-detector packet must never consume a slice number**. Consuming **A-S4's** number would additionally have made a **pause look like a completion** — the worst possible record, given A-S4 is paused, not done.
- **`T-NNN` is the Build OS *runtime* series** (T-001 adoption, T-002 the source pin). EV-001's subject is the **product's evidence architecture**, not the build system.

**Rejected, with reasons:** `D-` and `M-` (collide with Layer D / Layer M), `PL-` (reads too near the frozen `P-`), `S-` (reads as the A-S slice suffix).

---

## Scope

**In:**
- A new authoritative document, `specs/evidence-allocation-and-phase-boundaries.md`, assigning an **evidence method** to each of the 15 checks and naming the owning **`PRODUCT_SCOPE.md` §35 phase** for every non-Phase-1 half.
- Four evidence modes recorded: **repository analysis, provider-native inspection, live probes, agent remediation.**
- A **15-row matrix in two panels** (identical id set to `P1§3`), separating the **general rule** (who holds the fact) from the **vendor mechanism** (how it is fetched today), so the document survives a vendor changing its surface.
- **LG-003 and LG-012 modelled as SPLITS, not reassignments** — each keeps a repository-deterministic blocking half in Phase 1.
- Every divergence from `P1§13` recorded as a **PROPOSED** amendment (§9), **none enacted**.
- Exactly **two lines** of status/cross-reference in `specs/phase-1-repository-auditor.md`.

**Out (explicit):**
- **ALL product code.** Zero movement under `src/`, `tests/`, `bin/`, `fixtures/`, `package.json`, `package-lock.json`, `vitest.config.ts`, `tsconfig.json`.
- **No detector work.** No LG-check added, changed, or removed. Detector coverage untouched at **11 of 15 implemented — QUALIFIED: 8 can move an offline decision; 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.**
- **No amendment to `P1§3` or `P1§13`.** `P1§3` remains the sole authority for what a check *means*.
- **No fix to the two named open defects** (LG-005 delegated-helper blind spot, LG-009 unbanded elision). They are **located and owner-assigned** in §7, not repaired.
- **No push, no merge, no deploy.**

---

## Commits

- **`258cc70`** — *EV-001: allocate evidence method per check; restate the Phase-1 boundary* — **2 files, +497 / −2.**
  - `specs/evidence-allocation-and-phase-boundaries.md` — **NEW, 495 lines.**
  - `specs/phase-1-repository-auditor.md` — **+2 / −2**, header status only:
    ```
    - - **Status:** Specification — approved scope, not yet implemented.
    -   Implementation awaits explicit go.
    + - **Status:** Specification — approved scope; implementation underway, one gated packet at a time.
    +   Evidence allocation: [phase boundaries](evidence-allocation-and-phase-boundaries.md).
    ```
- **ONE commit. ≤2 commits: held.**
- **Amend history:** originally `e3c0425`, **amended once** to fold in the qa DC-13 fix and the four reviewer-required fixes. Per the standing `--amend`-is-unsafe constraint (A-S2), **HEAD was verified to be our own unpushed commit before amending.**
- **Zero product code.** Verified by subtree hash, not asserted.

---

## THE CENTRAL FINDING — the reframing is a RECOVERY of the founding architecture, not a pivot

**The user's strategic reframing does not depart from the approved scope; it restores it.** `PRODUCT_SCOPE.md` §35 already assigns the phases:

| §35 line | Phase | Name |
|---|---|---|
| `:1598` | Phase 1 | Repository auditor |
| `:1614` | Phase 2 | **Agent coordinator** |
| `:1626` | Phase 3 | **Provider inspector and router** |
| `:1640` | Phase 6 | **Verification** |

and the Phase 1 spec's §13 deferral table **already encodes most of the allocation**:

- `:646` read-only provider API inspection → **Phase 3**
- **`:647` external-side resolution of LG-003 / 010 / 012 / 014 / 015 → Phase 3**
- `:648` DNS / TLS / domain probes → **Phase 3**
- `:651` canonical customer-journey execution and the unqualified `Ready` → **Phase 6**

**So the four evidence modes map onto an already-approved phase map.** The document REFINES; it does not replace.

**RECORDING "PHASE 2" FOR PROVIDER / LIVE EVIDENCE WOULD HAVE BEEN WRONG.** §35's Phase 2 is the **agent coordinator**. Writing "Phase 2" for provider inspection would have implied the phase numbering was **informal** — and informal numbering is **exactly the crack the boundary leak comes through**. The phase numbers are load-bearing; treat them as citations, not shorthand.

---

## LG-009 IS THE SINGLE GENUINE DEPARTURE FROM THE SPEC

**Verified at source:**
- `P1§3:120` marks LG-009 **`External component: No`**.
- LG-009 is **ABSENT** from §13's `:647` external-resolution row.

Everything else in the allocation is already in §13. **LG-009 is the one place the implementation's experience contradicts the spec's assumption.** The spec assumed tenant isolation was **repository-determinable**; implementation proved it is not — at a cost of **a full packet, seven false-certification routes across four adversarial passes, and both deterministic branches withheld** (A-S3).

**Recorded as a PROPOSED amendment (§9.1), NOT enacted.** `P1§3`'s `External component: No` cell **still wins today**. Panel A and Panel B both carry an explicit divergence marker saying so; §3.7 is titled *"why LG-009's primary authority **should** move…"*, not *"has moved"*.

---

## QA proof — GREEN on DC-1..DC-12, DC-14, DC-15; RED on DC-13 (fixed in the amend)

- `npm test` — **387 tests / 30 files.** `npm run typecheck` — **exit 0.** `npm run eval` — **11 fixtures / 11-of-11 decisions / 0 blocker false positives.** Identical at base and tip.
- **Commit-1 isolation:** GREEN. Commit 1 is the only commit, and it touches no executable file.
- **Diff is exactly 2 files.** Subtree hashes **byte-identical base ↔ HEAD**: `src` `30f116ff`, `tests` `496e919b`, `bin` `49442941`, `build-os` `b4be19ff`, `.claude` `1c0e5943`.
- `fixtures/` tree **`a981446bac76039147d93efedd14a092c2aeadc1`** — **SIXTH CONSECUTIVE ZERO-FIXTURE-MOVEMENT PACKET** (A-S1c, A-S2, A-S3, T-001, T-002, EV-001).
- **Assertion budget: 0.** No test file changed.
- **`P1§3`, `P1§10`, `P1§13` byte-identical base ↔ HEAD** — the packet demonstrably did not amend the authority it defers to.
- **Both panels carry 15 unique ids; exactly 30 `^| LG-` lines** — no check silently dropped or duplicated.
- **Boundary grep: zero imperatives, zero SDK names, ZERO URL/HOST LITERALS DOC-WIDE.** A document about provider mechanisms that names no host is a document a builder cannot lift a call from.
- **Safety grep clean; zero key-shaped literals.** TEST-DATA POLICY intact.
- **ClaudeOrchestrator clean, still `0a63f66`.**

### DC-13 — the qa RED, and the disagreement it exposed. RECORD THIS.

**Finding:** the document cited the LG-005 defect site as `src/checks/lg005.ts:205`. **That line is `prefers` — a *ranking* predicate that excludes nothing.** The **excluding** predicate the surrounding prose describes is **`selects`, at `src/checks/lg005.ts:203`.** Corrected in the amend.

**THE DISAGREEMENT AND ITS RESOLUTION — carry this as a process note.** The reviewer **independently asserted that `:205` IS `selects`.** The main session read the source directly and settled it:

```
src/checks/lg005.ts:203    selects: (content) => IDEMPOTENCY_SIGNAL_RE.test(content),
src/checks/lg005.ts:204    anchor: IDEMPOTENCY_SIGNAL_RE,
src/checks/lg005.ts:205    prefers: [(content) => WEBHOOK_DEDUP_RE.test(content)],
```

**qa was right; the reviewer was mistaken on that single point.** Re-verified independently by the archivist at close — the three lines above are quoted from the tree at `258cc70`.

**The process lesson is the durable part: two independent reviews disagreed on a one-line fact, and only DIRECT VERIFICATION OF THE SOURCE settled it.** Neither report's confidence was evidence. Note also that **T-002's residue and `current_state.md` both record the Codex LG-005 finding at `:205`** — that citation is **inherited from Codex and is off by two**; the defect is real, the line number was wrong, and prior receipts are append-only, so the correction lives here.

### qa's independent corroboration of the Codex LG-009 finding — and the open question, now ANSWERED from source

qa corroborated from source that the trailing band is genuinely dropped:

- `src/scan/surface.ts:174` — `const elidedByBand = new Array<number>(bands.length + 1).fill(0);` — the `+1` is the **trailing unbanded slot**.
- `src/checks/lg009.ts:423` — `const specificElided = (delegated.elidedByBand[0] ?? 0) + (delegated.elidedByBand[1] ?? 0);` — sums **only** indices 0 and 1.

**THE OPEN QUESTION WAS: does LG-009 rank across THREE ordered bands (in which case `[0]+[1]` misses a real band AND the trailing slot — a wider hole than Codex described)? ANSWER, ESTABLISHED FROM THE CODE AT CLOSE, NOT FROM EITHER REPORT:**

`src/checks/lg009.ts:412-415` passes **exactly TWO** `prefers` predicates:
```
    prefers: [
      (c) => SCOPING_MECHANISM_RE.test(c),
      (c) => TENANT_COLUMN_RE.test(c),
    ],
```
Therefore `bands.length === 2`, `elidedByBand.length === 3`, and `[0] + [1]` **omits exactly index 2 — the unbanded remainder, and nothing else.**

**CONSEQUENCE — TWO CORRECTIONS:**
1. **The hole is exactly what Codex described** (the trailing generic band). It is **not** wider.
2. **The "three ordered bands" description carried in memory is WRONG for LG-009** — three is the length of `elidedByBand`, not the number of ranking bands. That misreading is what created the phantom wider hole. **Corrected in `residue.md` at this close.**

The fix packet should still re-derive this from the code rather than from this receipt; the reading is recorded here so that **the question does not have to be re-opened from scratch**, not so it can be trusted unchecked.

---

## Reviewer — fix-then-pass (recorded as fixed). All four required fixes APPLIED in the amend.

### Charge 1 (boundary leakage) came back UNFOUNDED — and that is a real result, not an absence of one

The reviewer grepped **every permissive modal in the file** and found **all ten sit inside a negation**. It credited the construction on three independent grounds:

- **§4 restates SEC-2 / SEC-3 as binding *independently* from `P1§10`** — so the ban does **not** depend on §4.1 surviving.
- The document's own answer to §1.3 is the correct one: ***"An allocation table is an instruction. It cannot and does not satisfy a structural requirement."***
- **Every "Vendor mechanism today" cell naming a real surface sits in a row whose phase cell says Phase 3 or Phase 6** — **no mechanism appears un-phased.**

**Verdict: a builder cannot cite this text to justify instantiating a provider SDK.** That was the packet's primary safety obligation and it was independently tested, not assumed.

### Fix 1 — §1 DECLARED THE DOCUMENT'S OWN GATES NON-NORMATIVE. The sharpest finding of the packet.

§1 read *"normative for exactly two things… non-normative for everything else."* But **§4 (FORBIDDEN), §5.3 (GATE) and §6 (non-deletion) are none of those two things** — so **§1 rendered all three non-normative by its own terms.**

§4 survives on borrowed authority (SEC-2/SEC-3). **§5.3's gate and §6's clause had NO HOME DOCUMENT.** A future builder could cite **§1 — the most authoritative-looking clause in the file** — to **discard exactly the two rails while keeping the reclassification.**

**FIXED:** §1 now **enumerates §4, §5.3 and §6 as additionally normative**, framed as ***restrictions only — they grant nothing***, and states that none of them may be read as permission.

### Fix 2 — §6's release valve was DISJUNCTIVE where residue R1 is CONJUNCTIVE

The clause read *"until a packet with the R1 shape **or** Phase-3 provider evidence says otherwise."* **A Phase-3 builder holding a Supabase advisor read could claim requirement 3 alone discharges the condition and reinstate a certifying branch — dropping requirements 1 and 2.** And **§5.3's gate does NOT backstop this**, because reinstating the `confirmed` **fail** branch need not make Rule 7 reachable.

**FIXED:** now **conjunctive**, citing R1 in full — **(1)** a real SQL statement parser, **(2)** a default-deny defeater denylist, **(3)** evidence the connecting role is subject to the policies. **Provider evidence discharges (3) ALONE and is explicitly NOT grounds for reinstatement.** The no-backstop point is stated inside the clause so it travels with it.

### Fix 3 — §5.1 row 2 was the compression hazard, and was also simply wrong

It read *"`ready` becomes reachable **for infrastructure checks**"* — **category-confused** (there is **no per-check `ready`**; `P1§3` fixes per-check *outcomes*, and `ready` is a **global Rule 7 decision**) and **false** (after Phase 3 alone, LG-012 still carries a pending external verification whose proof is **Phase 6**, so **Rule 6 still fires**).

**FIXED:** the row now names **which** pending verifications provider state resolves (**LG-003 / 010 / 014 / 015**), states that **Rule 7 stays blocked** while LG-012's Phase-6 proof is outstanding **and** until §5.3's gate is discharged, and states plainly that **there is no per-check `ready`.**

### Fix 4 — D-2's replacement was a NET LOOSENING

Removing *"Implementation awaits explicit go."* was within a status-only edit — but the replacement left **the authoritative spec with NO explicit-go language anywhere.** **The packet whose entire purpose is to stop permission being read into prose had deleted the one sentence saying permission is required.**

**FIXED:** the header now reads *"implementation underway, one gated packet at a time."* **Still exactly +2 / −2.**

### Recommended items, also applied

- LG-009's **Panel A and Panel B** cells now carry explicit divergence markers: *"proposed — §9.1; `P1§3` marks it `External component: No` and that cell wins today."*
- **§3.7 retitled** to *"…**should** move (proposed…)"*.
- **A Panel A banner** states that **no row in that panel describes present permission.**
- **The six-class defeater taxonomy is now LOCATED AND INLINED in §5.3.** It was **invoked three times and never located**, which made **the gate undischargeable** — a gate whose terms are not in the document cannot be satisfied by reading the document.
- **`SEC-8` renamed `SEC-8′` (proposed)** — **a non-normative section may not mint identifiers into `P1§10`'s SEC namespace.**

### NOT applied — carried as residue

- **Reviewer item 7: section-number collision across the two documents.** Bare `§3` / `§7` / `§8` are used in **both** senses. §1 qualifies the path at the one place it matters most, and `P1§` qualifiers were introduced in the repaired passages, **but a full sweep was out of budget.**
- **The `:116` "and/or" paraphrase drift on LG-013.**

---

## Second eyes — NOT OBTAINED on this diff. Recorded honestly.

**No independent second-model pass happened on EV-001.** The reviewer confirmed **`codex` is absent from PATH** and **no plugin route exists**; the only live Codex channel on this surface is the **GitHub PR bot, which requires a push** — and nothing was pushed.

**Do not let a future session read T-002's "second eyes obtained" as covering this packet.** It does not.

**STANDING QUALIFIER, from the reviewer's own caveat, and it should now travel with every doctrine packet: PROSE REVIEW IS WEAKER EVIDENCE THAN CODE REVIEW.** There is **no test that fails if a sentence is misread.** The failure mode is **a reading three sessions from now, under delivery pressure** — which is precisely why the four fixes above were treated as required rather than stylistic.

---

## TRAJECTORY FLAG — the most important forward item on this receipt

**THIS IS THE THIRD CONSECUTIVE PACKET WITH ZERO DETECTOR MOVEMENT — T-001, T-002, EV-001 — against FOUR UNIMPLEMENTED DETECTORS (LG-007, LG-011, LG-012, LG-013) and TWO NAMED OPEN DEFECTS.**

**Doctrine is now well-capitalized relative to code.** The three packets were each individually justified; the *pattern* is the risk.

**THE NEXT PACKET SHOULD BE CODE.** The leading candidate is **LG-005's delegated-helper blind spot** (EV-001 §7.1, **`src/checks/lg005.ts:203`**):

- It is a **shipped, repository-authoritative detector** that can **currently manufacture a false blocker**.
- **No provider and no probe can fix it** — it is squarely inside Phase 1's own authority.
- **Hardening it is a better use of the same effort than a twelfth detector**, and it **honours the A-S4 pause rather than working around it.**

---

## Residue

- **Section-number collision between the two spec documents** (reviewer item 7) — bare `§3`/`§7`/`§8` are ambiguous; a full `P1§` qualification sweep is owed.
- **The `:116` "and/or" paraphrase drift on LG-013** — unapplied.
- **The two open product defects remain open, deliberately:** LG-005 delegated-helper blind spot (`src/checks/lg005.ts:203`) and LG-009 unbanded elision (`src/checks/lg009.ts:423` / `src/scan/surface.ts:174`). **EV-001 located and owner-assigned them; it fixed neither.**
- **§9's proposed amendments are PROPOSED.** §9.1 (LG-009's authority moving to the provider) is **not enacted**; `P1§3:120`'s `External component: No` **wins today**.
- **The `.sql`/`.prisma` prompt-surface SEC-5 adjudication is still owed** (unchanged by this packet).
- **No second eyes on this diff** (above).
- **The self-updating bootstrap is self-triggering and the pin FAILS OPEN** (T-002 residue TOP ITEM) — **run `git status` on managed paths before any build-os commit.** Honoured at this close: the tree was clean before these writes.

---

## Open boundaries (awaiting explicit go)

- **`258cc70` IS LOCAL AND UNPUSHED.** `origin/main` is at `8652bac`; **three commits are unpushed** (`c7baff9`, `9706d87`, `258cc70`).
- **`build-os/receipts/EV-001.md` and these memory updates are NOT committed.** The user handles commit and push, per the standing "hold all `build-os/` commits until close" constraint. **≤2 commits held (one commit).**
- **Nothing merged, deployed, published, or allowlisted. No secrets touched. NO REAL MODEL API CALL occurred in EV-001** — the packet ran no scanner and no model client.
- **G1d remains OPEN, USER ACTION ONLY** — set the GitHub repository default branch to `main`.
- **G5 remains OPEN, USER ACTION ONLY** — three stale ClaudeOrchestrator branches; **only ever delete the ClaudeOrchestrator one, verify the repo slug first.**
- **A-S4 remains PAUSED BY USER DIRECTIVE.** All eleven binding shaping inputs are **preserved, not cleared**, in `build-os/packets/active_packet.md`. **The thesis is not abandoned; the build sequence is adjusted.**

---

_Closed by the archivist, 2026-07-28. Receipts are append-only: this receipt corrects the `lg005.ts:205` line citation inherited from Codex via T-002, but `build-os/receipts/T-002.md` is NOT edited._
