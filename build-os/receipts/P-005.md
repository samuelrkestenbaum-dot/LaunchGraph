# Receipt — P-005: Phase 1 Repository Auditor — model-layer substrate + first D+M check (LG-006) — Fork Branch A, Slice 1 (A-S1)

- **Date:** 2026-07-23
- **Branch base (merge-base):** LaunchGraph repo, branch `claude/launchgraph-product-scope-43pgdx`, based at `a4ffbf5` (P-004 tip) — verified via `git merge-base` at go (exact match, tree clean, 181 tests green). Both packet commits sit directly on that base and were pushed to the feature branch as a normal fast-forward (`a4ffbf5..fb5663b` on origin).

## Scope
- **In:** The **model-layer substrate** (the FORK's Branch A foundation) + the FIRST Layer D+M detector, **LG-006** (missing cancellation handling; non-external), threaded through the scanner and CLI.
  - **New source (Commit 1, all additions — touched no existing file):**
    - `src/model/client.ts` — the injected `ModelClient` interface + `InferenceRequest`/`InferenceResult` types + `buildUntrustedDataEnvelope` (the SEC-5 wrapper that fences already-redacted excerpts between explicit untrusted-data delimiters labeled "not instructions").
    - `src/model/fakeClient.ts` — `FakeModelClient`, a scripted client used in **ALL** tests (no real network, ever).
    - `src/model/realClient.ts` — `RealModelClient` skeleton over the **built-in global `fetch`** (no SDK, zero new runtime deps); imported ONLY by the bin.
    - `src/model/inference.ts` — the pure D→M contract enforcer (`runInferenceContract` / `InferencePresentation`): discards uncited model claims (cite-or-discard), caps confidence at 0.9, classifies at best `inferred`, emits `contradictory` when the model verdict conflicts with a deterministic supporting fact (`establishesVerdict`), and builds the `Finding` via `makeFinding`.
    - `src/checks/lg006.ts` — `surfaceLg006Candidates` (Layer D: locate the subscription webhook handler, enumerate event names, detect the cancellation branch, gather redacted excerpts; `not_applicable` when there is no subscription webhook) + `interpretLg006` (no judgment → `unknown` "model layer disabled"; judgment → `inferred` fail/pass capped 0.9).
    - Tests: `tests/checks/lg006.test.ts`, `tests/model/client.test.ts`, `tests/model/inference.test.ts` (Commit 1); `tests/scan/scanModel.test.ts` (Commit 2).
  - **Additive edits (Commit 2 only — behavior-additive, sync/offline path byte-identical to base):** `src/scan/scanner.ts` (sync `createScanner` emits LG-006 `unknown` offline; new async `scanWithModel` for the online model path — the sync two-arg path unchanged), `src/cli/run.ts` (extracted a shared `emitReport` + added an async overload; the sync two-arg path unchanged), `bin/launchgraph.ts` (constructs `RealModelClient` from config read at the bin; `--offline` / no-model → sync path). These three are the ONLY pre-existing files changed.
  - **Load-bearing new behavior — the model layer is EMIT-ONLY:** `engine.ts` and `schema/index.ts` are **byte-identical to base** (blob SHAs unchanged). The model layer only emits the right classification/confidence; the existing engine Rules 3/4/5/6 + schema (inferred ≤ 0.9, confirmed === 1.0) do the rest. Model results are at best `inferred`, capped 0.9, **never** `confirmed`; `contradictory` when the model verdict conflicts with a deterministic supporting fact; uncited model claims discarded. `--offline` / model-unconfigured → LG-006 emits `unknown` ("model layer disabled") — **closes AT-27's owed half**; deterministic checks unaffected; the report stays valid + deterministic.
  - **The credential boundary (the safety gate):** `RealModelClient` is imported ONLY by `bin/launchgraph.ts`, uses the built-in global `fetch`, and reads no `process.env` itself — config is injected at the bin, which is the credential boundary for LaunchGraph's OWN model API (spec §10's one permitted egress; NOT provider credentials — SEC-3 intact). No model/detector code reads `process.env`, `fetch`, or any credential.
- **Out (explicit):** A-S2..A-S7 (LG-005/011/013/009/007/012 — LG-012 the external one LAST). Eval-harness model wiring + the `broken-lg-006` eval fixture + AT-06-via-eval (LG-006 fail/pass proven here via fake-model integration tests instead). The behavioral AT-21 hostile fixture (the SEC-5 mechanism is built + unit-tested now; the `hostile/` fixture is deferred). NO schema/engine change (emit-only). NO Express/Vite/Fly (Gravito) recipe. Merge or PR. Provider access. NO new RUNTIME deps (`RealModelClient` uses built-in `fetch`, no SDK).

## Commits
- 8b8b60a Add model-layer substrate + LG-006 detector (surface + interpret) — 8 files, 968 insertions, 0 deletions; ALL additions, touched no existing file.
- fb5663b Thread LG-006 through scanner + CLI, add --offline model gate — additive edits to `scanner.ts`, `cli/run.ts`, `bin/launchgraph.ts` + one new test `tests/scan/scanModel.test.ts`.
- Frozen library source untouched; the only pre-existing files changed are `scanner.ts` + `cli/run.ts` + `bin/launchgraph.ts` (all behavior-additive). No fixtures added or modified.

## QA proof
- Suite:        `npm test` @ HEAD `fb5663b` → **213 passed, 0 failed, 0 skipped**, 24 files. New tests: lg006 9, model/client 6, model/inference 7, scan/scanModel 10 = **32 new**; the 181 prior tests unchanged. `tsc --noEmit` clean. `npm run eval` → **10/10 decisions correct, 0 blocker false-positives, exit 0**.
- Regression:   ALL protected/frozen files byte-identical — **`engine.ts` + `schema/index.ts` blob SHAs unchanged (proving emit-only)**; plus `PRODUCT_SCOPE`, specs, serialize, harness, collect, redact, detectorKit, lg001/002/003/004/008/010/014/015, registry, cli/args. No fixtures added or modified. Only existing files changed are `scanner.ts` + `cli/run.ts` + `bin/launchgraph.ts` (all behavior-additive; `run.ts` extracted a shared `emitReport` + added an async overload, the sync two-arg path unchanged). All 181 prior tests pass. Model contract re-derived on the fake client (incl. a throwaway probe running the model/inference/lg006 logic): uncited → no usable judgment; 0.99 → capped 0.9, never confirmed / never > 0.9; model-pass vs deterministic fail-fact → `contradictory`; surface with/without cancellation branch; no-webhook → `not_applicable`; no-judgment → `unknown` "model layer disabled"; fake fail → fail/inferred/blocker(from registry)/≤0.9/evidence; fake pass → pass; `validateReport` clean on an inferred report. Fixture decisions all **UNCHANGED vs P-004** (`broken-lg-004` now also carries an LG-006 `unknown` but stays `not_ready`; every other decision/exit unchanged); NO unqualified `ready`.
- Commit-1 iso: fresh worktree @ `8b8b60a`, `npm ci` → **203 passed, 23 files**; `tsc --noEmit` clean. `git diff a4ffbf5..8b8b60a` = 8 files, 968 insertions, **0 deletions** (purely additive — no existing file touched; scanner/run/bin/args byte-identical to base at Commit 1) → green in isolation.
- Safety grep:  **Scanner-safety grep — ZERO secret patterns** across all P-005 files, fixtures, and BOTH commit trees. SEC: `RealModelClient` imported ONLY by the bin; zero new runtime deps; built-in `fetch`; no `process.env` in model/detector code (only the bin's `readModelConfig`). SEC-5 wrapper unit-tested (an injection excerpt lands INSIDE the delimiters). No real network in any test. Main-session pre-push re-verification independently confirmed scanner-safety zero and pushed the fast-forward.
- UI smoke:     N/A (model-layer + CLI packet; behavior exercised via the model/inference/lg006/scanModel tests and the real bin).
- Acceptance tests: **AT-27 closed** — `--offline` / model-unconfigured → LG-006 `unknown`; two fixed-clock offline runs byte-identical; Layer-D findings identical online-vs-offline. (P-004 had left AT-27's "mark M-checks unknown" half OWED to the model-layer packet; it is now discharged.) AT-06 proven here via fake-model integration (fail/pass) rather than the eval fixture (deferred).

## Review
- Verdict: **PASS** — no must-fix-now.
- Codex second-eyes: not available (no CLI/plugin).
- Product Trajectory Check: the `ModelClient` / `InferenceRequest` / `InferenceResult` / `runInferenceContract` / `InferencePresentation` seam is **generic** — A-S2..A-S7 plug in by supplying a Layer-D surface fn + a presentation. `InferencePresentation.externalVerification` already threads through `makeFinding`, giving LG-012 (the last pending external check, A-S7) a clear path. Engine + schema untouched; registry stays the single source of check metadata; the ceiling watch-item is undisturbed.
- NOTABLE ADJUDICATIONS (all ten challenges cleared):
  1. **Safety boundary decisive** — the credential boundary is genuinely at the bin only.
  2. Excerpts-only + cite-or-discard confirmed — no raw repo text reaches the model.
  3. Cap 0.9 + `inferred`/`contradictory`; the <0.7-blocker downgrade is left to engine Rule 4 (not re-implemented in the model layer).
  4. **Engine + schema byte-identical (emit-only).**
  5. AT-27 honest.
  6. The offline-`unknown` finding classified `confirmed`/1.0 is schema-valid AND non-misleading — that qualifier is never rendered: `categorize()` buckets on severity+outcome+classification and an `unknown`-outcome finding matches no bucket, so it only increments `counts.unknowns`; the alternative `unverified` would be actively WRONG (implying external Phase-3). **PASS as-is.**
  7. The `isWebhookHandler` restatement — the two predicates (lg004 / lg006) are currently BYTE-IDENTICAL, so LG-004/LG-006 agree today; acceptable this slice, but **extract a shared locator before A-S2** (LG-005 will be a third consumer). See residue #2.
  8. SEC-5 mechanism built + unit-tested.
  9. Sync/offline byte-identity + async isolation confirmed (no existing exact-finding-set assertion needed updating).
  10. Scope / secrets / trajectory clean — the seam is generic; `InferencePresentation.externalVerification` gives LG-012 a clear path.

## Residue
- Deferred / follow-up packets:
  1. **LG-006 DETERMINISTIC-DISJUNCT FIDELITY GAP — TOP ITEM (surfaced prominently, not buried).** Per spec §3 LG-006 ("No handler branch … fails as a blocker") and §4.3 (model judgment may NOT substitute a required deterministic signal), a **FULLY MISSING cancellation branch is a DETERMINISTIC `confirmed` blocker `fail`** and should stand even under `--offline`. Currently that case reports `unknown` offline / routes through the model as `inferred` online — a **false-negative on the canonical LG-006 defect in the deterministic/CI (`--offline`) path**. The deterministic signal IS captured (`fact:lg006.no-cancellation-branch` + the contradiction guard), so this is a **FIDELITY gap, NOT a safety gap.** Fix: emit a deterministic `confirmed` blocker `fail` when NO cancellation branch exists at all; reserve `unknown`/`inferred` for the branch-present-but-downgrade-unconfirmed case. Sequence it with the `broken-lg-006`/AT-06 fixture (and ideally with follow-up #2, the shared locator) — recommend as an early next slice.
  2. **Extract the shared webhook-handler locator.** The locator is currently restated IDENTICALLY in `lg004.ts` and `lg006.ts`; extract it into a shared module (e.g. `src/scan/webhook.ts` or `detectorKit`) **BEFORE A-S2/LG-005** (a third consumer), with a guard test asserting the locators agree. Note also: LG-006 currently analyzes only the lexicographically-first webhook handler (`.find()`) vs LG-004's all-handlers (`.filter()`) — a slice-1 multi-handler limitation (deterministic; AT-23 holds).
  3. **Harden `buildUntrustedDataEnvelope`** against an excerpt reproducing the END delimiter token (strip/escape delimiter tokens in excerpts). Low consequence given the structural bounds (no tools, schema-only output, cite-or-discard, contradiction-wins, 0.9 cap); do it alongside the AT-21 hostile fixture.
  - Carried forward (still open, non-blocking): spec-header staleness ("not yet implemented"); `report.repo.commit` null; banner locator artifact on fixture scans; `eval` over the 10-of-18 fixtures (§9.3 recall/decision thresholds a program-level gate); LG-008 optional warning for a lone committed DB URL; LG-003 provider-agnostic webhook presence; LG-010 last-two-labels domain heuristic (no PSL — can only under-warn); LG-014 implicit/dashboard-only Sentry mechanisms.
- Known risks / carry-forward obligations still binding:
  - **externalVerification obligation** — **LG-012 remains the LAST pending external check** (comes in A-S7); the substrate already supports it via `InferencePresentation.externalVerification`.
  - **TEST-DATA POLICY** (<20 contiguous alphanumerics for every key-shaped fake, no Sentry-DSN shapes, every surface incl. receipts/memory; never allowlist a secret).
  - **CEILING WATCH-ITEM** (`hasAppSignal ⊇ scanner.supported`; add the AT-16 regression when `golden/` lands). Registry stays the single source of check metadata.
  - The model-layer substrate seam (`ModelClient`/`InferenceRequest`/`InferenceResult`/`runInferenceContract`/`InferencePresentation`) is the plug-in point for A-S2..A-S7.

## Open boundaries (awaiting explicit go)
- Push to the feature branch was executed under standing authorization AFTER qa green + reviewer PASS + a two-repo scanner-safety grep, landing as a normal fast-forward `a4ffbf5..fb5663b`.
- NO merge to any default branch and NO pull request — both await explicit approval.
- No deploys, no secrets touched/allowlisted, no provider access. **No real model API call occurred** — the model layer's runtime egress only activates when a real model is configured at the bin, which did not happen in this packet.
