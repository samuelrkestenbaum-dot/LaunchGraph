# Receipt — T-002: Pin the Build OS source; close the long-carried gaps

- **Date:** 2026-07-27
- **Branch:** `claude/sugarbee-project-handoff-tdfylf` (local tip `c7baff9`; remote ref at `8652bac`).
- **Branch base (merge-base):** **`8652bac`** — `git merge-base HEAD 8652bac` → `8652bac`, a **strict ancestor** of the tip. Fast-forward lineage, no fork. After the `origin/HEAD` repair below, `git merge-base HEAD origin/HEAD` also → **`8652bac`**.
- **Series:** `T-NNN` — tooling / framework-adoption packets in the product repo (established at T-001). `P-0NN` frozen; `A-S*` reserved for the detector fork.
- **Guide:** canonical ClaudeOrchestrator at `/home/user/ClaudeOrchestrator`, tip **`0a63f66`** — **read only. Never written.** Verified clean and still `0a63f66` at close, reflog untouched.

---

## Scope

**In:**
- Set `env.BUILD_OS_SOURCE = "/home/user/LaunchGraph"` in `.claude/settings.json`, freezing the self-updating SessionStart bootstrap (T-001's TOP residue item, mitigation (a)).
- Prove the freeze with a **non-vacuous three-arm control**, not an assertion.
- Close, or bring to a decision boundary, the gaps carried since A-S1b: **G1** (no default branch), **G2** (no second eyes), **G3** (self-updating hook unmitigated), **G4** (runtime configured-but-unproven), **G5** (the stale ClaudeOrchestrator branch).
- Local `git remote set-head` repair (a ref change, **not** in the commit diff).

**Out (explicit):**
- **All product code.** Zero movement under `src/`, `tests/`, `bin/`, `fixtures/`, `package.json`, `package-lock.json`, `vitest.config.ts`, `tsconfig.json`.
- **No detector work.** No LG-check added, changed, or removed. Detector coverage untouched at **11 of 15 implemented — QUALIFIED: 8 can move an offline decision; 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.**
- **No writes to ClaudeOrchestrator.** Including the P2 Codex finding against `project-bootstrap.sh` — recorded as owed upstream, deliberately not fixed here.
- **No fix to the two real product defects Codex found** (LG-005, LG-009). Findings on **closed** receipts are input to a **future** packet, never a reopening. Receipts are append-only.

---

## Commits

- **`c7baff9`** — *Pin the Build OS source to freeze the self-updating bootstrap (T-002)* — **2 files, +3 / −2.**
  - `.claude/settings.json` — one added key inside the existing `env` object:
    ```
       "env": {
    -    "BUILD_OS_SPECIALIST_HANDOFF": "1"
    +    "BUILD_OS_SPECIALIST_HANDOFF": "1",
    +    "BUILD_OS_SOURCE": "/home/user/LaunchGraph"
       },
    ```
  - `build-os/.install-manifest.json` — one line (`installed_at` / `source_sha` churn).
- **1 commit. ≤2 commits: held.**
- The `git remote set-head` repair is a **local ref change** and appears in no diff.

---

## QA proof — GREEN (DC-1..DC-7); DC-8 recorded as a finding

- `npm test` — **387 tests / 30 files**. `npm run typecheck` — **exit 0**. `npm run eval` — **11 fixtures / 11-of-11 decisions / 0 blocker false positives**. Identical at base and tip.
- **Commit-1 isolation:** detached worktree at `c7baff9` — **387 / 30 GREEN**. (Commit 1 is the only commit.)
- **Diff is exactly two files** — `.claude/settings.json`, `build-os/.install-manifest.json`. **Zero product movement.**
- `fixtures/` tree `a981446bac76039147d93efedd14a092c2aeadc1` — **FIFTH consecutive zero-fixture-movement packet** (A-S1c, A-S2, A-S3, T-001, T-002).
- **The pin survives `project-bootstrap.sh --force`** — both `env` keys intact, hooks intact — **including the harsher unpinned-`--force` variant**.
- **ClaudeOrchestrator clean, still `0a63f66`, reflog untouched.** The arm-(b) clone left no trace.
- **Safety grep clean.** The diff adds 3 content lines.
- **Assertion budget: 0 of 5.** No test file changed. Consistent with T-001: for a settings-pin packet, **empirical control arms were substituted for behavioural tests** — say that plainly; it is mechanism evidence, not suite evidence.

---

## THE CENTRAL TECHNICAL RESULT — the routed mechanism was wrong, and the hazard is self-triggering

The build-orchestrator routed a **frozen canonical clone** (`git clone` ClaudeOrchestrator, `checkout --detach 0a63f66`, point `BUILD_OS_SOURCE` there). **It does not close the hazard.**

Three-arm control. A canary line was appended to the MANAGED file `build-os/memory/tool_router.md`, and the bootstrap was invoked **exactly as the SessionStart hook invokes it**:

| arm | `BUILD_OS_SOURCE` | result |
|---|---|---|
| (a) unpinned | — | canary **REVERTED** (hazard fires) |
| (b) pinned to a frozen canonical clone | frozen clone of CO @ `0a63f66` | canary **REVERTED** — **does NOT close it** |
| (c) pinned to the project itself | `/home/user/LaunchGraph` | `nothing to install` — canary **SURVIVED** |

**Reproduced independently by BOTH qa and reviewer.**

**The reason is structural, not versional.** A frozen clone still satisfies `is_canonical_src()`, so `SRC != TARGET` and the full **stage → backup → promote** path still runs. **Freezing pins *which* bytes get written; it never pins *whether* writes happen.** Arm (c) is a **TRUE ZERO-WRITE no-op** — qa proved it by leaving a tampered `source_sha` of 40 zeros in place afterward; an overwrite would have restored it.

**THE REVIEWER'S CONTROL WAS STRONGER THAN THE BUILDER'S AND IT CHANGES THE HAZARD MODEL: the reviewer did NOT tamper the manifest.** The canary edit **alone** flips the file-hash half of the cache gate (`project-bootstrap.sh:196-215`). **Therefore THE HAZARD IS SELF-TRIGGERING — it needs no external ClaudeOrchestrator commit at all.** The clone-pin would have frozen only the `source_sha` half and left the **file-hash half fully live**.

---

## FOUR PREMISE CORRECTIONS — these overturn beliefs carried for up to six packets

### C1. "This repo has no default branch" (HANDOFF §10.1 / residue) **IS FALSE**, and was carried **SIX** times.
`git ls-remote --symref origin HEAD` → `ref: refs/heads/claude/launchgraph-product-scope-43pgdx`. The GitHub repo object confirms `default_branch: claude/launchgraph-product-scope-43pgdx` @ `a00d194`.
**What is actually true, and it is much narrower:** there was no branch *named* `main`/`master`, and `refs/remotes/origin/HEAD` was unset **in the local clone only**. `a00d194` is a **strict ancestor** of the tip.
**A PR was openable all along.**

### C2. ClaudeOrchestrator's remote has **SIX** branches, not two.
`claude/add-build-os` @ `0a63f66` (its default) · `claude/launchgraph-product-scope-43pgdx` @ `ef8a75c` (the G5 target) · `claude/orchestrator-tools-list-e0mdaz` @ `0a63f66` · `claude/project-handoff-merge-ramhds` @ `d3d8305` · `claude/repo-verification-cleanup-m9cfsm` @ `0a63f66` · `claude/song-lyrics-docx-ksh0qr` @ `33629b9` (unrelated content on the framework repo).
Also: **`claude/sugarbee-project-handoff-ic47uc @ 7ef50e8` on ClaudeOrchestrator, named in HANDOFF §5, is GONE** — that caveat is **stale**.
T-001's residue line "**two branches with disjoint histories**" is a **STALE LOCAL REF**: `origin/claude/add-build-os` reads `0f831fd` locally and `0a63f66` on the remote.

### C3. `~/build-os/.canonical-source` **IS NOT A VERSION PIN.**
`resolve_source()` order is `$BUILD_OS_SOURCE` → stamp → hard-coded paths → mirror. But `canonical_sha()` reads `git rev-parse HEAD` **LIVE** from the resolved path; `STAMPED_SHA` is used **only** as a fallback when the resolved source has no git history.
**The stamp pins the SOURCE PATH, never the SOURCE CONTENT.** Residue named the stamp as the mitigation. **A future session must not "apply the stamp" and believe G3 is closed.**

### C4. Second eyes were **NOT unavailable** — they were **mis-diagnosed for six packets**.
Every prior session checked `codex` on PATH, found nothing, and recorded "no second-eyes." **Codex is configured as a GitHub PR review bot on this repo** (`chatgpt-codex-connector[bot]`). It was reachable the whole time **via a PR** — which nobody opened, because of C1's false premise.
**§10.1 and §10.2 were the same defect.**

---

## Reviewer verdict — **fix-then-pass (recorded as fixed)**. Five items, all record-level; no code change.

### R1. **THE FAIL-OPEN — this is a residue TOP ITEM.**
The pin is an **ABSOLUTE PATH** and it **fails open in BOTH directions**, verified:
- **(a) Path gone.** If `/home/user/LaunchGraph` no longer exists, `is_canonical_src()` fails and `resolve_source()` (`project-bootstrap.sh:63-76`) falls through to `/home/user/ClaudeOrchestrator`, `$HOME/ClaudeOrchestrator`, **or `$(dirname TARGET)/ClaudeOrchestrator`**. The overwrite returns **silently, with the key still present and looking applied.**
- **(b) Re-checkout — WORSE THAN THE ORIGINAL HAZARD.** If the repo is checked out at a **new** path while the old one still exists, `SRC` = the **stale old checkout** and `TARGET` = the new path, so it **installs into the new checkout FROM the abandoned one**, reporting LaunchGraph's own HEAD as "canonical." **The abandoned checkout becomes a de-facto source repo.**
- **Include a one-line re-check** in any future session that moves or clones this repo.
- **It fails SAFE on a clean machine** (no ClaudeOrchestrator anywhere → the provisional `SRC` is the repo itself → no-op) and **fails OPEN on this one.**

### R2. **The upgrade path in `c7baff9`'s commit message is WRONG.**
The message says *"run the bootstrap explicitly from a ClaudeOrchestrator checkout with `--target`."* **That is false.** `resolve_source():65` consults `$BUILD_OS_SOURCE` **first, regardless of which copy of the script executes**, so that form still prints `nothing to install`.
**The only working form is:**
```
BUILD_OS_SOURCE=/home/user/ClaudeOrchestrator bash build-os/tools/project-bootstrap.sh --target /home/user/LaunchGraph --force
```
Corrected here because **commit messages are immutable history.**

### R3. **The `origin/HEAD` claim was misleading and has been FIXED.**
`git remote set-head -a` pointed `origin/HEAD` at the **repo default** `claude/launchgraph-product-scope-43pgdx`, so `git merge-base HEAD origin/HEAD` → **`a00d194`** — **three commits behind the real base and off this lineage**, handing the next session a **confidently wrong** answer where it previously got an honest failure.
**Repointed to `refs/remotes/origin/main`. `git merge-base HEAD origin/HEAD` now → `8652bac`, the actual packet base.**
**This is a LOCAL ref, and `git remote set-head -a` would revert it** until the repo default is actually changed (G1d).

### R4. **Authorization for the `main` push and PR #1 — ON RECORD, not a breach.**
The reviewer could not see the exchange and flagged a possible hard-gate breach. **The user was asked explicitly and answered: "Both: create main AND PR now."** Recorded here so this receipt is not later read as an unauthorized external mutation.

### R5. **Test-hygiene near-miss.**
During the packet an **uncommitted manifest with a zeroed `source_sha`** was briefly present (control residue). **A zeroed `source_sha` guarantees a permanent cache miss**, so committing it would have **pre-armed the exact overwrite the pin defends against.** Restored; tree clean at close.
Also recorded: an **unpinned `--force` run rewrote the manifest timestamp and had to be reverted** — precisely the **spurious-diff effect T-001 predicted**.

---

## GAP STATUS at close

- **G1a — CLOSED.** `origin/HEAD` is set (→ `origin/main`); `git merge-base` resolves **for the first time**.
- **G1b — CLOSED.** **PR #1 open** — head `claude/sugarbee-project-handoff-tdfylf`, base `claude/launchgraph-product-scope-43pgdx`, **20 commits**. **It is a REVIEW VEHICLE, NOT proposed for merge.**
- **G1c — CLOSED.** **`main` created at `8652bac`** — all closed, qa-green, reviewer-passed work. It is **a new name on existing history: every SHA in every receipt stays valid.**
- **G1d — OPEN. USER ACTION ONLY.** **No `mcp__github__*` tool can set a repository default branch** (`create_branch` / `create_repository` / `fork_repository` / `list_branches` exist; **no repo-settings mutation**). `gh` is **absent from PATH**. **User: GitHub → Settings → Branches → set `main`.** The token has admin.
- **G2 — CLOSED. SECOND EYES OBTAINED, FIRST TIME IN SIX PACKETS.** See the four Codex findings below.
- **G3 — CLOSED**, with R1's fail-open caveat.
- **G4 — ANSWERED: CONFIGURED-BUT-NOT-FIRING (proven).** This **replaces** T-001's "DURABLY CONFIGURED, unproven" wording.
  - The `hook-once` mechanism was validated to create `${TMPDIR:-/tmp}/build-os-hook-once` on a `session_id`-bearing payload, and there are **ZERO markers from real events**; `CLAUDE_PROJECT_DIR` unset; no hook banner in the session context; `/tmp` not cleared (it holds a Jul-23 file).
  - **Separately and independently: `settings.json` `env` is NOT delivered to processes on this surface.** qa found **no `BUILD_OS_*` in the parent session's `/proc/*/environ`** despite the key being committed since `8652bac`, and the session was launched with `--settings /root/.claude/launcher-settings.json` whose `env` is `None`.
  - **CONSEQUENCE — record it explicitly: T-001's `BUILD_OS_SPECIALIST_HANDOFF` gate rests on the SAME delivery mechanism and may ALSO be inert here.** Both are **dormant only because the hooks do not fire** — which is **NOT the same as being protected.**
- **G5 — OPEN. USER ACTION ONLY.** No branch-delete tool; no `gh`; proxy 403 on ref deletion. **It is THREE branches, not one** (see C2).
  - **CAUTION, RESTATED:** LaunchGraph's same-named branch `claude/launchgraph-product-scope-43pgdx` is **currently that repo's DEFAULT branch — deleting it would remove the default.** **Only ever delete the ClaudeOrchestrator one; verify the repo slug first.**
  - The nine-file byte-identity finding stands **on the PRIOR session's record** — it was **NOT re-verified here**, because that would require `git fetch` into canonical.

---

## THE FOUR CODEX FINDINGS — independent, non-Claude, on commit `8652bac`

**Binding rule: findings on closed receipts are input to a FUTURE packet, never a reopening. Receipts are append-only.**

### 1. P1 — `.claude/settings.json:4` — *"Add the source pin promised by this change."*
Codex reviewed **`8652bac`, which predates the pin**, and **independently concluded** the self-update hazard was fully active and that a project-self pin (or another fail-closed mechanism) was needed. **This is third-party corroboration that T-002's pin was necessary and correctly designed** — `c7baff9` is exactly this fix. **Not a new defect.**

### 2. P1 — `src/checks/lg005.ts:205` — *"Surface delegated side-effect implementations."* **A REAL PRODUCT DEFECT ON CLOSED WORK (A-S2).**
When a webhook **delegates** to a helper that is **idempotent by construction** (e.g. a plain state-reconciliation `UPDATE`), the `selects` predicate **excludes it**, because it carries none of the event-id / upsert / dedup markers. The model therefore **cannot see the code needed to distinguish that safe path from a helper that inserts, or sends mail, on every delivery** — **yet LG-005's verdict can still become a blocker.**
This is **defeater class 6** (a collection boundary that never read it) and a direct instance of the project's own governing rule being violated: **it is an ALLOWLIST OF POSITIVES, not a denylist of defeaters.** It also **sharpens** the standing residue item "`prefers` re-weights, it does not cure."

### 3. P1 — `src/checks/lg009.ts:424` — *"Treat every elided LG-009 candidate as incomplete."* **A REAL PRODUCT DEFECT ON CLOSED WORK (A-S3).**
When five preferred files fill the cap and an **additional table-name-only candidate holds the real isolation logic** (e.g. a relation predicate not spelling a recognized tenant column), `incompleteSurface` **ignores the omitted unmatched/trailing band**, so a model `fail` is accepted as an inferred blocker **although the exonerating query was deliberately withheld**.
**DC-11 must account for the trailing generic band, or prove those candidates cannot contain scoping.** This **extends A-S2's "incompleteness WITHIN a band" to the unbanded remainder**, and it is exactly the asymmetry DC-11 was built to guarantee.

### 4. P2 — `build-os/tools/project-bootstrap.sh:209` — *"Validate merged settings and CLAUDE.md before cache exit."* **An UPSTREAM ClaudeOrchestrator defect.**
When the canonical SHA and the manifest-listed file hashes match but the managed `CLAUDE.md` block **or** either `.claude/settings.json` hook entry has **drifted**, the **cache path exits before either merge runs**. **Those two managed outputs are absent from the manifest's `files` map**, so a bootstrap **reports the runtime up to date without repairing them.**
**DO NOT FIX HERE — never write to ClaudeOrchestrator.** Recorded as **owed upstream**.

---

## Router gap — recorded here because it cannot be recorded where it belongs

`build-os/memory/tool_router.md` still has **NO row for framework/tooling adoption**, and **it IS MANAGED**, so a local row added to it is reverted by the next bootstrap.
**NEW FINDING:** canonical's documented **3-tier fallback** (project `build-os/memory/tool_router.md` → **user-scope `~/build-os/memory/tool_router.md`** → embedded lanes) means a **user-scope router is an UNMANAGED, non-reverting home for a `T-` row**. **`~/build-os` does not exist on this host** (`/root/build-os`: no such file or directory).

---

## Residue / deferred

- **R1's fail-open** — new TOP residue item.
- **G1d** and **G5** — **USER ACTION ONLY**; neither is reachable from a session on this surface.
- **G4's delivery finding** — `settings.json` `env` is not delivered here, so **the `BUILD_OS_SPECIALIST_HANDOFF` gate may be inert**. Dormant ≠ protected.
- **Codex findings 2 and 3** — real product defects on **closed** A-S2 / A-S3 work. **Not reopened.** Added as **binding shaping inputs for A-S4.**
- **Codex finding 4** — owed **upstream** to ClaudeOrchestrator. Never write to canonical from here.
- **The C2 branch inventory** supersedes T-001's "two branches with disjoint histories" (stale local ref).
- **AT-05 and AT-09 remain owed**; AT-09 still has **no eval path to closure**. Untouched by T-002.

---

## Open boundaries (awaiting explicit go)

- **`main` push and PR #1 were EXPLICITLY AUTHORIZED** by the user ("Both: create main AND PR now"). **No merge was performed and none is proposed.** PR #1 is a **review vehicle only**.
- **NO merge** of PR #1, or of anything else, in either repo. **No deploys. No secrets touched or allowlisted. No provider access. No real model API call occurred in T-002.**
- **`c7baff9` is local; the remote branch ref is at `8652bac`.** This receipt and the memory updates are **NOT committed** — the user handles commit and push, per the standing "hold all `build-os/` commits until close" constraint.
- **Setting the GitHub repository default branch to `main` (G1d)** and **deleting the three stale ClaudeOrchestrator branches (G5)** are **user actions**, not session actions.

---
_Written by the archivist on close of **T-002** (2026-07-27). Receipts are append-only:
this file adds history and rewrites none. The four Codex findings and the four premise
corrections above **correct** earlier records; the earlier receipts are **not edited**._
