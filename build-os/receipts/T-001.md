# Receipt — T-001: Adopt canonical ClaudeOrchestrator Build OS tooling into the product repo

- **Date:** 2026-07-26
- **Branch:** `claude/sugarbee-project-handoff-tdfylf` (session-designated) — see **Branch base** below, which contains a **correction** to `current_state.md`.
- **Branch base (merge-base):** `f6a74c0` — `git merge-base HEAD f6a74c0` → `f6a74c0`, i.e. the base is a **strict ancestor** of the work tip. Fast-forward lineage, no fork.
- **Canonical source:** ClaudeOrchestrator tip **`0a63f66`** (P-023 project-agnostic bootstrap, P-024 measured activation boundary, P-025 zero-touch global bootstrap), installed via `build-os/tools/project-bootstrap.sh`.

---

## NAMESPACE — why `T-`, recorded deliberately

`T-` is a **NEW receipt series for tooling / framework-adoption packets**, and it is
deliberately distinct from the two series already in this directory:

- **`P-0NN`** — SugarBee's own product packets `P-001…P-005`. This id space
  **collides by filename** with canonical ClaudeOrchestrator's own development
  packets `P-001…P-025`. That collision is the exact failure the **2026-07-25
  migration killed** by moving SugarBee's Build OS memory into the product repo.
  Re-using `P-0NN` for a packet whose subject matter *is* canonical
  ClaudeOrchestrator would have re-created the collision inside the receipt it
  documents. Not doing that.
- **`A-S*`** — reserved for the **detector fork** (`A-S1b`, `A-S1c`, `A-S2`, `A-S3`,
  and the staged `A-S4`). A tooling packet is not a detector slice and must not
  consume a slice number, or the fork's numbering stops meaning what it says.

So: **`T-NNN` = tooling/runtime-adoption packets in the product repo.** T-001 is the
first. Receipts remain **append-only** — this file adds history; it rewrites none.

---

## Scope

**In:**
- Run canonical `build-os/tools/project-bootstrap.sh` from ClaudeOrchestrator tip
  `0a63f66` against this repo, adopting the MANAGED Build OS runtime.
- Adds: `build-os/tools/{project-bootstrap,capability-profile,supervise,specialist-handoff,skill-budget-audit}.sh`,
  `build-os/memory/skill_budget.md`, `build-os/global-claude-md.md`,
  `build-os/.install-manifest.json`.
- Modifies: `.claude/hooks/session-start-build-os.sh`, and **exactly one marker line**
  in `CLAUDE.md` (`install-project.sh` → `project-bootstrap.sh`).
- The reviewer-required activation gate (Commit 2).

**Out (explicit):**
- **All product code.** Zero movement under `src/`, `tests/`, `bin/`, `fixtures/`,
  `package.json`, `package-lock.json`, `vitest.config.ts`, `tsconfig.json` — proven by
  subtree hash below, not asserted.
- **No detector work.** No LG-check added, changed, or removed. Detector coverage is
  untouched at **11 of 15 implemented — QUALIFIED: 8 can move an offline decision;
  3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.**
- **No writes to ClaudeOrchestrator.** Never-write-to-canonical held; it is clean and
  still at `0a63f66`.
- **No preserved-state rewrite:** `current_state.md`, `residue.md`, `packets/**`,
  `receipts/**` were left byte-untouched by the bootstrap itself (the archivist's
  own updates are Commit 2).

---

## Commits

- **`4920653`** — *Adopt canonical Build OS tooling into the product repo (T-001)* —
  **10 files, +1290 / −2.**
  - `.claude/hooks/session-start-build-os.sh` (+29/−…), `CLAUDE.md` (1 line),
    `build-os/.install-manifest.json` (+49), `build-os/global-claude-md.md` (+56),
    `build-os/memory/skill_budget.md` (+88), `build-os/tools/capability-profile.sh` (+81),
    `build-os/tools/project-bootstrap.sh` (+410), `build-os/tools/skill-budget-audit.sh` (+184),
    `build-os/tools/specialist-handoff.sh` (+339), `build-os/tools/supervise.sh` (+54).
- **Commit 2 — PENDING AT WRITE TIME (the user commits it together with this receipt).**
  Contents: `.claude/settings.json` (the `env.BUILD_OS_SPECIALIST_HANDOFF = "1"` gate),
  `build-os/.install-manifest.json` (one-line `installed_at` churn:
  `2026-07-26T15:32:55+00:00` → `2026-07-26T15:41:46+00:00`), and this receipt plus the
  three memory files.

**≤2 commits: held.**

---

## QA proof — GREEN, 10 / 10 gates

Independently re-derived from a `git archive f6a74c0` export, not from the live tree.

- **Suite:** `npm test` → **387 passed / 30 files**. `npm run typecheck` → exit 0.
  `npm run eval` → **11 fixtures, 11/11 decisions correct, 0 blocker false positives.**
  **Identical at base and at tip** — the packet moves no number.
- **Commit-1 isolation:** green. `4920653` is a managed-paths-only change; the product
  build and suite are byte-identically unaffected (see subtree hashes), so Commit 1
  stands alone.
- **ZERO PRODUCT MOVEMENT — proven by subtree object hashes, identical at base and tip:**

  | path | tree/blob |
  |---|---|
  | `src` | `30f116ff` |
  | `tests` | `496e919b` |
  | `bin` | `49442941` |
  | `fixtures` | `a981446bac76039147d93efedd14a092c2aeadc1` |
  | `package.json` | `6cf6cc91` |
  | `package-lock.json` | `c9d039bf` |
  | `vitest.config.ts` | `7e7b1228` |
  | `tsconfig.json` | `014ddf5e` |

- **FOURTH CONSECUTIVE ZERO-FIXTURE-MOVEMENT PACKET** (`fixtures/` tree
  `a981446bac76039147d93efedd14a092c2aeadc1`, unchanged since A-S1c).
- **Preserved files:** empty diff on `current_state.md`, `residue.md`, `packets/**`,
  `receipts/**`.
- **Never-write-to-canonical:** ClaudeOrchestrator working tree clean, still `0a63f66`.
- **Fidelity:** all **20 managed files byte-identical to canonical — 20/20 `cmp`**.
- **Safety grep: clean.** No provider-key, Sentry-DSN, PEM, or token shapes. The only
  long-hex hits are (a) the manifest's own sha256 integrity hashes and (b) a pinned git
  commit in a serena dependency URL. `rm -rf` occurrences are scoped to `$STAGE`
  (mktemp, fail-closed guard) and `$BACKUP`.
- **UI smoke:** N/A — no UI surface in this repo.

### Assertion-change budget and the honest test story

- **Assertion budget: ZERO of 5 spent. No test file changed.**
- **NO TESTS WERE ADDED — and that is correct for a vendored tooling copy, but it must
  be said plainly rather than dressed up as a test-first pass.** The substituted proof
  is: **hash-equality against canonical (20/20 `cmp`)** plus **a preservation probe**
  (empty diff on the four PRESERVED paths). That is fidelity evidence, not behavioural
  evidence. The behavioural evidence for the adopted tooling lives in ClaudeOrchestrator,
  not here.

---

## Review — **fix-then-pass (recorded as fixed)**

### THE CENTRAL FINDING

**Adoption ACTIVATED a previously-dead prompt-classifying surface, and that classifier
is tuned to this project's own vocabulary.**

At base `f6a74c0`, `git ls-tree f6a74c0 build-os/` returns **only** `memory/`, `packets/`,
`receipts/` — **there is no `tools/`**. So `.claude/hooks/prompt-router.sh:16-22` resolved
`HANDOFF=""` and the entire specialist-handoff block was **dead code**. This packet makes
it resolve. `prompt-router.sh` is registered as **`UserPromptSubmit`**, and `claude` is on
PATH at `/opt/node22/bin/claude`.

**Measured classifier routes** (the reviewer ran these, they are not predictions):

| prompt | route |
|---|---|
| `"Shape A-S4 and pick the next detector"` | focused |
| `"keep going"` | focused |
| `"do an architecture review of the model layer"` | **ecc** |
| `"review the eval harness results"` | **ecc** |
| `"Next packet: LG-009 postgres RLS migration schema review"` | **ecc** |

`CAP_ECC` matches `postgres.*(schema|migration|index|query|performance)`,
`eval.*harness`, and `architect(ure)?.*review` — **SugarBee's core subject matter.**
The `ecc` / `zeroize` routes are exactly the two that:

1. **write `$HOME/.claude/settings.json` and `$HOME/.claude.json`** — *outside the product
   repo, machine-global* — via `capability-profile.sh:6-8,53-71`;
2. **register a third-party MCP fetched from GitHub**
   (`uvx --from git+https://github.com/oraios/serena@68884f11…`);
3. **spawn an autonomous `claude -p` child for up to 900s** via
   `specialist-handoff.sh:210-307`.

That is **external mutation** and an **unplanned agent launch without explicit go** —
against CLAUDE.md's hard gates on both counts.

### THE FIX APPLIED (Commit 2)

Set **`env.BUILD_OS_SPECIALIST_HANDOFF = "1"`** in `.claude/settings.json` — the recursion
guard read at `prompt-router.sh:16`.

**Why this location and not another:** `project-bootstrap.sh:322-350` loads the settings
dict, mutates **only** `data["hooks"]`, and writes the rest back — so **the `env` gate
survives every future canonical refresh.** Editing `prompt-router.sh` or deleting the
tools would **not** survive: both are managed and would be restored on the next bootstrap,
producing permanent **DRIFT churn**.

**Proven, not assumed:**
- With the gate set, a `UserPromptSubmit` payload classifying as `ecc` produced **only the
  routing reminder** and did **not** invoke `specialist-handoff.sh`.
- `~/.claude/settings.json` **was not created**.
- `~/.claude.json` **untouched** — it pre-dates the session (mtime 15:26:10) and its
  `mcpServers` is empty.
- The gate **survived `project-bootstrap.sh --force` intact.**
- Post-fix re-run: **387 / 30 green, typecheck clean, 11/11 eval, zero product movement.**

### Second eyes

**Not available — FIFTH CONSECUTIVE PACKET.** `codex` is absent from PATH. The reviewer
confirms this review was **single-model analysis plus empirical probes**. See Residue §4
for the new coupling: `mcp__github__request_copilot_review` *is* live on this surface and
would be a genuinely independent non-Claude reviewer, but it is PR-scoped and this repo
has **no default branch and nowhere to open a PR**.

### Product Trajectory Check

Neutral-to-positive on trajectory: the runtime is now durable and refreshable, and the
detector program is untouched. The packet **neither discharges nor worsens** the
second-eyes risk — that is A-S4's problem, and the count does not reset.

---

## Residue

Seven standing items, mirrored into `build-os/memory/residue.md`.

1. **THE SELF-UPDATING HOOK — new standing constraint.**
   `.claude/hooks/session-start-build-os.sh:18-31` now runs
   `project-bootstrap.sh --target "$ROOT"` on **every SessionStart**, with **no
   `--dry-run` and no `--verify`** — a live write. `resolve_source()` hard-codes
   `/home/user/ClaudeOrchestrator` first, so the vendored copy **does not shield the
   repo**. Steady state is quiet (the cache gate requires a canonical SHA match **and** a
   sha256 match on all 20 managed files). **On a cache miss it overwrites all 20 managed
   files plus `.claude/settings.json` and the `CLAUDE.md` managed block.** qa
   **demonstrated** this in a throwaway repo: a simulated drift **silently reverted a local
   edit to `build-os/memory/tool_router.md` to canonical content.** The trigger is
   **entirely outside this repo** — anyone committing to, or even `git checkout`-ing in,
   `/home/user/ClaudeOrchestrator`, which currently holds two branches with **disjoint
   histories** (`claude/sugarbee-project-handoff-tdfylf` `0a63f66` and
   `claude/add-build-os`, empty merge-base). It does not commit, so it does not literally
   breach "hold all `build-os/` commits until the packet closes" — **but a builder running
   `git add build-os` or `git commit -a` mid-packet sweeps it in**, the same collision class
   that previously required a reflog repair. **Mitigations, NONE APPLIED:** pin
   `BUILD_OS_SOURCE`, or run `git status` for managed-path churn before any build-os commit.

2. **`tool_router.md` AND `skill_budget.md` ARE NOW MANAGED — new and load-bearing.**
   The manifest lists **20 managed files**; **PRESERVED is only** `current_state.md`,
   `residue.md`, `packets/active_packet.md`, `receipts/README.md`. **Any SugarBee-specific
   routing knowledge written into `tool_router.md` will be silently reverted by the next
   bootstrap.** Project-specific routing facts must live in `current_state.md` /
   `residue.md` instead. Also recorded: **the router has NO row for framework/tooling
   adoption** — this packet routed from the Build row with reviewer dropped. **The gap is
   real.**

3. **SURFACE-VERIFIED TOOL INVENTORY (2026-07-26, Claude Code on the web / remote
   container).** The router's capability tables are **HOST-specific (P-012, a Mac) and are
   FALSE on this surface.** Live evidence gathered this session:
   - **Contradicted — router UNDERSTATES:** `tool_router.md` says "Not live (out of scope
     until enabled): Stripe & Cloudflare Developer Platform (installed, need auth)."
     `ListConnectors` here reports **both `connected: true, enabledInChat: true`**.
   - **Contradicted — router OVERSTATES:** the "Build accelerators (verified 2026-07 —
     P-012)" table marks Serena / Repomix / ccusage / Trail of Bits / Context Mode
     **ACTIVE**. On this surface `serena`, `repomix`, `ccusage` and `codex` are **all absent
     from PATH**; `claude plugin list` returns "No plugins installed"; there is no
     `~/.claude/plugins` and no `~/.claude/settings.json`. **Every ACTIVE claim in that
     table is false here.**
   - **`skill_budget.md` is INAPPLICABLE here.** Its premise (685 skills / 171,824 chars,
     5.7× over the 30,000-char budget; disable `ecc@ecc`) is a Mac-host condition.
     `build-os/tools/skill-budget-audit.sh --claude-dir ~/.claude 30000` on this surface
     reports **0 installed / 0 enabled plugins**. **Both the problem and its remediation
     are absent.**
   - **Confirmed accurate:** Google Calendar / Google Drive / Microsoft 365 installed but
     `enabledInChat: false`.
   - **Actually live here:** 16 claude.ai connectors (21st, Apollo.io, Clay, Cloudflare,
     Docusign, Gmail, Higgsfield, HubSpot, Hugging Face, Netlify, Notion, Otter.ai, Slack,
     Stripe, Supabase, Zapier); 10 enabled plugins (design, data, productivity,
     brand-voice, marketing, sales, small-business, legal, cowork-plugin-management,
     ui-ux-pro-max); the `github` and Claude Code Remote session MCPs; Build OS agents
     **5/5 discoverable**.
   - **This is the router's own "no-route-to-unverified" rule catching the router.**
     **Re-verify per surface; do not inherit P-012's host verdicts.**

4. **§10 COUPLING — NEW, and it links the two decisions carried unresolved.**
   Second-eyes review is **still unavailable** (`codex` absent; fifth consecutive packet).
   **But `mcp__github__request_copilot_review` IS live on this surface** — a genuinely
   independent, non-Claude reviewer. It is **PR-scoped**, and this repo has **no default
   branch and nowhere to open a PR** (§10.1, now on its **sixth carry-forward**). **So
   §10.1 is actively BLOCKING the only available mitigation for §10.2.** The file-copy
   packet **neither discharges nor worsens** the second-eyes risk — **A-S4 is where it must
   be answered** — so the count is **not silently reset**.

5. **Manifest timestamp — two readings, both recorded.** `build-os/.install-manifest.json`
   embeds `installed_at`. **Reviewer's read:** the cache gate means it is only rewritten on
   a real install, so a new timestamp is **accurate rather than noise**. **qa's read:**
   expected one-line churn. **A future session must not read this timestamp as evidence of
   anything except the time of the last write.**

6. **Inert but armed.** `session-start-build-os.sh:48-51` background-launches
   `$ROOT/install-accelerators.sh` **if that file ever appears at the repo root.** It does
   not exist in LaunchGraph today.

7. **STATUS LABEL — the runtime is DURABLY CONFIGURED, not ACTIVE.** Installed ≠ activated.
   Whether Claude Code fires these hooks on this surface is **unproven**. The reviewer's
   note, recorded because it is uncomfortable and true: **the packet cannot claim
   self-updating adoption while simultaneously relying on the hook NOT firing for safety.
   Both halves rest on the same unproven premise.**

---

## Branch base — RECORDED AS A CORRECTION

`current_state.md:9` named `claude/sugarbee-project-handoff-ic47uc` as the primary branch.
**HEAD is now on `claude/sugarbee-project-handoff-tdfylf`,** the session-designated branch.
The facts, so a future session does not mistake this for a fork:

- Local `tdfylf` was at **`a00d194`**, a **strict ancestor** of the work tip — **0 ahead /
  18 behind**. It was **fast-forwarded to `f6a74c0`**.
- **`f6a74c0` is ALSO published as `origin/claude/sugarbee-project-handoff-ic47uc`.**
  So `tdfylf` is a **second ref on the SAME lineage, not a content fork.**
- `origin/claude/sugarbee-project-handoff-tdfylf` (still showing `a00d194`) is a **stale
  local tracking ref for a branch that never existed remotely.**
- **Both branch names are recorded, and every SHA recorded in prior receipts remains
  valid.** Nothing in `P-001…P-005`, `A-S1b`, `A-S1c`, `A-S2` or `A-S3` needs restating.

---

## Open boundaries (awaiting explicit go)

- **Commit 2 is NOT committed and this receipt is NOT committed** — the user commits both
  together. Standing constraint honoured: all `build-os/` commits are held until close.
- **No push.** No merge. No PR. No deploy. No secrets touched or allowlisted.
- **NO DEFAULT BRANCH — SIXTH PACKET FLAGGING IT.** There is still no `main`/`master`
  locally or remote. Merge has no target and a PR has nowhere to land. As of this packet it
  additionally **blocks the only available second-eyes mitigation** (Residue §4). **This
  needs a user decision, not a seventh carry-forward.**
- **No real model API call occurred in this packet.** No detector ran outside the offline
  eval harness.

---

_Written by the archivist on close of **T-001** (2026-07-26). Receipts are append-only;
no prior receipt was edited._
