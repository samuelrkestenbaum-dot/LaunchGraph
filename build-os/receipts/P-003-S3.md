# Receipt — P-003-S3: Phase 1 Repository Auditor — Slice 3: deterministic detectors LG-003, LG-008, LG-015 (externalVerification obligation first binds)

- **Date:** 2026-07-23
- **Branch base (merge-base):** LaunchGraph repo, branch `claude/launchgraph-product-scope-43pgdx`, based at `ea9d250` (P-003-S2 tip) — verified via `git merge-base` at go (exact match, tree clean). Both packet commits sit directly on that base and were pushed to the feature branch as a normal fast-forward (`ea9d250..47fbb8d` on origin).

## Scope
- **In:** Three deterministic detectors drawing id/title/severity from the S1 check registry via `detectorKit` (registry remains the single source of check metadata):
  - **LG-003** (missing production webhook) — fail/confirmed/blocker + documentary `externalVerification` (provider `stripe`, phase-3) when a subscription usage signal is present but no webhook route exists; present webhook route → pass/unverified + the same `externalVerification`; no subscription usage → `not_applicable`.
  - **LG-008** (preview using production database) — repo-provable mixing (an identical concrete remote DB URL committed under BOTH a production and a non-production scope) → fail/confirmed/blocker with **NO** `externalVerification`; partial / dashboard-only signal → unknown/unverified + `externalVerification` (provider `supabase`, phase-3). Deterministic-only: a merely-suggestive signal reaches `unknown`, never an inferred low-confidence blocker.
  - **LG-015** (missing/unverified production domain) — no canonical production URL configured anywhere, or configured values disagree → fail/confirmed/blocker + `externalVerification` (provider `dns`, phase-3); present & consistent → pass/unverified + the same `externalVerification`; gated on an app signal (`hasAppSignal`, disclosed in docstring) → `not_applicable` on non-app repos.
  - Additive `detectorKit` `externalVerification` passthrough plus a `makeExternalVerification` helper centralizing the single phase-3 literal.
  - Three inert broken fixtures (`fixtures/broken-lg-00{3,8}/`, `fixtures/broken-lg-015/`) and scanner registration of the three detectors.
  - `fixtures/clean-min/expected.json` flipped `ready` → `ready_with_warnings` — the pre-documented Phase-1 ceiling transition, demonstrated in code for the first time.
  - A disclosed one-line hygiene fix shortening a pre-existing over-length fake webhook-secret literal in `tests/scan/redact.test.ts` to satisfy the standing TEST-DATA POLICY (<20 contiguous alphanumerics).
- **Out (explicit):** The remaining detectors — LG-005/006/007/009/010/011/012/013/014; the `scan`/`eval` CLI; the model layer; remediation packages (no machinery exists — the go-instructions' remediation clause was conditional and did not apply); `hostile/` and real `golden/` fixtures (AT-16 still deferred). Everything in spec §13. Merge or PR. Provider access of any kind — no provider SDKs, no credential intake, no runtime network, no execution of fixture code.

## Commits
- e743cd6 Add LG-003/008/015 pure detectors + detectorKit externalVerification passthrough
- 47fbb8d Register LG-003/008/015, add three broken fixtures, flip clean-min ceiling
- P-001/P-002 docs and all P-003-S1/S2 source untouched. Only 5 pre-existing files additively changed: `detectorKit.ts`, `scanner.ts` (+3 registrations only), `tests/scan/scanner.test.ts`, `tests/scan/redact.test.ts` (hygiene), `fixtures/clean-min/expected.json` (ceiling flip). Both commits author+committer `noreply@anthropic.com`.

## QA proof
- Suite:        `npm test` @ HEAD `47fbb8d` → **131 passed, 0 failed, 0 skipped**, 15 files (lg001 6, lg002 4, lg003 4, lg004 4, lg008 4, lg015 6, engine 19, harness 13, registry 7, collect 7, redact 10, scanner 8, schema 25, sec1 5, serialize 9); `tsc --noEmit` clean.
- Regression:   Docs + all S1/S2 source untouched; only the 5 pre-existing files above additively changed. LG-001/002/004 unaffected.
- Commit-1 iso: fresh worktree @ `e743cd6`, `npm ci` → **129 passed**, typecheck clean, `scanner.ts` still S2 (only lg001/002/004 registered), Commit-2 fixtures absent, `clean-min` still `ready` → green in isolation.
- externalVerification / ceiling table (qa re-derived by running the composed scanner over every fixture): `broken-lg-003` → not_ready + LG-003 fail carrying `stripe` externalVerification; `broken-lg-008` → not_ready + LG-008 fail with NO externalVerification; `broken-lg-015` → not_ready + LG-015 fail carrying `dns` externalVerification; `clean-min` → **ready_with_warnings** + LG-015 pass/unverified. Each broken fixture seeds exactly one blocker (broken-lg-003 also emits LG-004 not_applicable; broken-lg-015 also emits LG-001 unknown). All severities valid enums; every `externalVerification.phase === phase-3`; `validateReport` clean on all fixtures. Ceiling held: no fixture yields unqualified `ready`.
- Safety grep:  Scanner-safety grep — the Stripe live/test key pattern, the webhook-secret prefix pattern, and the Resend prefix pattern all **ZERO** across the worktree AND both commit trees; the old over-length fake literals absent; the hygiene fix present (short suffix). Re-verified independently by the main session pre-push. SEC-1: all 8 fixtures inert (no scripts/workspaces, `private: true`, excluded by vitest/tsconfig). SEC-2: no runtime network. SEC-3: no provider SDK deps, no provider client instantiation, no `process.env` credential reads in `src/`. SEC-4: all new evidence via `buildEvidence`/`buildAbsenceEvidence`. SEC-6: `collect.ts` untouched. AT-23 determinism: byte-identical across two fixed-clock runs; suite passes twice identically. AT-24 fixture-path validity: every `expected.json` evidence-path substring resolves to a real file.
- UI smoke:     N/A
- Acceptance tests: AT-03/AT-08/AT-15 exercised end-to-end through the real §9 harness; AT-23/AT-25/AT-26 reinforced. AT-16 (real `golden/`) still deferred — `clean-min` remains the subset-scoped ceiling demonstration.

## Review
- Verdict: **pass** — no must-fix-now items. All ten mandated challenges cleared.
- Codex second-eyes: not available (no CLI/plugin).
- Product Trajectory Check: seams remain clean for further detector slices; the registry stays the single source of check metadata and the externalVerification passthrough is now proven for two external checks.
- NOTABLE FINDING (spec discrepancy in the go-instructions, recorded for durability): the go-instructions' challenge-3 premise mis-stated the spec. It asserted spec §3 LG-015 lists "still using localhost" and "still using a preview hostname" as conclusions LG-015 may reach — but that "localhost / preview host" language belongs to **LG-011** (§3), not LG-015. §3 LG-015's only fail conditions are "no canonical production URL configured anywhere, or configured values disagree." The builder's presence-not-correctness handling is therefore exactly spec-faithful: a localhost production URL counts as PRESENT → LG-015 pass/unverified, while LG-001 owns the dev-origin failure as a confirmed blocker, so a localhost-in-prod repo is still not_ready via LG-001. The authoritative spec is unchanged; no action — noted so future slices do not propagate the confusion.
- Challenge 5 (ceiling hole) proven impossible: LG-015's app-signal gate (`hasAppSignal`) is a strict SUPERSET of the scanner's `supported` predicate, so every supported repo always emits an LG-015 finding carrying `externalVerification` → decision Rule 7 (`ready`) is unreachable; unsupported repos hit Rule 1 (`not_evaluated`).
- Challenge 2 (LG-008 narrowing) upheld as spec-faithful and safe: worst case is external-`unknown` vs a warning framing — never missed-to-green; the primary defect is a confirmed blocker and the distractor guards the false positive.

## Residue
- Deferred / follow-up packets (all reviewer-sourced, non-blocking, NONE must-fix-now):
  1. **LG-008 warning-outcome enhancement** — for the narrow sub-case of a lone CONCRETE remote DB URL committed in an *unscoped* carrier (plain `.env` / a single `vercel.json` entry) with no per-environment separation, consider emitting a warning-outcome repo-side finding rather than folding it into the external `unknown`, so spec §3 LG-008 condition (b) reads as a repo-side smell.
  2. **LG-003 provider-agnostic webhook presence** — `isWebhookRoute` treats any *webhook* route as present (a non-Stripe webhook route reads as present). Fine for this deterministic slice; refine later without treading on LG-004.
  3. **CEILING WATCH-ITEM (standing invariant, carry forward):** the Phase-1 ceiling depends on `hasAppSignal ⊇ scanner.supported`. Any future slice that broadens the scanner's `supported` predicate OR narrows LG-015's applicability gate without preserving this superset relationship could open an unqualified-`ready` hole. Add a regression assertion when the `golden/`/AT-16 fixture lands.
  4. **SPEC-DISCREPANCY NOTE** — the go-instructions mis-attributed LG-011 "localhost / preview host" language to LG-015 (see Review). Spec is authoritative and unchanged; no action, but noted so the confusion is not propagated.
- Carry-forward obligations still binding: the externalVerification obligation for LG-003/010/012/014/015 is now proven in code for LG-003 and LG-015; still pending for LG-010/012/014. TEST-DATA POLICY (<20 contiguous alphanumerics for every key-shaped fake, every surface incl. receipts/memory; never allowlist a secret). Registry stays single source of check metadata.
- Separate user-flagged governance visibility item (non-blocking, outside Build OS scope, carried forward): the live substrate-readiness check still reports Claude, Manus, and surplus_recovery as missing surfaces — a distinct Gravito governance gap.

## Open boundaries (awaiting explicit go)
- Push to the designated feature branch was executed under standing authorization AFTER qa green + reviewer pass + a two-repo scanner-safety grep, landing as a fast-forward.
- NO merge to any default branch and NO pull request — both await explicit approval.
- No deploys, no secrets touched/allowlisted, no provider access.
