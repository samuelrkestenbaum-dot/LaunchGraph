# Receipt — P-003-S2: Phase 1 Repository Auditor — Slice 2: first deterministic detector slice (LG-001, LG-002, LG-004) wired through the §9 ScannerFn seam

- **Date:** 2026-07-23
- **Branch base (merge-base):** LaunchGraph repo, branch `claude/launchgraph-product-scope-43pgdx`, based at `263ac7c` (P-003-S1 tip) — verified via `git merge-base` by the orchestrator at go. Both packet commits sit directly on that base and were pushed to the feature branch as a normal fast-forward (`263ac7c..ea9d250` on origin).

## Scope
- **In:** SEC-6 traversal (`src/scan/collect.ts` — symlink-escape rejection, binary skip, 2MB per-file / 50k file caps, SEC-7 degradation list). SEC-4 redaction + sole (redacting) evidence builder (`src/scan/redact.ts`). Deterministic detectors LG-001 (production callbacks using localhost), LG-002 (Stripe test/live key mixing), LG-004 (missing webhook signature verification) — `src/checks/lg00{1,2,4}.ts` + `detectorKit.ts`, each drawing id/title/severity from the S1 check registry (single source of check metadata). Composed library-level `ScannerFn` (`src/scan/scanner.ts`): collect → detectors → `decide()` → `Report`, with an injectable clock, wired through the existing S1 §9 harness seam. Fixtures `broken-lg-00{1,2,4}/` + `clean-min/` (all inert: `private: true`, no scripts, excluded by vitest/tsconfig). Generalized `tests/sec1.test.ts` to all `fixtures/*`; minimally scoped one `tests/harness.test.ts` assertion (`allDecisionsCorrect` → unsupported-only) because the new supported fixtures legitimately changed the eval set.
- **Out (explicit):** The other 12 detectors; the `scan`/`eval` CLI; the model layer; remediation packages; `hostile/` and real `golden/` fixtures (AT-16 deferred); remaining fixtures. Everything in spec §13. Merge or PR. Provider access of any kind. No credential intake; no runtime network; no execution of fixture code.

## Commits
- 4c4a8ae Add scan substrate (SEC-6/SEC-4) and LG-001/002/004 detectors
- ea9d250 Add composed ScannerFn, LG-001/002/004 fixtures, and §9 integration proof
- Totals: 39 files, +1740 / -8. P-001/P-002 docs and all five P-003-S1 source files untouched. Both commits author+committer `noreply@anthropic.com` (unsigned — environmental, no signing key).

### Push-protection incident + resolution (history note)
- The packet was FIRST built as commits `13d2d34` + `ca74666` and passed qa (GREEN) and reviewer (pass) at those hashes.
- The push was then REJECTED by GitHub push protection: the LG-002 fixture/test fake Stripe key (prefix `sk_live_` followed by a 33-character alphanumeric fake suffix) matched GitHub's Stripe live-key pattern. The literal is not reproduced here — writing it into this receipt would re-trip the same scanner.
- Resolved **WITHOUT allowlisting the secret**: a focused fix-then-pass shortened the fake-key suffix to `EXAMPLEnotreal` (14 chars) — it still matches the LG-002 detector's `[A-Za-z0-9]{4,}` and still redacts to `sk_live_***REDACTED***`, but sits under GitHub's 24-char threshold. Applied via history rewrite across BOTH commits; since origin never had `13d2d34`/`ca74666`, the push landed as a normal fast-forward (NO force-push).
- `src/` is byte-identical between pre-fix and post-fix commits (`git diff 13d2d34 4c4a8ae -- src/` and `git diff ca74666 ea9d250 -- src/` both empty) — only 6 files' literal string values changed. The reviewer's logic approval therefore carries over unchanged.

## QA proof
- Suite:        `npm test` @ HEAD `ea9d250` → 115 passed, 0 failed, 0 skipped (12 files); `npm run typecheck` (`tsc --noEmit`) exit 0. (qa agent reported GREEN on the pre-fix commits; the main session independently re-verified on the pushed post-fix commits.)
- Regression:   `PRODUCT_SCOPE.md` / `README.md` / `specs/` and all five S1 source files untouched. Only two pre-existing test files modified: `tests/harness.test.ts` (scoping verified NOT over-weakened — decision-correctness still tested by the perfect/degraded scanner tests) and `tests/sec1.test.ts` (change is a strengthening — generalized to all fixtures).
- Commit-1 iso: fresh worktree @ `4c4a8ae`, `npm ci` → 109 passed / 11 files, typecheck clean, no Commit-2 file present → green.
- Safety grep:  Scanner-safety grep `(sk|pk|rk)_(live|test)_[A-Za-z0-9]{20,}` → ZERO hits on worktree and BOTH commit trees; bare `FAKE51` substring → ZERO. SEC-1: all 5 fixtures inert (no scripts, no workspaces, `private: true`; excluded by vitest/tsconfig). SEC-2: no runtime network. SEC-3: no provider SDK deps (deps absent; devDeps only typescript/vitest/@types/node), no provider client instantiation, no `process.env` credential reads in `src/`. SEC-4 REDACTION PROOF: fake key masked to `sk_live_***REDACTED***`; raw key absent from serialized scanner output. SEC-6 containment tests genuinely exercise symlink-escape / binary-skip / size+count caps. AT-23 determinism: byte-identical serialized output across two fixed-`scannedAt` runs.
- UI smoke:     N/A
- Acceptance tests: AT-01/AT-02/AT-04 exercised for all three checks (seeded finding, correct id/severity/evidence, no other blocker, through the real §9 harness). AT-20 (SEC-4 redaction) and AT-22 (SEC-6 containment) MECHANISMS implemented + unit-proven and proven at the report seam; their hostile-fixture instantiations are DEFERRED (`hostile/` out of slice). AT-23/AT-25/AT-26 reinforced. AT-16 (golden) DEFERRED — a 3-detector scanner would honestly emit unqualified `ready`, which cannot satisfy AT-16's `ready_with_warnings` ceiling; a subset-scoped `clean-min/` negative fixture (documented in `expected.json` `_comment`, `scanner.ts` docstring, and the test name) proves blocker-FP=0 without overclaiming.

## Review
- Verdict: pass (on the pre-fix commits; logic unchanged by the fix — `src/` byte-identical — so the verdict stands for `4c4a8ae`/`ea9d250`).
- Codex second-eyes: not available (no CLI/plugin).
- Detail: all six builder judgment calls adjudicated spec-faithful — (1) LG-001 narrowed to config carriers (`.env.production*`, `vercel.json`); source-path scanning deliberately omitted to avoid the `process.env.X || 'http://localhost'` dev-fallback false-positive idiom — DISCLOSED in the `lg001.ts` docstring and the unknown-outcome absence note, NOT silently dropped; (2) LG-001 unknown-vs-fail matches §3; (3) categorical env redaction masks LG-001's own localhost value with the conclusion carried in the finding note — faithful to SEC-4/§5; (4) LG-004 no-handler → `not_applicable` respects the LG-003/LG-004 division and recognizes both `constructEvent` and manual HMAC verification; (5) `harness.test.ts` scoping minimal and not a weakening; (6) `clean-min` `ready` durably documented as subset-scoped.
- Product Trajectory Check: clean seams for the remaining detector slices — the registry is the single source of check metadata and `ScannerFn` injection is wired, so further detectors plug into the §9 gate without rework.
- Reviewer recommendation recorded here (per §13 ethos, mirrored into memory): the LG-001 source-path intra-check deferral must be revisited when the model layer / report surface lands.

## Residue
- Deferred / follow-up packets:
  1. **LG-001 SOURCE-PATH SCANNING deferred** — §3 lists "hardcoded in source on production paths" as an LG-001 signal; current impl covers config carriers only. Revisit when the model layer / report surface lands (needs care to avoid the dev-fallback false-positive idiom).
  2. AT-16 real `golden/` + AT-20/AT-22 hostile-fixture closure deferred to later slices.
- Known risks / non-blocking hardening (from reviewer):
  - LG-001 `URL_KEY_RE` substrings (`base`/`site`/`origin`) can over-match benign env keys — but can only shift pass↔unknown, never manufacture a false blocker.
  - `vercel.json` evaluation assumes single-line key/value.
- **TEST-DATA POLICY (new standing constraint, from the push-protection incident):** fake provider-key literals in fixtures/tests must keep short suffixes (<20 contiguous alphanumerics) so GitHub secret scanning does not block pushes. Carry into every future detector/fixture slice — especially LG-003 (`whsec_`) and any Supabase/Resend key fixtures.
- **Carry-forward obligation (unchanged, still binding on future slices):** every finding on LG-003/010/012/014/015 must carry `externalVerification` (or unverified) — enforces the §7 Phase-1 ceiling; AT-16 is the backstop. None of LG-001/002/004 are external, so it did NOT bind this slice.
- Queued next slices: remaining deterministic detectors (LG-003 needs the `externalVerification` obligation; LG-008/LG-015), then the D+M/model-layer detectors, the scan/eval CLI (closes AT-24 + exit codes), remaining fixtures incl. `hostile/` and `golden/`.
- Separate user-flagged governance visibility item (non-blocking, outside Build OS scope, carried from P-003-S1): the live substrate-readiness check still reports Claude, Manus, and surplus_recovery as missing surfaces — a distinct Gravito governance gap.

## Open boundaries (awaiting explicit go)
- Push to the designated feature branch was executed under standing authorization AFTER qa green + reviewer pass (and after resolving GitHub push protection properly, without allowlisting a secret).
- NO merge to any default branch and NO pull request — both await explicit approval.
- No deploys, no secrets allowlisted, no provider access.
