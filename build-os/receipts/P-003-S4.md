# Receipt — P-003-S4: Phase 1 Repository Auditor — Slice 4: warning-ceiling external detectors LG-010, LG-014

- **Date:** 2026-07-23
- **Branch base (merge-base):** LaunchGraph repo, branch `claude/launchgraph-product-scope-43pgdx`, based at `47fbb8d` (P-003-S3 tip) — verified via `git merge-base` at go (exact match, tree clean). Both packet commits sit directly on that base and were pushed to the feature branch as a normal fast-forward (`47fbb8d..006cadd` on origin).

## Scope
- **In:** Two deterministic, externally-bound, **WARNING-severity** detectors drawing id/title/severity from the S1 check registry via `makeFinding`/`detectorKit` (registry remains the single source of check metadata):
  - **LG-010** (unauthenticated email domain) — a provider-default sender (e.g. an `onboarding@resend.dev`-style provider address) or a from-domain mismatched with the derived production domain → fail / **warning** / confirmed + `externalVerification` (provider `resend`, claim "the sending domain is authenticated (SPF/DKIM/DMARC) at the email provider", phase-3); a present/plausible custom sending domain → pass / **warning** / unverified + the same `externalVerification`; no email-send usage → `not_applicable`.
  - **LG-014** (Sentry installed but unverified in production) — SDK present but production DSN absent, or no environment/release tagging → fail / **warning** / confirmed + `externalVerification` (provider `sentry`, claim "errors actually arrive in the production Sentry project with correct environment/release tagging", phase-3); SDK + DSN + tagging wired → pass / **warning** / unverified + the same `externalVerification`; no Sentry usage → `not_applicable`. A `process.env` DSN reference counts as production-wired.
  - Two inert broken fixtures (`fixtures/broken-lg-010/`, `fixtures/broken-lg-014/`) and scanner registration of the two detectors.
  - Additive `tests/scan/scanner.test.ts` only — a separate `WARNING_CEILING` recall list plus two `ready_with_warnings` ceiling tests plus an extended AT-23 determinism loop; the existing BROKEN blocker array untouched.
- **Out (explicit):** The remaining detectors — LG-005/006/007/009/011/012/013 (D+M / M — all need the model layer, a separate decision) and LG-012 (D+M, external, also needs the model layer); the `scan`/`eval` CLI; remediation packages (no machinery exists — not applicable); `hostile/` and real `golden/` fixtures (AT-16 still deferred). No `detectorKit` edit this slice (the `externalVerification` helpers pre-existed from S3), no registry edit, no `clean-min` change (both checks → `not_applicable` there). Merge or PR. Provider access of any kind — no provider SDKs, no credential intake, no runtime network, no execution of fixture code.

## Commits
- 3c83f41 Add LG-010/LG-014 warning-ceiling external detectors + unit tests
- 006cadd Register LG-010/LG-014 in scanner, add two warning-ceiling fixtures + integration
- P-001/P-002 docs and all P-003-S1/S2/S3 source untouched. Only two pre-existing files additively changed: `scanner.ts` (+2 registrations + docstring, **no decision logic**) and `tests/scan/scanner.test.ts` (additive `WARNING_CEILING` list + two ceiling tests + extended AT-23 loop; BROKEN blocker array unchanged). `harness.test.ts` unchanged; LG-001..LG-015 prior detectors unaffected. Both commits author+committer `noreply@anthropic.com`.

## QA proof
- Suite:        `npm test` @ HEAD `006cadd` → **143 passed, 0 failed, 0 skipped**, 17 files (lg001 6, lg002 4, lg003 4, lg004 4, lg008 4, lg010 5, lg014 4, lg015 6, engine 19, harness 13, registry 7, collect 7, redact 10, scanner 11, schema 25, sec1 5, serialize 9); `tsc --noEmit` clean.
- Regression:   Docs + all frozen S1/S2/S3 source untouched; only the two pre-existing files above additively changed (`scanner.ts` registrations only, `scanner.test.ts` additive). The 8 existing fixtures unchanged in decision (LG-010/LG-014 → `not_applicable` on all of them). LG-001..LG-015 prior detectors unaffected.
- Commit-1 iso: fresh worktree @ `3c83f41`, `npm ci` → **140 passed**, typecheck clean, `scanner.ts` still S3 (no lg010/lg014 registered), Commit-2 fixtures absent → green in isolation.
- **Warning-tier proof (the KEY new behavior — qa re-derived by running the composed scanner over all 10 fixtures):** both checks are Warning-ceiling, so a repo-side problem is emitted as outcome `fail` + severity `warning` (severity drawn from the registry via `makeFinding`, never hardcoded; NEVER outcome `warning`, NEVER blocker). The engine's `countFindings` routes fail+warning to warnings (not blockers); the blocker-only Rules 2/3 do not fire. Decision table: `broken-lg-001/002/003/004/008/015` → `not_ready` (blocker tier); **`broken-lg-010` → `ready_with_warnings` / exit 0** with LG-010 fail/warning + `externalVerification{resend, phase-3}`; **`broken-lg-014` → `ready_with_warnings` / exit 0** with LG-014 fail/warning + `externalVerification{sentry, phase-3}`; `clean-min` → `ready_with_warnings` (LG-015 pass/unverified); `unsupported` → `not_evaluated` / exit 3. These are the FIRST fixtures to demonstrate the warning tier rather than the blocker tier. No fixture yields unqualified `ready`. Each new fixture seeds exactly one warning fail and no blocker. All severities valid enums; every `externalVerification.phase === phase-3`; `validateReport` clean on all fixtures.
- Safety grep:  **Scanner-safety grep — the Stripe/provider live/test key prefix patterns, the webhook-secret prefix pattern, the Resend key prefix pattern, AND all Sentry-DSN shapes are ZERO** across the worktree AND both commit trees. The only `resend.dev` use is the public, non-secret provider-default sender address (`onboarding@resend.dev`); no API-key literal, no DSN literal — the LG-014 defect is genuinely DSN-absent. SEC-1: all 10 fixtures inert (no scripts/workspaces, `private: true`, excluded by vitest/tsconfig). SEC-2: no runtime network. SEC-3: no provider SDK deps/imports/client-instantiation, no `process.env` credential reads in `src/`. SEC-4: all new evidence via `buildEvidence`/`buildAbsenceEvidence`. SEC-6: `collect.ts` untouched. AT-23 determinism: byte-identical across two fixed-clock runs; suite passes twice identically. AT-24 fixture-path validity: both new `expected.json` evidence-path substrings resolve to real files. Re-verified independently by the main session pre-push (scanner-safety zero incl. Sentry-DSN shapes) before the fast-forward push.
- UI smoke:     N/A
- Acceptance tests: AT-10 / AT-14 exercised end-to-end through the real §9 harness (all three branches per detector: fail/warning, pass/unverified, not_applicable); AT-23/AT-25/AT-26 reinforced. AT-16 (real `golden/`) still deferred — `clean-min` remains the subset-scoped ceiling demonstration.

## Review
- Verdict: **pass** — no must-fix-now items. All ten mandated challenges cleared.
- Codex second-eyes: not available (no CLI/plugin).
- Product Trajectory Check: clean seams remain for further slices; the registry stays the single source of check metadata; the `externalVerification` passthrough is now proven for four external checks (LG-003/010/014/015). **LG-012 is now the last pending-external detector**, and it needs the model layer. The deterministic-only detector run is COMPLETE — the remaining seven checks all require a Layer-D+M / Layer-M model foundation the orchestrator must shape explicitly.
- NOTABLE ADJUDICATIONS:
  - (1) Warning-severity correctness traced end-to-end: fail+warning drawn from the registry → counted as a warning → `ready_with_warnings`, never `not_ready`, never unqualified `ready`.
  - (3) LG-010's last-two-labels registrable-domain heuristic (no Public Suffix List, disclosed) can ONLY under-warn — it collapses two distinct registrable domains into a match but can never split one domain apart. The multi-part-TLD case (`example.co.uk` vs `mail.example.co.uk`) collapses both to the same value and MATCHES → no false fail is possible; defaulting to pass/unverified on an indeterminable domain is the safe direction for a warning-only, externally-unverified check.
  - (5) LG-014's "unverified in production" reading (production-wired iff DSN wired AND env/release tagging present; a `process.env` reference counts as wired) is a sound, non-overclaiming deterministic criterion. The only false-warning cases are repos using implicit/dashboard-only mechanisms genuinely absent from repository config → a warning marked arrival-unverified is spec-faithful and never blocks.
  - Ceiling watch-item `hasAppSignal ⊇ scanner.supported` undisturbed — neither detector touches `detectStack`/`supported` or LG-015's applicability gate.
- Neither detector represents repository evidence as provider-side proof.

## Residue
- Deferred / follow-up packets (all reviewer-sourced, non-blocking, NONE must-fix-now):
  1. **LG-010 domain heuristic** uses last-two-labels (no Public Suffix List) — it under-warns on multi-part public suffixes and on separately-registered-but-authenticated sender domains. Revisit if/when a PSL is introduced.
  2. **LG-014 implicit-Sentry mechanisms** — does not recognize Sentry's implicit release-injection / dashboard-only DSN mechanisms. Acceptable (those are genuinely absent from repo config; a warning marked unverified is correct); refine later if needed.
  3. Carry forward from S3 (still open): the **CEILING WATCH-ITEM** (`hasAppSignal ⊇ scanner.supported` — add the AT-16 regression when `golden/` lands); the **LG-008** optional warning for a lone committed DB URL; the **LG-003** provider-agnostic webhook presence.
- Carry-forward obligations still binding: the **externalVerification obligation** is now proven in code for LG-003 (`stripe`) / LG-010 (`resend`) / LG-014 (`sentry`) / LG-015 (`dns`); among external checks **only LG-012 remains pending, and it needs the model layer**. **TEST-DATA POLICY** (<20 contiguous alphanumerics for every key-shaped fake, PLUS no Sentry-DSN shapes; every surface incl. receipts/memory; never allowlist a secret). Registry stays single source of check metadata.
- **Coverage after this slice: 8 of 15 detectors implemented** — LG-001/002/003/004/008/015 (blocker tier) + LG-010/014 (warning tier). Remaining 7 — LG-005/006/009/011/013 (Layer D+M), LG-007 (Layer M), and LG-012 (Layer D+M, external) — **all require the model layer**. There are NO remaining pure-D checks: the deterministic-only detector run is COMPLETE. The next slice faces a genuine fork (stand up the model layer OR do another non-detector slice) — flagged for the orchestrator.
- Separate user-flagged governance visibility item (non-blocking, outside Build OS scope, carried forward): the live substrate-readiness check still reports Claude, Manus, and surplus_recovery as missing surfaces — a distinct Gravito governance gap. New this session: the user shared that Gravito is a Vite+Express+Fly.io TypeScript SaaS (NOT Next.js/Vercel), so LaunchGraph's current golden-path scanner would honestly return `not_evaluated` (unsupported stack) on it; an Express/Vite/Fly recipe is a candidate future golden-path expansion if auditing the user's own portfolio becomes a goal. Product-direction note, not a Build OS action item.

## Open boundaries (awaiting explicit go)
- Push to the designated feature branch was executed under standing authorization AFTER qa green + reviewer pass + a two-repo scanner-safety grep (incl. Sentry-DSN shapes), landing as a fast-forward `47fbb8d..006cadd`.
- NO merge to any default branch and NO pull request — both await explicit approval.
- No deploys, no secrets touched/allowlisted, no provider access.
