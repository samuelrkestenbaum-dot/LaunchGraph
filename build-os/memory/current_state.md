# Current State

> The "where are we" snapshot. The orchestrator reads this first every session.
> The archivist advances it when a packet closes. Keep it short and true.

## Project

- **What this repo is:** **SugarBee.ai** — a production-readiness auditor for AI-built SaaS. The shipping wedge (Phase 1) is a narrow, structurally read-only static auditor for TypeScript/Next.js repos: 15 evidence-backed checks producing a Ready / Ready with warnings / Not ready decision. Never executes the scanned repo, reads no provider credentials, instantiates no provider SDKs; the only permitted network egress is SugarBee's own model API. Founding scope: `PRODUCT_SCOPE.md`.
- **Primary branch / base (CORRECTED at T-001, 2026-07-26):** the session-designated working branch is now **`claude/sugarbee-project-handoff-tdfylf`**, and **`claude/sugarbee-project-handoff-ic47uc` is a SECOND REF ON THE SAME LINEAGE, NOT A FORK.** Facts: local `tdfylf` was at `a00d194`, a **strict ancestor** of the work tip (**0 ahead / 18 behind**), and was **fast-forwarded to `f6a74c0`**; **`f6a74c0` is ALSO published as `origin/claude/sugarbee-project-handoff-ic47uc`**; `origin/claude/sugarbee-project-handoff-tdfylf` (still showing `a00d194`) is a **stale local tracking ref for a branch that never existed remotely**. `git merge-base HEAD f6a74c0` → `f6a74c0`. **Every SHA recorded in every prior receipt remains valid** — nothing needs restating. Both names also supersede the retired `claude/launchgraph-product-scope-43pgdx`. **CORRECTED AT T-002 (2026-07-27) — "THIS REPO HAS NO DEFAULT BRANCH" WAS FALSE AND WAS CARRIED SIX TIMES.** `git ls-remote --symref origin HEAD` → `ref: refs/heads/claude/launchgraph-product-scope-43pgdx`; the GitHub repo object confirms `default_branch: claude/launchgraph-product-scope-43pgdx` @ `a00d194`. What was **actually** true is much narrower: there was no branch *named* `main`/`master`, and `refs/remotes/origin/HEAD` was unset **IN THE LOCAL CLONE ONLY**. `a00d194` is a **strict ancestor** of the tip. **A PR WAS OPENABLE ALL ALONG.** T-002 created **`main` at `8652bac`** (a new name on existing history — **every SHA in every receipt stays valid**) and set `origin/HEAD` → `refs/remotes/origin/main`, so **`git merge-base HEAD origin/HEAD` → `8652bac`**. **`origin/HEAD` is a LOCAL ref and `git remote set-head -a` would revert it to `a00d194` (three commits behind, off this lineage) until the GitHub repository default is actually changed — see residue G1d, USER ACTION ONLY.**
- **Build/test command:** `npm test` (Vitest) · `npm run typecheck` (tsc --noEmit) · `npm run eval` (fixture harness). CLI: `npx tsx bin/sugarbee.ts scan [path] | eval`.

## Build OS location + runtime (changed 2026-07-25; runtime ADOPTED at T-001, 2026-07-26)

This project's Build OS memory lives **HERE, in the product repo**
(`build-os/memory/`, `build-os/packets/`, `build-os/receipts/`), installed via
canonical **`build-os/tools/project-bootstrap.sh`** (it was `install-project.sh`
before T-001). It previously lived in the ClaudeOrchestrator control repo, where
SugarBee's `P-001…P-005` receipts **collided by filename** with the framework's own
`P-001…P-025` development packets. That collision is now structurally impossible:
**ClaudeOrchestrator is the framework only** (canonical tip **`0a63f66`**);
SugarBee's project state is the product repo's own. Receipt ids `P-001…P-005` are
preserved unchanged.

**RECEIPT NAMESPACES — three series, deliberately disjoint:**
- **`P-0NN`** — SugarBee product packets `P-001…P-005`. **Frozen. Do not extend** —
  this id space collides by filename with canonical's own `P-001…P-025`, the exact
  collision the 2026-07-25 migration killed.
- **`A-S*`** — **reserved for the detector fork** (`A-S1b`, `A-S1c`, `A-S2`, `A-S3`,
  staged `A-S4`). A non-detector packet must never consume a slice number.
- **`T-NNN`** — **NEW at T-001: tooling / framework-adoption packets** in the product
  repo. Use this for anything whose subject is the Build OS runtime itself.

**MANAGED vs PRESERVED — new at T-001 and LOAD-BEARING.** The install manifest
(`build-os/.install-manifest.json`) lists **20 MANAGED files**, which the bootstrap
**overwrites** from canonical. **`build-os/memory/tool_router.md` and
`build-os/memory/skill_budget.md` ARE MANAGED.** **PRESERVED is ONLY**
`memory/current_state.md`, `memory/residue.md`, `packets/active_packet.md`,
`receipts/README.md`. **Any SugarBee-specific routing knowledge written into
`tool_router.md` will be silently reverted by the next bootstrap** — project-specific
routing facts must live in `current_state.md` / `residue.md` instead.

## Where we are

- **Last closed packet:** **A-S3** — Phase 1 Repository Auditor — **the `ModelCheck` seam + LG-009 (missing tenant-isolation evidence, Layer D+M)** (Fork Branch A, Slice 3). Two commits: (1) **the seam** — `src/model/modelCheck.ts` (types only), LG-006 **and** LG-005 migrated onto it, `scanWithModel` reduced to one loop over the fixed **append-only** array `MODEL_CHECKS` = `['LG-006','LG-005','LG-009']` (sequential, no `Promise.all`) with `createScanner` iterating the **same** array; `prefers` generalized to **ordered bands** via concatenated filters (one-band case byte-identical **by construction**), `DelegatedSurface` gaining `elidedByBand`; the duplicated disclosure helpers folded into `surface.ts`; both transmitted questions pinned as literals. Behaviour-preserving — all 11 hashes frozen. (2) **LG-009**, plus **SQL comment stripping** implemented as a **string-aware walker, not a regex**, applied at carrier read time. qa **GREEN**, reviewer **pass**. Commits `406d81b` + `04a5ea0` (amended four times: `6f677d3` → `913e2c5` → `428f113` → `04a5ea0`), on the build-os-only shaping commit `0e33643` (itself on `696518b`); **branch tip and code tip both `04a5ea0`**, pushed as fast-forward `0e33643..04a5ea0`.
- **THE CENTRAL OUTCOME — LG-009 SHIPS WITH BOTH DETERMINISTIC BRANCHES WITHHELD.** No `confirmed` fail, no `confirmed` pass. It emits only `not_applicable`, `unknown`, or model-derived `inferred` verdicts capped at 0.9 — **the first check in this program with both deterministic branches withheld**. The **fail** branch was withheld at shaping time on the settled reading (absence of RLS is not evidence of missing isolation; five false-blocker routes FB-1..FB-5, and FB-1 — a Prisma `$extends` app — is the **modal architecture of the target population**). The **pass** branch was implemented, reviewed, fixed twice, then **withdrawn mid-packet by USER DECISION** after **seven false-certification routes** across **four adversarial passes**, each pass finding new ones (worst: RLS DDL inside a **string literal** certified a repo with **zero RLS**). **The deciding argument:** because the Phase-1 ceiling is structural, **`pass` and `unknown` produce the identical decision and exit code** — the branch bought a line of report text and a skipped model call, against the risk of certifying tenant isolation on a repo that has none. **Routes 5–8 are unreachable BY ABSENCE OF THE BRANCH, not by patching.**
- **DETECTOR COVERAGE 10 → 11 of 15 — RECORD ONLY QUALIFIED: 11 of 15 implemented, of which 8 can move an offline decision, and 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.** LG-009's contribution to the 11/11 eval result is **exactly zero**, and it advances §9.3's 15/15-recall and 18/18-decision thresholds **not at all**. **"11 of 15" must not stand unqualified anywhere.** Remaining: **LG-007, LG-011, LG-012, LG-013**.
- **Numbers:** `npm test` **305 / 27 → 387 / 30**; `npm run typecheck` clean; `npm run eval` unchanged at **11 fixtures / 11-of-11 decisions / 0 blocker false positives**. Commit-1 isolation at `406d81b` in a fresh `npm ci` worktree: **323 / 29**, typecheck clean, eval 11/11 → GREEN. **Assertion budget 0 of 5 — the first packet to spend NONE** (the 22 assertions removed during the amend sequence were all in `tests/checks/lg009.test.ts`, a file that does not exist at base). Zero fixture movement (`fixtures/` tree object `a981446bac76039147d93efedd14a092c2aeadc1` identical at both ends). DC-6 blobs byte-identical. Ceiling holds.
- **LAST CLOSED PACKET — T-001 (2026-07-26): CANONICAL BUILD OS TOOLING ADOPTED INTO THE PRODUCT REPO.** Ran `build-os/tools/project-bootstrap.sh` from canonical ClaudeOrchestrator tip **`0a63f66`** (P-023 project-agnostic bootstrap, P-024 measured activation boundary, P-025 zero-touch global bootstrap). **Commit 1 `4920653`** — adds `build-os/tools/{project-bootstrap,capability-profile,supervise,specialist-handoff,skill-budget-audit}.sh`, `build-os/memory/skill_budget.md`, `build-os/global-claude-md.md`, `build-os/.install-manifest.json`; modifies `.claude/hooks/session-start-build-os.sh` and **exactly one marker line** in `CLAUDE.md` (`install-project.sh` → `project-bootstrap.sh`). **10 files, +1290/−2.** **Commit 2** — the reviewer-required activation gate plus the receipt and these memory updates. Base **`f6a74c0`**. qa **GREEN, 10/10 gates**; reviewer **fix-then-pass (as fixed)**. Receipt: **`build-os/receipts/T-001.md`**.
- **T-001's CENTRAL FINDING — ADOPTION ACTIVATED A PREVIOUSLY-DEAD PROMPT-CLASSIFYING SURFACE, AND THE CLASSIFIER IS TUNED TO THIS PROJECT'S OWN VOCABULARY.** At base `f6a74c0`, `build-os/` contained **only** `memory/`, `packets/`, `receipts/` — **no `tools/`** (`git ls-tree`), so `.claude/hooks/prompt-router.sh:16-22` resolved `HANDOFF=""` and the whole specialist-handoff block was **DEAD CODE**. This packet makes it resolve; `prompt-router.sh` is registered as **`UserPromptSubmit`** and `claude` is on PATH at `/opt/node22/bin/claude`. **Measured routes:** `"Shape A-S4 and pick the next detector"` → focused; `"keep going"` → focused; `"do an architecture review of the model layer"` → **ecc**; `"review the eval harness results"` → **ecc**; `"Next packet: LG-009 postgres RLS migration schema review"` → **ecc**. `CAP_ECC` matches `postgres.*(schema|migration|index|query|performance)`, `eval.*harness`, `architect(ure)?.*review` — **SugarBee's core subject matter.** The `ecc`/`zeroize` routes are exactly the two that (a) write `$HOME/.claude/settings.json` and `$HOME/.claude.json` — **outside the product repo, machine-global** — via `capability-profile.sh:6-8,53-71`, (b) register a third-party MCP fetched from GitHub (`uvx --from git+https://github.com/oraios/serena@68884f11…`), and (c) spawn an autonomous `claude -p` child for up to **900s** via `specialist-handoff.sh:210-307`. **External mutation + an unplanned agent launch without explicit go — against CLAUDE.md's hard gates.**
- **THE FIX (Commit 2), AND WHY IT IS WHERE IT IS.** `env.BUILD_OS_SPECIALIST_HANDOFF = "1"` in `.claude/settings.json` — the recursion guard read at `prompt-router.sh:16`. Chosen because **`project-bootstrap.sh:322-350` loads the settings dict, mutates ONLY `data["hooks"]`, and writes the rest back, so the `env` gate SURVIVES EVERY FUTURE CANONICAL REFRESH.** Editing `prompt-router.sh` or deleting the tools would **not** survive — both are managed and would be restored, producing permanent **DRIFT churn**. **Proven:** an `ecc`-classifying `UserPromptSubmit` payload produced only the routing reminder and did **not** invoke specialist-handoff; `~/.claude/settings.json` was not created; `~/.claude.json` untouched (pre-dates the session, mtime 15:26:10, `mcpServers` empty); the gate **survived `project-bootstrap.sh --force`**.
- **T-001 NUMBERS — ZERO PRODUCT MOVEMENT, PROVEN BY SUBTREE HASH, NOT ASSERTED.** `npm test` **387 / 30**; typecheck exit 0; eval **11 fixtures / 11-of-11 / 0 blocker FPs** — **identical at base and tip**. Subtree hashes unchanged: `src` `30f116ff`, `tests` `496e919b`, `bin` `49442941`, `fixtures` `a981446bac76039147d93efedd14a092c2aeadc1`, `package.json` `6cf6cc91`, `package-lock.json` `c9d039bf`, `vitest.config.ts` `7e7b1228`, `tsconfig.json` `014ddf5e`. **FOURTH CONSECUTIVE ZERO-FIXTURE-MOVEMENT PACKET.** Preserved files empty diff; ClaudeOrchestrator clean and still `0a63f66` (never-write-to-canonical held); **all 20 managed files byte-identical to canonical, 20/20 `cmp`**; safety grep clean (the only long-hex hits are the manifest's own sha256 integrity hashes and a pinned git commit in a serena dependency URL; `rm -rf` is scoped to `$STAGE` (mktemp, fail-closed guard) and `$BACKUP`). **Assertion budget: ZERO spent — no test file changed. NO TESTS WERE ADDED, correctly, for a vendored tooling copy: hash-equality against canonical plus a preservation probe were SUBSTITUTED for behavioural tests. Say that plainly; it is fidelity evidence, not behavioural evidence.**
- **DETECTOR COVERAGE UNCHANGED BY T-001 — 11 of 15 implemented, QUALIFIED: 8 can move an offline decision, and 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.** T-001 touched no detector. **"11 of 15" must not stand unqualified anywhere.** Remaining: **LG-007, LG-011, LG-012, LG-013**.
- **~~STATUS LABEL — DURABLY CONFIGURED, NOT ACTIVE (unproven).~~ SUPERSEDED AT T-002 — THE STATUS IS NOW PROVEN: CONFIGURED-BUT-NOT-FIRING.** Evidence: the `hook-once` mechanism was **validated** to create `${TMPDIR:-/tmp}/build-os-hook-once` on a `session_id`-bearing payload, and there are **ZERO markers from real events**; `CLAUDE_PROJECT_DIR` unset; no hook banner in the session context; `/tmp` not cleared (it holds a Jul-23 file). **Separately and independently: `settings.json` `env` IS NOT DELIVERED TO PROCESSES ON THIS SURFACE** — qa found **no `BUILD_OS_*` in the parent session's `/proc/*/environ`** despite the key being committed since `8652bac`, and the session was launched with `--settings /root/.claude/launcher-settings.json` whose `env` is `None`. **CONSEQUENCE, RECORD IT EXPLICITLY: T-001's `BUILD_OS_SPECIALIST_HANDOFF` gate RESTS ON THE SAME DELIVERY MECHANISM AND MAY ALSO BE INERT HERE.** Both are **dormant only because the hooks do not fire — which is NOT the same as being protected.**
- **LAST CLOSED PACKET — T-002 (2026-07-27): THE BUILD OS SOURCE IS PINNED; THE LONG-CARRIED GAPS ARE CLOSED OR AT A USER-ACTION BOUNDARY.** One commit **`c7baff9`** on base **`8652bac`** (`git merge-base HEAD 8652bac` → `8652bac`, strict ancestor). **2 files, +3/−2** — `env.BUILD_OS_SOURCE = "/home/user/LaunchGraph"` added to `.claude/settings.json`, plus one manifest churn line. A local `git remote set-head` repair rode along (a ref change, in no diff). qa **GREEN (DC-1..DC-7)**; reviewer **fix-then-pass (recorded as fixed)**. Receipt: **`build-os/receipts/T-002.md`**.
- **T-002's CENTRAL TECHNICAL RESULT — THE ROUTED MECHANISM WAS WRONG, AND THE HAZARD IS SELF-TRIGGERING.** The orchestrator routed a **frozen canonical clone**; **it does not close the hazard.** Three-arm control (canary line appended to the MANAGED file `build-os/memory/tool_router.md`, bootstrap invoked exactly as the SessionStart hook does): **(a) unpinned → canary REVERTED; (b) pinned to a frozen canonical clone → canary REVERTED; (c) pinned to the project itself → `nothing to install`, canary SURVIVED.** Reproduced **independently by BOTH qa and reviewer**. **The reason is structural, not versional:** a frozen clone still satisfies `is_canonical_src()`, so `SRC != TARGET` and the full **stage→backup→promote** path still runs — **freezing pins WHICH bytes get written, never WHETHER writes happen.** Arm (c) is a **TRUE ZERO-WRITE no-op**: qa proved it by leaving a tampered `source_sha` of 40 zeros in place afterward (an overwrite would have restored it). **THE REVIEWER'S CONTROL WAS STRONGER AND CHANGES THE HAZARD MODEL — IT DID NOT TAMPER THE MANIFEST: the canary edit ALONE flips the file-hash half of the cache gate (`project-bootstrap.sh:196-215`), so THE HAZARD IS SELF-TRIGGERING and needs NO external ClaudeOrchestrator commit. The clone-pin would have frozen only the `source_sha` half and left the file-hash half fully live.**
- **T-002 NUMBERS — ZERO PRODUCT MOVEMENT.** `npm test` **387 / 30**; typecheck exit 0; eval **11 fixtures / 11-of-11 / 0 blocker FPs**. Commit-1 isolation in a detached worktree at `c7baff9`: **387 / 30 GREEN**. Diff is **exactly two files** (`.claude/settings.json`, `build-os/.install-manifest.json`). `fixtures/` tree `a981446bac76039147d93efedd14a092c2aeadc1` — **FIFTH CONSECUTIVE ZERO-FIXTURE-MOVEMENT PACKET**. The pin **survives `project-bootstrap.sh --force`** (both `env` keys intact, hooks intact), including the harsher unpinned-`--force` variant. **ClaudeOrchestrator clean, still `0a63f66`, reflog untouched** — the arm-(b) clone left no trace. Safety grep clean (the diff adds 3 content lines). **Assertion budget 0 of 5** — no test file changed; as at T-001, **empirical control arms were SUBSTITUTED for behavioural tests. Say that plainly: it is mechanism evidence, not suite evidence.**
- **T-002 GAP STATUS.** **G1a CLOSED** (`origin/HEAD` set → `origin/main`; `merge-base` resolves for the first time). **G1b CLOSED** — **PR #1 open**: head `claude/sugarbee-project-handoff-tdfylf`, base `claude/launchgraph-product-scope-43pgdx`, 20 commits — **a REVIEW VEHICLE, NOT proposed for merge**. **G1c CLOSED** — `main` created at `8652bac`. **G1d OPEN, USER ACTION ONLY** — no `mcp__github__*` tool can set a repository default branch, `gh` absent from PATH. **G2 CLOSED — SECOND EYES OBTAINED, FIRST TIME IN SIX PACKETS.** **G3 CLOSED** with the fail-open caveat (residue TOP ITEM). **G4 ANSWERED** (see the status-label line above). **G5 OPEN, USER ACTION ONLY — and it is THREE branches, not one.**
- **DETECTOR COVERAGE UNCHANGED BY T-002 — 11 of 15 implemented, QUALIFIED: 8 can move an offline decision; 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.** T-002 touched no detector, no test, no fixture. **"11 of 15" must never stand unqualified.** Remaining: **LG-007, LG-011, LG-012, LG-013**.
- **T-002 OBTAINED THE FIRST INDEPENDENT, NON-CLAUDE REVIEW IN SIX PACKETS — AND IT FOUND TWO REAL PRODUCT DEFECTS ON CLOSED WORK.** Codex (`chatgpt-codex-connector[bot]`) reviewed `8652bac` via PR #1. (1) **P1 `.claude/settings.json:4`** — independently concluded the self-update hazard was fully active and a project-self pin was needed: **third-party corroboration that T-002's pin was necessary and correctly designed**, not a new defect. (2) **P1 `src/checks/lg005.ts:205` (A-S2)** — a webhook delegating to a helper that is **idempotent by construction** is excluded by `selects`, so the model cannot distinguish it from a helper that inserts or mails on every delivery — **yet LG-005's verdict can still become a blocker**. **Defeater class 6, and an ALLOWLIST OF POSITIVES where the governing rule requires a DENYLIST OF DEFEATERS.** (3) **P1 `src/checks/lg009.ts:424` (A-S3)** — `incompleteSurface` **ignores the omitted unmatched/trailing band**, so a model `fail` is accepted as an inferred blocker although the exonerating query was withheld; **DC-11 must account for the trailing generic band, or prove those candidates cannot contain scoping.** (4) **P2 `build-os/tools/project-bootstrap.sh:209`** — an **UPSTREAM** defect: the cache path exits before the `CLAUDE.md` and `settings.json` merges, and **those two managed outputs are absent from the manifest's `files` map**, so a bootstrap reports the runtime up to date without repairing drift. **Owed upstream — NEVER write to ClaudeOrchestrator from here.** **BINDING: findings on CLOSED receipts are input to a FUTURE packet, never a reopening. Receipts are append-only.**
- **LAST CLOSED PACKET — EV-001 (2026-07-28): THE EVIDENCE-METHOD ALLOCATION AND PHASE-BOUNDARY DOCTRINE IS NOW A DURABLE ARTIFACT.** **ONE commit `258cc70`** on base **`9706d87`** — **2 files, +497/−2**: `specs/evidence-allocation-and-phase-boundaries.md` (**NEW, 495 lines**) plus **exactly 2 lines** of header status/cross-reference in `specs/phase-1-repository-auditor.md`. **ZERO PRODUCT CODE.** Originally `e3c0425`, **amended once** to fold in the qa DC-13 fix and the four reviewer-required fixes (HEAD verified to be our own unpushed commit first, per the standing `--amend` constraint). qa **GREEN on DC-1..DC-12, DC-14, DC-15; RED on DC-13, now fixed**; reviewer **fix-then-pass (recorded as fixed)**. Receipt: **`build-os/receipts/EV-001.md`**.
- **NEW SERIES `EV-NNN` — evidence-method allocation and phase-boundary doctrine; packets that produce a durable decision artifact and ship no product code. FORCED BY EXISTING NAMESPACE RULES, NOT PREFERENCE.** **`P-0NN` is FROZEN** (collides with canonical's `P-001…P-025`); **`A-S*` is reserved for the detector fork** and **a non-detector packet must never consume a slice number** — consuming **A-S4's** number would have made a **pause look like a completion**; **`T-NNN` is the Build OS *runtime* series**, whereas EV-001's subject is the **product's evidence architecture**. **Rejected:** `D-`/`M-` (collide with Layer D / Layer M), `PL-` (reads near the frozen `P-`), `S-` (reads as the A-S slice suffix).
- **EV-001's CENTRAL FINDING — THE STRATEGIC REFRAMING IS A *RECOVERY* OF THE FOUNDING ARCHITECTURE, NOT A PIVOT.** `PRODUCT_SCOPE.md` **§35 already assigns the phases** — **`:1598` Phase 1 Repository auditor · `:1614` Phase 2 AGENT COORDINATOR · `:1626` Phase 3 PROVIDER INSPECTOR AND ROUTER · `:1640` Phase 6 VERIFICATION** — and the Phase 1 spec's **§13 already defers most of the allocation**: **`:646`** read-only provider API inspection → Phase 3; **`:647` external-side resolution of LG-003/010/012/014/015 → Phase 3**; **`:648`** DNS/TLS/domain probes → Phase 3; **`:651`** canonical customer-journey execution and the unqualified `Ready` → Phase 6. **So the four evidence modes map onto an ALREADY-APPROVED phase map; the document REFINES and does not replace.** **RECORDING "PHASE 2" FOR PROVIDER/LIVE EVIDENCE WOULD HAVE BEEN WRONG** — §35's Phase 2 is the **agent coordinator** — **and it would have implied the numbering was informal, which is exactly the crack the boundary leak comes through. Phase numbers are citations, not shorthand.**
- **LG-009 IS THE SINGLE GENUINE DEPARTURE FROM THE SPEC.** Verified: **`P1§3:120` marks LG-009 `External component: No`**, and **LG-009 is ABSENT from §13's `:647` row**. Everything else in the allocation is already in §13. **The spec assumed tenant isolation was repository-determinable; implementation proved it is not** — at the cost of a full packet, **seven false-certification routes across four adversarial passes**, and **both deterministic branches withheld** (A-S3). Recorded as a **PROPOSED** amendment (**§9.1**), **NOT ENACTED** — **`P1§3`'s cell still wins today**, and both panels plus §3.7's title say so explicitly.
- **EV-001 NUMBERS — ZERO PRODUCT MOVEMENT, PROVEN BY SUBTREE HASH.** `npm test` **387 / 30**; typecheck **exit 0**; eval **11/11 · 0 blocker FPs**. Diff **exactly 2 files**; subtrees **byte-identical** base↔HEAD (`src` `30f116ff`, `tests` `496e919b`, `bin` `49442941`, `build-os` `b4be19ff`, `.claude` `1c0e5943`). `fixtures` tree `a981446bac76039147d93efedd14a092c2aeadc1` — **SIXTH CONSECUTIVE ZERO-FIXTURE-MOVEMENT PACKET**. **Assertion budget 0.** **`P1§3`/`§10`/`§13` byte-identical base↔HEAD** — the packet demonstrably did not amend the authority it defers to. Both panels **15 unique ids, exactly 30 `^| LG-` lines**. **Boundary grep: zero imperatives, zero SDK names, ZERO URL/HOST LITERALS DOC-WIDE.** Safety grep clean, zero key-shaped literals. ClaudeOrchestrator clean at `0a63f66`.
- **EV-001's PROCESS RESULT — qa AND THE REVIEWER DISAGREED ON A ONE-LINE FACT AND ONLY DIRECT SOURCE VERIFICATION SETTLED IT.** qa's DC-13 RED: the doc cited the LG-005 defect site as `src/checks/lg005.ts:205`, but **`:205` is `prefers`** (a *ranking* predicate that excludes nothing); the **excluding** predicate the prose describes is **`selects` at `:203`**. **The reviewer independently asserted `:205` IS `selects`. The main session read the source: `:203` is `selects`, `:205` is `prefers` — qa was RIGHT, the reviewer was MISTAKEN on that point.** Re-verified by the archivist at close. **CONSEQUENCE: the `lg005.ts:205` citation carried in T-002's record is INHERITED FROM CODEX AND IS OFF BY TWO — the defect is real, the line number was wrong. The correct site is `src/checks/lg005.ts:203`.** Receipts are append-only; `T-002.md` is not edited.
- **EV-001 ANSWERED THE LG-009 BAND-COUNT OPEN QUESTION FROM SOURCE — AND THE ANSWER NARROWS THE HOLE.** `src/scan/surface.ts:174` sizes `elidedByBand` as **`bands.length + 1`** (the `+1` is the **trailing unbanded slot**) while `src/checks/lg009.ts:423` sums **only `[0] + [1]`**. The open worry was that LG-009 ranks across **three** bands, which would mean `[0]+[1]` misses a real band **and** the trailing slot. **It does not: `src/checks/lg009.ts:412-415` passes EXACTLY TWO `prefers` predicates** (`SCOPING_MECHANISM_RE`, `TENANT_COLUMN_RE`), so **`bands.length === 2`, `elidedByBand.length === 3`, and `[0]+[1]` omits EXACTLY index 2 — the unbanded remainder, and nothing else.** **The hole is precisely what Codex described; it is NOT wider. "Three ordered bands" was a misreading of `elidedByBand`'s LENGTH, not the band count.** The fix packet should still re-derive this from the code.
- **EV-001 SECOND EYES — NOT OBTAINED. RECORD IT HONESTLY.** The reviewer confirmed **`codex` is absent from PATH** and **no plugin route exists**; the only live Codex channel is the **GitHub PR bot, which requires a push**, and nothing was pushed. **NO INDEPENDENT SECOND-MODEL PASS HAPPENED ON EV-001 — do not let T-002's "second eyes obtained" be read as covering it.** **STANDING QUALIFIER, from the reviewer's own caveat: PROSE REVIEW IS WEAKER EVIDENCE THAN CODE REVIEW — there is no test that fails if a sentence is misread, and the failure mode is a reading three sessions from now under delivery pressure.**
- **EV-001 REVIEWER — CHARGE 1 (BOUNDARY LEAKAGE) CAME BACK *UNFOUNDED*, AND THAT IS A REAL RESULT.** The reviewer grepped **every permissive modal** and found **all ten inside a negation**; **§4 restates SEC-2/SEC-3 as binding INDEPENDENTLY of `P1§10`**, so the ban does not depend on §4.1 surviving; **every "Vendor mechanism today" cell naming a real surface sits in a row whose phase cell says Phase 3 or Phase 6 — no mechanism appears un-phased**; and the document's own answer to §1.3 is correct: ***"An allocation table is an instruction. It cannot and does not satisfy a structural requirement."*** **A builder cannot cite this text to justify instantiating a provider SDK.** **Four required fixes were applied in the amend** — the sharpest being that **§1 had declared the document's OWN gates non-normative** (§4/§5.3/§6 are none of §1's "exactly two things", leaving **§5.3's gate and §6's clause with NO HOME DOCUMENT**, so a builder could cite §1 to discard exactly the two rails while keeping the reclassification); also **§6's release valve was disjunctive where residue R1 is conjunctive**, **§5.1 row 2 invented a per-check `ready`**, and **D-2's replacement had deleted the last explicit-go sentence in the spec**. Details in `build-os/receipts/EV-001.md`.
- **TRAJECTORY FLAG — THE MOST IMPORTANT FORWARD ITEM. THIS IS THE THIRD CONSECUTIVE PACKET WITH ZERO DETECTOR MOVEMENT (T-001, T-002, EV-001), AGAINST FOUR UNIMPLEMENTED DETECTORS AND TWO NAMED OPEN DEFECTS. Doctrine is now well-capitalized relative to code — each packet was individually justified; the PATTERN is the risk. THE NEXT PACKET SHOULD BE CODE.** Leading candidate: **LG-005's delegated-helper blind spot (EV-001 §7.1, `src/checks/lg005.ts:203`)** — a **shipped, repository-authoritative detector that can currently manufacture a false blocker**, which **no provider and no probe can fix**. **Hardening it is a better use of the same effort than a twelfth detector, and it honours the A-S4 pause rather than working around it.**
- **DETECTOR COVERAGE UNCHANGED BY EV-001 — 11 of 15 implemented, QUALIFIED: 8 can move an offline decision; 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.** EV-001 touched no detector, no test, no fixture. **"11 of 15" must never stand unqualified.** Remaining: **LG-007, LG-011, LG-012, LG-013**.
- **Now:** none active.
- **Next:** **CODE, not doctrine — see the TRAJECTORY FLAG above.** Staged candidate: **LG-005 delegated-helper hardening** (`src/checks/lg005.ts:203`, EV-001 §7.1) — awaiting orchestrator shaping + explicit go. **A-S4 is PAUSED BY USER DIRECTIVE, NOT ABANDONED**: all eleven binding shaping inputs are **preserved, not cleared**, in `build-os/packets/active_packet.md`. **The thesis is not abandoned; the build sequence is adjusted.**

## Stable facts (slow-changing)

- **THE BUILD OS RUNTIME IS SELF-UPDATING, AND THAT IS A STANDING CONSTRAINT (T-001).** `.claude/hooks/session-start-build-os.sh:18-31` runs `project-bootstrap.sh --target "$ROOT"` on **EVERY SessionStart**, with **no `--dry-run` and no `--verify`** — a live write. `resolve_source()` **hard-codes `/home/user/ClaudeOrchestrator` first**, so the vendored copy does **not** shield this repo. Steady state is quiet (the cache gate requires a canonical SHA match **AND** a sha256 match on all 20 managed files), but **on a cache miss it overwrites all 20 managed files plus `.claude/settings.json` and the `CLAUDE.md` managed block.** qa **demonstrated** this in a throwaway repo: a simulated drift **silently reverted a local edit to `build-os/memory/tool_router.md` to canonical content.** **The trigger is entirely OUTSIDE this repo** — anyone committing to, or even `git checkout`-ing in, `/home/user/ClaudeOrchestrator`, which currently holds **two branches with disjoint histories** (`claude/sugarbee-project-handoff-tdfylf` `0a63f66` and `claude/add-build-os`, empty merge-base). It does not commit, so it does not literally breach "hold all `build-os/` commits until close" — **but a builder running `git add build-os` or `git commit -a` mid-packet sweeps it in**, the same collision class that previously required a reflog repair. **MITIGATION APPLIED AT T-002 (`c7baff9`): `env.BUILD_OS_SOURCE = "/home/user/LaunchGraph"` in `.claude/settings.json`** — `SRC == TARGET`, the bootstrap exits `nothing to install`, a **TRUE ZERO-WRITE no-op**, and it **survives `--force`**. **THREE CORRECTIONS TO THE T-001 TEXT ABOVE, all proven at T-002:** **(i) THE HAZARD IS SELF-TRIGGERING.** The trigger is **NOT** entirely outside this repo — a local edit to any managed file **alone** flips the file-hash half of the cache gate (`project-bootstrap.sh:196-215`). No external ClaudeOrchestrator commit is needed. **(ii) "TWO BRANCHES WITH DISJOINT HISTORIES" IS A STALE LOCAL REF.** ClaudeOrchestrator's remote has **SIX** branches; `origin/claude/add-build-os` reads `0f831fd` locally and **`0a63f66`** on the remote. **(iii) FREEZING A CLONE DOES NOT WORK, AND `~/build-os/.canonical-source` IS NOT A VERSION PIN.** A frozen canonical clone still satisfies `is_canonical_src()`, so the full stage→backup→promote path still runs (arm (b): canary reverted). And `canonical_sha()` reads `git rev-parse HEAD` **LIVE** from the resolved path — `STAMPED_SHA` is only a fallback when the resolved source has no git history. **The stamp pins the SOURCE PATH, NEVER THE SOURCE CONTENT. A future session must not "apply the stamp" and believe this closed.** **AND THE PIN FAILS OPEN — see the residue TOP ITEM before moving or re-cloning this repo.** **The only working upgrade form is `BUILD_OS_SOURCE=/home/user/ClaudeOrchestrator bash build-os/tools/project-bootstrap.sh --target /home/user/LaunchGraph --force`** — `c7baff9`'s commit message says otherwise and **is wrong**, because `resolve_source():65` consults `$BUILD_OS_SOURCE` first **regardless of which copy of the script executes**.
- **THE TOOL ROUTER'S CAPABILITY TABLES ARE HOST-SPECIFIC AND ARE FALSE ON THIS SURFACE (T-001, verified 2026-07-26, Claude Code on the web / remote container).** They were verified at **P-012 on a Mac**. Live evidence here: **router UNDERSTATES** — it says Stripe & Cloudflare Developer Platform are "installed, need auth", but `ListConnectors` reports **both `connected: true, enabledInChat: true`**. **Router OVERSTATES** — the "Build accelerators (verified 2026-07 — P-012)" table marks Serena / Repomix / ccusage / Trail of Bits / Context Mode **ACTIVE**, yet `serena`, `repomix`, `ccusage` and `codex` are **all absent from PATH**, `claude plugin list` returns "No plugins installed", and there is **no `~/.claude/plugins` and no `~/.claude/settings.json` — **every ACTIVE claim in that table is false here.** **`skill_budget.md` is INAPPLICABLE** — its premise (685 skills / 171,824 chars, 5.7× over a 30,000-char budget; disable `ecc@ecc`) is a Mac-host condition; `build-os/tools/skill-budget-audit.sh --claude-dir ~/.claude 30000` reports **0 installed / 0 enabled plugins**, so both the problem and its remediation are absent. **Confirmed accurate:** Google Calendar / Drive / Microsoft 365 installed but `enabledInChat: false`. **Actually live here:** 16 claude.ai connectors (21st, Apollo.io, Clay, Cloudflare, Docusign, Gmail, Higgsfield, HubSpot, Hugging Face, Netlify, Notion, Otter.ai, Slack, Stripe, Supabase, Zapier), 10 enabled plugins (design, data, productivity, brand-voice, marketing, sales, small-business, legal, cowork-plugin-management, ui-ux-pro-max), the `github` and Claude Code Remote session MCPs, and **Build OS agents 5/5 discoverable**. **This is the router's own "no-route-to-unverified" rule catching the router: RE-VERIFY PER SURFACE; DO NOT INHERIT P-012'S HOST VERDICTS.**
- SugarBee.ai product repo is GitHub **`samuelrkestenbaum-dot/LaunchGraph`** (the repo slug still says LaunchGraph — the brand rename to SugarBee.ai is not renamable from a session); local checkout **`/home/user/LaunchGraph`**. Control repo (Build OS framework only): `samuelrkestenbaum-dot/ClaudeOrchestrator` at `/home/user/ClaudeOrchestrator`.
- **Check ids keep the `LG-` prefix** — spec-declared "stable, versioned" (~414 occurrences); moving them to `SB-` is a deliberate, separate packet if ever requested.
- SugarBee.ai's founding scope is PRODUCT_SCOPE.md (root commit c7267e8).
- SugarBee.ai's first buildable target is the Part II production-readiness layer.
- Phase 1 spec lives at `specs/phase-1-repository-auditor.md` in THIS repo (commit 5621aa9). **The stale "not yet implemented" header was RECONCILED AT EV-001 (`258cc70`)** — it now reads *"approved scope; implementation underway, one gated packet at a time"* and cross-references `specs/evidence-allocation-and-phase-boundaries.md`. **The "one gated packet at a time" wording is LOAD-BEARING and was a reviewer-required fix**: the first replacement had removed the spec's ONLY explicit-go sentence, leaving the authoritative document with no permission language at all.
- Phase 1 decision ceiling is `ready_with_warnings` until Phase 3+.
- **THE EVIDENCE-METHOD ALLOCATION DOCUMENT EXISTS** (EV-001, `specs/evidence-allocation-and-phase-boundaries.md`, 495 lines). **Normative for EXACTLY TWO THINGS — evidence-source allocation and phase ownership — PLUS, as a reviewer-required fix, §4 (FORBIDDEN), §5.3 (GATE) and §6 (LG-009 non-deletion), which are enumerated as ADDITIONALLY NORMATIVE and framed as RESTRICTIONS ONLY: THEY GRANT NOTHING.** `P1§3` **remains the sole authority for what a check MEANS** and wins wherever this document could be read as altering it. Four evidence modes: **repository analysis, provider-native inspection, live probes, agent remediation.** **15-row matrix in two panels**, separating the **general rule** (who holds the fact) from the **vendor mechanism** (how it is fetched today) so the doc survives a vendor changing its surface. **LG-003 and LG-012 are SPLITS, not reassignments** — each keeps a repository-deterministic blocking half in Phase 1. **Every divergence from `P1§13` is a PROPOSED amendment (§9); NONE is enacted.** **`SEC-8′` is marked *proposed*** — a non-normative section may not mint identifiers into `P1§10`'s SEC namespace. **The six-class defeater taxonomy is INLINED in §5.3** (it was invoked three times and never located, which made the gate undischargeable). **ZERO URL/host literals doc-wide, by design.**
- **The MODEL-LAYER SUBSTRATE now EXISTS** (P-005, branch tip `fb5663b`): an injected `ModelClient` (interface + `InferenceRequest`/`InferenceResult` + the `buildUntrustedDataEnvelope` SEC-5 wrapper) + `FakeModelClient` (drives ALL tests, no real network) + `RealModelClient` (bin-only, built-in global `fetch`, no SDK) + a pure D→M inference contract (`runInferenceContract`/`InferencePresentation`: cite-or-discard, cap 0.9, `inferred`/`contradictory`, builds Findings via `makeFinding`). **EMIT-ONLY — `engine.ts` + `schema/index.ts` are byte-identical to base (blob SHAs unchanged); the model only emits the right classification/confidence and the existing engine Rules 3/4/5/6 + schema do the rest.** Model results are at best `inferred`, capped 0.9, never `confirmed`; `contradictory` when the model verdict conflicts with a deterministic supporting fact; uncited claims discarded. **The credential boundary is the bin only** — `RealModelClient` reads no `process.env`; config is injected at `bin/sugarbee.ts` (spec §10's one permitted egress, SugarBee.ai's OWN model API — NOT provider credentials, SEC-3 intact). `--offline` / model-unconfigured → the model-dependent check emits `unknown` ("model layer disabled"), **closing AT-27's owed half**; the report stays valid + deterministic. Zero new RUNTIME deps.
- **The `sugarbee` scan/eval CLI EXISTS** (P-004, extended in P-005): `run(argv, io): number` — pure, synchronous, returns the exit code; all effects flow through an injected `Io {stdout, stderr, now, cwd, writeFile}`; the bin runs via `tsx` (DEV-only dep). Deterministic exit codes: `ready`/`ready_with_warnings` → 0, `not_ready` → 1, scan/parse error → 2, `not_evaluated` → 3 (unsupported stack → exit 3, closing AT-24 with ZERO human-surface findings). `--json` → `serializeReport` to stdout only, zero files; default → `report.json` + `report.md` + a human banner, writes CONFINED to `--out`. Flags: `scan [path]`, `--json`, `--out`, `--offline`, `--checks`, `--app`; plus `eval`. P-005 additively extended `run.ts` (shared `emitReport` + an async overload; the sync two-arg path unchanged) and the bin (constructs `RealModelClient` from bin-read config; `--offline`/no-model → sync path); `scanner.ts` gained a sync offline `unknown`-emit for LG-006 + a new async `scanWithModel` (the sync two-arg path byte-identical).
- SugarBee.ai now has the S1 proof surface + scan substrate + **ELEVEN working detectors** — **eight deterministic (LG-001/002/003/004/008/010/014/015) + three Layer D+M (LG-006, LG-005, LG-009)** — all wired into the runnable CLI at tip `04a5ea0`. Source: S1 surface (`src/schema`, `src/checks/registry`, `src/decision/engine`, `src/report/serialize`, `src/eval/harness`) + scan substrate (`src/scan/{collect,redact,scanner}.ts`) + detectors `src/checks/lg00{1,2,3,4,5,6,8,9}.ts`, `lg010.ts`, `lg014.ts`, `lg015.ts` + the model layer `src/model/{client,fakeClient,realClient,inference,modelCheck}.ts` + the shared webhook locator `src/scan/webhook.ts` (A-S1b) + the delegated-surface seam `src/scan/surface.ts` (A-S2, extended A-S3) + CLI (`src/cli/{args,io,run,reportMd,banner}.ts`, `bin/sugarbee.ts`). **387 tests / 30 files** (`npm test`); `npm run eval` → **11 fixtures, 11/11 decisions correct, 0 blocker FPs**. **Zero runtime deps** (tsx is dev-only; `RealModelClient` uses built-in `fetch`, no SDK).
- **THE `ModelCheck` SEAM EXISTS** (A-S3, `src/model/modelCheck.ts` — types only, importing nothing from `src/checks/`). The third shared seam after `webhook.ts` and `surface.ts`. **Five load-bearing properties:**
  1. **SETTLEDNESS IS THE ABSENCE OF A REQUEST, NOT A BOOLEAN — this is the POLARITY PROPERTY, and it is the seam's whole point.** `request: undefined` means "no model may be consulted." A per-check `isSettled` boolean would have to be supplied with the right polarity every time, and its plausible default (`() => false`) would **silently ship an `infer` call — repository text off-process — for a check that had already decided**. With `request: undefined` there is **no polarity to get backwards**, and "no request" cannot accidentally mean "ask anyway." `interpret` enforces the same rule independently, so a caller ignoring the contract still cannot let a judgment override a settled deterministic signal — pinned by a test.
  2. **`MODEL_CHECKS` is ONE FIXED, APPEND-ONLY array** — `['LG-006','LG-005','LG-009']`, **sequential, no `Promise.all`**. Reordering invalidates positional assertions and breaks AT-23 comparability.
  3. **`scanWithModel` AND `createScanner` iterate the SAME array** — which is what stops a detector being wired into one path and not the other (**the AT-27 defect class**).
  4. **`--checks` model-call suppression is now STRUCTURAL, not per-check.** A security property, not a convenience.
  5. **`surface()` returns a BOUND object** so each check's candidate shape stays private; a generic parameter would collapse to `any` the moment the array held two checks with different candidate types.
  **Evidence base, recorded honestly:** the seam was justified against **{settles-fail, settles-nothing, settles-pass}**; with LG-009's pass branch withheld the true base is **{fail, nothing, nothing}** and **the settles-`pass` shape is UNPROVEN AND OWED** (LG-012's `externalVerification`-on-unknown is the next structural variant due). **But the withdrawal produced STRONGER evidence than the original argument:** LG-009 changed settle-shape **late in the packet** and the seam absorbed it with **exactly one line** plus an import, with `modelCheck.ts`, `surface.ts`, `lg005.ts` and `lg006.ts` **byte-identical between Commit 1 and the final Commit 2**. An interface that survives its motivating consumer changing shape mid-packet is better evidenced than one that fit three consumers on paper.
- **`prefers` IS NOW ORDERED BANDS** (A-S3): `readonly ((c)=>boolean)[]`, implemented as **successive filters over the remainder rather than a sort**, so the one-band case is **byte-identical by construction**, not by a sort-stability argument. `DelegatedSurface` gains `elidedByBand` — indexed as `prefers` with a trailing unbanded entry, always summing to `elided` — so a caller can tell **what kind** of evidence the cap dropped. Losing generic matches is noise; **losing the specific band means the surface can no longer support a confident negative verdict**, which is exactly what DC-11 keys on. `elisionDisclosure` / `withheldNonSourceDisclosure` now live **once**, in `src/scan/surface.ts`; zero copies remain in `lg005.ts`/`lg006.ts`. Both transmitted questions are pinned as **full literals** in `tests/model/questionPin.test.ts`, on a repo exercising **both** disclosures at once.
- **THE STRING-AWARE SQL COMMENT WALKER EXISTS** (A-S3, in `src/checks/lg009.ts`): comment stripping applied at carrier read time as a **walker, not a regex** — it tracks string-literal state, so `--` and `/* */` inside a string literal are not treated as comments and RLS DDL inside a string literal is not treated as DDL. Built in response to defeater routes 2, 3 and 8.
- **LG-009 IS THE FIRST CHECK WITH BOTH DETERMINISTIC BRANCHES WITHHELD** (A-S3). **What it CAN do:** determine that a repo declares a tenancy boundary in an in-repo schema (**name + foreign-key conjunct — the conjunct is what kills FB-3**) and rule itself out when it does not; surface the application code most likely to contain scoping, ranked **mechanism-first** across three ordered bands; ask a model whether that code scopes queries and report `inferred` pass/fail **capped at 0.9**; **refuse to report `fail` when the cap ate the exonerating band** (DC-11 — asymmetric by design: incompleteness can only hide exoneration). **What it CANNOT do:** assert that a repository **is** isolated, or that it **is not**. **There is no deterministic verdict beyond applicability.** **Offline — which is what eval and every fixture exercise — LG-009 emits only `not_applicable` or `unknown`, and contributes no fail, no warning, and no blocker to any offline scan, ever.** It carries **no `externalVerification`** (§3 External = No) and **no `establishesVerdict: 'fail'`**. **AT-09 is OWED and has NO EVAL PATH to closure**, since eval is offline and LG-009 has no offline verdict.
- **THE SHARED WEBHOOK LOCATOR EXISTS** (A-S1b, `src/scan/webhook.ts`): one path predicate + `locateWebhookHandlers(fileset)` returning **ALL** matching handlers in deterministic order. **One locator = one contract: all-handlers (`.filter()`) semantics**, adopted by LG-006 and LG-005, already shipped by LG-004. Determinism rests on `collect.ts:270` sorting collected files by path. NOTE: `lg003.ts:51` still carries a **third byte-identical copy** of the predicate — not yet migrated (residue). **A-S1c added a second shared predicate, `isCodeFile` (exported from `src/scan/collect.ts`), consumed by `webhook.ts` + `lg006.ts`** — but unification is **only at the sites A-S1c touched**: `lg003.ts:35`, `lg015.ts:39`, and `lg010.ts:62` still carry private `CODE_EXT_RE` copies. Do not describe `isCodeFile` as the single source of truth.
- **LG-006 IS A GENUINE D+M DISJUNCTION** (A-S1b): **no cancellation signal anywhere in the repo → Layer D → `fail`/`confirmed`/1.0/blocker, standing under `--offline`**; branch present but downgrade unproven → Layer M → `inferred` (cap 0.9) online / `unknown` ("model layer disabled") offline; no webhook handler → `not_applicable`. The blocker gate is **repo-wide (`hasCancellationSignalAnywhere`)**, not handler-file-scoped — that is what stops it manufacturing a false blocker on a correct handler that *delegates* the branch to another module. **Two load-bearing consequences:** (a) `collect.ts:47`'s **`node_modules` exclusion is load-bearing** — the `stripe` package's own typings name `customer.subscription.deleted`, so a future "scan vendored dependencies" mode would silently kill this blocker on every Stripe repo; (b) the repo-wide scan reads raw content of ALL collected files, so a README / agent spec / commented-out branch / SQL comment merely *naming* the event suppresses the blocker to `unknown` — direction-safe (under-warn only) but wider in SugarBee's AI-built-SaaS target population.
- **THE D→M SURFACING CONTRACT EXISTS** (A-S1c, `src/model/inference.ts` + `src/checks/lg006.ts` + `src/scan/scanner.ts` + additive `isCodeFile` in `src/scan/collect.ts`). Three stable rules bind every Layer D+M check:
  1. **The surface follows the QUESTION's scope, not the file.** `request.excerpts` = every located handler (whole file) **plus** up to **5** additional **code** files elsewhere in the repo carrying the signal, path-sorted, windowed **−10/+30** around the first match, with any elision disclosed **in `request.question`** (NOT in `Evidence.note` — `buildUntrustedDataEnvelope` does not transmit that field). **Only code files may ever be surfaced** — SEC-5 blast radius: zero non-code excerpts reach a prompt.
  2. **`contradictory` forces `outcome: 'fail'`.** Spec §4.2 rule 4 — "the fact wins": the deterministic fact carries `establishesVerdict: 'fail'`, so `fail` IS the fact-wins outcome and `contradictory` is the classification recording "needs human review." Engine rule 5 then fires → `requires_confirmation`, counted as a warning. A contradictory blocker-capable `fail` can **never** manufacture `not_ready`. Choosing `unknown` instead would make rule 5 dead code for every model-derived finding.
  3. **The deterministic layer settles BEFORE the model is consulted at all.** When the check is settled, `scanWithModel` **skips `model.infer` entirely** (provable: zero `infer` calls). **A-S3 made this structural** — settledness is now the absence of a request, not a per-check boolean.
  **`fact:lg006.no-cancellation-branch` is RETIRED** (handler-scoped, and simply false on delegating repos) in favour of **`fact:lg006.no-code-cancellation-signal`** (`establishesVerdict: 'fail'`). **A-S2 closed the false-fact gap for `.mts`/`.cts` and stopped the false claim for `.sql`/`.prisma`.** What survives is **disclosed** withholding of `.sql`/`.prisma` carriers, pending a SEC-5 adjudication.
- **The WARNING TIER is proven in code** (P-003-S4): LG-010/LG-014 are Warning-severity, so a repo-side problem is outcome `fail` + severity `warning` (severity from the registry via `makeFinding`, never hardcoded, never outcome `warning`, never blocker). The engine routes fail+warning to warnings, so a fixture seeding only such a problem yields `ready_with_warnings` (exit 0), NOT `not_ready`. LG-001/002/003/004/008/015 remain the blocker tier. Still no fixture yields unqualified `ready`.
- **externalVerification obligation is proven in code** for LG-003 (`stripe`), LG-010 (`resend`), LG-014 (`sentry`), and LG-015 (`dns`); LG-008 carries it only on its partial/external branch (`supabase`), never on its confirmed-fail branch. LG-005 and LG-009 correctly carry none on any branch. The substrate supports it on model-derived findings via `InferencePresentation.externalVerification`. **Among external checks only LG-012 remains pending — it is Layer D+M and lands LAST (A-S7); its `externalVerification`-on-unknown shape is also the owed structural variant for the `ModelCheck` seam.**
- **Detector coverage: 11 of 15 implemented — QUALIFIED: 8 can move an offline decision; 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so.** Done: LG-001/002/003/004/005/006/008/009/010/014/015. Remaining 4 all ride the model layer: **LG-007 (Layer M), LG-011 (D+M), LG-013 (D+M, warning-tier), LG-012 (D+M, external — last, A-S7)**. There are no remaining pure-D checks.
- **clean-min demonstrates the `ready_with_warnings` ceiling** (flipped in S3): a repo-present-but-unverified external finding yields `ready_with_warnings`, and no fixture yields unqualified `ready`.
- **THE DELEGATED-SURFACE SEAM EXISTS** (A-S2, `src/scan/surface.ts`), consumed by `lg006.ts`, `lg005.ts` and now `lg009.ts`. `surfaceDelegatedCandidates(fileset, opts)` returns `{ excerpts, elided, elidedByBand }`. **Four load-bearing properties:**
  1. **`selects` and `anchor` are TWO parameters, not one.** File selection may be conjunctive; the line-level window anchor is disjunctive. Collapsing them silently widens file selection — pinned by a test.
  2. **`prefers` is an OPTIONAL content-derived ranking, now ORDERED BANDS (A-S3)**, filling the 5-file cap band by band, **preserving path order within each band** so AT-23 byte-identity holds. **It RE-WEIGHTS; it does not cure** — see residue.
  3. **`isModelSurfaceableFile` is DISTINCT from `isCodeFile`** — it is `isCodeFile` ∪ `{.mts,.cts}` and is consumed **only** by the D→M surface and the fact gate. `isCodeFile` was deliberately **not** widened, because widening it would also widen `isWebhookHandler` and change LG-003/LG-004 inputs — and Next.js App Router resolves `route.ts|tsx|js|jsx` only, so `isWebhookHandler`'s narrower set is **correct as it stands**.
  4. **The fact gate is a PROSE ALLOWLIST, not a code denylist.** `fact:lg006.no-code-cancellation-signal` fires **iff** no surfaceable-source file carries the signal **and** every non-surfaceable carrier matches `PROSE_EXT_RE` (`.md|.mdx|.markdown|.txt|.rst`). A pure narrowing, and it **fails safe on omission**. **`PROSE_EXT_RE` is therefore adjudication-gated.**
- **`--checks` SUPPRESSES THE MODEL CALL** (A-S2; **made structural in A-S3**) — `scanWithModel` makes **zero** `infer` calls for a check excluded by `--checks`, asserted per check id for LG-005, LG-006 and LG-009 individually. **This is a security property, not a convenience.**
- **LG-005 IS THE PRODUCT'S FIRST PURE-MODEL BLOCKER** (A-S2): **no deterministic disjunct, no `establishesVerdict` fact**, no `externalVerification`. No handler → `not_applicable`; handler + no judgment → `unknown`; handler + judgment → the D→M contract → `inferred` fail/pass **capped 0.9, never `confirmed`**. **Do NOT invent an `isLg005SettledDeterministically`.** **AT-05 is consequently NOT closed.**
- **HASH TABLES MUST PUBLISH BOTH THE ROOT TOKEN AND THE CLOCK** (A-S2). The complete convention: **sha256 of `serializeReport(report)` with `report.repo.root` replaced by the literal `<ROOT>`, first 16 hex, at fixed clock `2026-07-23T00:00:00.000Z`**. Byte length is **clock-invariant**; the digest is **not**. **The authoritative 11-row baseline is now the A-S3 receipt's table**; A-S2's is superseded (all 11 moved at A-S3 Commit 2, each gaining one LG-009 `not_applicable`, including `unsupported`).
- **`--amend` IS UNSAFE ON THIS BRANCH** (A-S2, standing practice): other agents commit concurrently. **Any amend must first verify `HEAD` is the intended commit** — done before each of A-S3 Commit 2's four amends. Adopted practice: **hold all `build-os/` commits until the packet closes.**
- **CEILING INVARIANT (load-bearing) — STRUCTURAL:** `hasAppSignal ⊇ scanner.supported` — every supported repo emits an LG-015 finding carrying externalVerification, so decision Rule 7 (`ready`) is unreachable. `supported = hasNextDep || hasAppRouter || hasPages` and `hasAppSignal = supported's disjuncts || vercel.json || metadataBase`, therefore **`supported ⇒ hasAppSignal`** and LG-015 can never be `not_applicable` on a supported stack; Rule 6 always pre-empts Rule 7. All **32** combinations of the five structural signals were enumerated as real directory scans: **0 unqualified `ready`, 0 ceiling violations**. **A-S1b's "incidental" caveat is WITHDRAWN** (correction lives in `build-os/receipts/A-S1c.md`). Any slice that broadens `supported` or narrows LG-015's gate must preserve the superset. **A-S3 made the invariant load-bearing in a NEW way: it is what made LG-009's pass-branch withdrawal free** — `pass` and `unknown` produce the identical decision and exit code.
- Fixtures on branch: inert `fixtures/unsupported/` (S1) + `fixtures/broken-lg-00{1,2,3,4,6,8}/`, `fixtures/broken-lg-01{0,4,5}/`, and `fixtures/clean-min/`— all `private: true`, no scripts, excluded by vitest/tsconfig. **A-S1c, A-S2 and A-S3 all moved NO fixture** — the `fixtures/` tree object is `a981446bac76039147d93efedd14a092c2aeadc1` throughout. `eval` runs over these 11 present fixtures; §9.3's 15/15-recall and 18/18-decision thresholds remain a program-level gate owed as golden/, hostile/, and the model-layer broken fixtures land. **No `broken-lg-009` exists and none can be built while LG-009 has no offline verdict.**
- **TEST-DATA POLICY in force:** fake provider-key literals in fixtures/tests/receipts/memory must keep short suffixes (<20 contiguous alphanumerics) AND avoid any Sentry-DSN shape, so GitHub secret scanning does not block pushes; never allowlist a secret. The model layer sends the model ONLY already-redacted excerpts wrapped as delimited untrusted data (SEC-5); no CLI or model surface emits a raw fake key.
- **SEC-5 TRUSTED-REGION DISCIPLINE** (A-S3, DC-12): `realClient.ts:37-44` sends `question`, `responseSchema` and `supportingFacts` as **trusted top-level fields** and only `excerpts` through `buildUntrustedDataEnvelope`. LG-009 was the first check to want repository-derived identifiers in that region. Identifiers are validated `^[A-Za-z_][A-Za-z0-9_]{0,62}$`, deduped, sorted, capped at 10; failures dropped and counted — proven with a **hostile-identifier test**. **No repository-derived free text reaches `question` or `SupportingFact.statement`.**
- AT-17 closed; AT-01/02/04 for LG-001/002/004; AT-03/08/15 for LG-003/008/015; AT-10/14 for LG-010/014; AT-24 closed (P-004); **AT-27 CLOSED (P-005)**, and A-S3 made its defect class structurally harder (`createScanner` and `scanWithModel` iterate the same array); **AT-06 DETERMINISTIC HALF CLOSED via eval on `broken-lg-006` (A-S1b) — the model-assisted half stays proven only by the `FakeModelClient` integration tests**; AT-23/25/26 reinforced end-to-end; AT-20/AT-22 mechanisms unit-proven (hostile-fixture closure deferred); AT-21 SEC-5 mechanism unit-proven (hostile fixture deferred); AT-16 (golden) still deferred. **AT-05 OWED. AT-09 OWED, with NO EVAL PATH to closure.**

---
_Updated by the archivist on close of **T-002** (2026-07-27). Prior updates: close
of T-001 (2026-07-26); A-S3 (2026-07-26); A-S2 (2026-07-25); A-S1c (2026-07-25); A-S1b (2026-07-25); P-005 (2026-07-23);
project section, repo identity, and Build OS location were reconciled on the
2026-07-25 handoff migration (memory relocated from ClaudeOrchestrator into this
product repo; branch `claude/launchgraph-product-scope-43pgdx` →
`claude/sugarbee-project-handoff-ic47uc` at the identical tip), which advanced no
packet state._
