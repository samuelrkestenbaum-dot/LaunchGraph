# Current State

> The "where are we" snapshot. The orchestrator reads this first every session.
> The archivist advances it when a packet closes. Keep it short and true.

## Project

- **What this repo is:** **SugarBee.ai** — a production-readiness auditor for AI-built SaaS. The shipping wedge (Phase 1) is a narrow, structurally read-only static auditor for TypeScript/Next.js repos: 15 evidence-backed checks producing a Ready / Ready with warnings / Not ready decision. Never executes the scanned repo, reads no provider credentials, instantiates no provider SDKs; the only permitted network egress is SugarBee's own model API. Founding scope: `PRODUCT_SCOPE.md`.
- **Primary branch / base:** `claude/sugarbee-project-handoff-ic47uc` (the designated working branch; supersedes the retired `claude/launchgraph-product-scope-43pgdx`, which it continues at the identical tip `a00d194`).
- **Build/test command:** `npm test` (Vitest) · `npm run typecheck` (tsc --noEmit) · `npm run eval` (fixture harness). CLI: `npx tsx bin/sugarbee.ts scan [path] | eval`.

## Build OS location (IMPORTANT — changed 2026-07-25)

This project's Build OS memory now lives **HERE, in the product repo**
(`build-os/memory/`, `build-os/packets/`, `build-os/receipts/`), installed via
canonical `install-project.sh`. It previously lived in the ClaudeOrchestrator
control repo, where SugarBee's `P-001…P-005` receipts **collided by filename**
with the framework's own `P-001…P-022` development packets. That collision is
now structurally impossible: **ClaudeOrchestrator is the framework only**
(canonical tip `7ef50e8`); SugarBee's project state is the product repo's own.
Receipt ids `P-001…P-005` are preserved unchanged. `build-os/memory/tool_router.md`
is canonical's (no SugarBee-specific rows existed in the old copy).

## Where we are

- **Last closed packet:** **A-S1c** — Phase 1 Repository Auditor — **the D→M surfacing contract**: repo-scoped excerpt surfacing + deterministic-layer-first ordering + honest contradiction handling (Fork Branch A, Slice 1c). Three coupled changes landed: (1) **a `contradictory` classification now forces `outcome: 'fail'`** in `runInferenceContract` (spec §4.2 rule 4 — "the fact wins"), so engine rule 5 actually fires instead of the finding rendering as a plain pass; (2) **the deterministic disjunct is hoisted above the judgment** and `scanWithModel` **skips `model.infer` entirely when the check is settled** — closing the §4.3 layer-assignment violation; (3) **`request.excerpts` now follows the question's scope, not the handler file** — handlers plus ≤5 path-sorted **code** files carrying a cancellation signal, windowed −10/+30, with the elision disclosed in `request.question`; `fact:lg006.no-cancellation-branch` is retired for **`fact:lg006.no-code-cancellation-signal`**. Engine + schema still byte-identical (emit-only). qa **GREEN**, reviewer **pass** (two must-fix-before-close items fixed in-packet via amend). Commits `f3a981f` + `7908d5d`; branch tip **`7908d5d`**, pushed as fast-forward `e8f6f99..7908d5d`.
- **Detector coverage is STILL 9 of 15 — a SECOND CONSECUTIVE PACKET WITH NO NEW DETECTOR.** The reason: the D→M surfacing contract had to be *right* before LG-005 and the other four D+M checks build on it. A-S1b hardened LG-006 and extracted the locator; A-S1c made the contract honest. Both were correct not to add a detector; a third such packet would need a real argument.
- **Numbers:** `npm test` **233 / 25 → 253 / 25**; `npm run typecheck` clean; `npm run eval` unchanged at **11 fixtures / 11-of-11 decisions / 0 blocker false positives**. Zero fixture movement (`fixtures/` tree object identical at both ends).
- **Now:** none active.
- **Next:** the FORK's **Branch A continues at A-S2 — LG-005 (non-idempotent webhook, Layer D+M)** — a staged candidate awaiting orchestrator shaping + explicit go. A-S1b handed it the shared locator seam; A-S1c hands it the D→M contract. **But A-S2 must NOT assume that contract is fully settled** — see residue items on `.mts`/`.cts`, the false-fact framing, and the two extraction seams. **LG-005 will hit the IDENTICAL false-fact trap** if it attaches a fail-establishing fact gated on `isCodeFile`.

## Stable facts (slow-changing)

- SugarBee.ai product repo is GitHub **`samuelrkestenbaum-dot/LaunchGraph`** (the repo slug still says LaunchGraph — the brand rename to SugarBee.ai is not renamable from a session); local checkout **`/home/user/LaunchGraph`**. Control repo (Build OS framework only): `samuelrkestenbaum-dot/ClaudeOrchestrator` at `/home/user/ClaudeOrchestrator`.
- **Check ids keep the `LG-` prefix** — spec-declared "stable, versioned" (~414 occurrences); moving them to `SB-` is a deliberate, separate packet if ever requested.
- SugarBee.ai's founding scope is PRODUCT_SCOPE.md (root commit c7267e8).
- SugarBee.ai's first buildable target is the Part II production-readiness layer.
- Phase 1 spec lives at `specs/phase-1-repository-auditor.md` in THIS repo (commit 5621aa9). NOTE: the spec header still says "not yet implemented" — stale now that implementation is underway; reconcile in a later doc pass (tracked in residue).
- Phase 1 decision ceiling is `ready_with_warnings` until Phase 3+.
- **The MODEL-LAYER SUBSTRATE now EXISTS** (P-005, branch tip `fb5663b`): an injected `ModelClient` (interface + `InferenceRequest`/`InferenceResult` + the `buildUntrustedDataEnvelope` SEC-5 wrapper) + `FakeModelClient` (drives ALL tests, no real network) + `RealModelClient` (bin-only, built-in global `fetch`, no SDK) + a pure D→M inference contract (`runInferenceContract`/`InferencePresentation`: cite-or-discard, cap 0.9, `inferred`/`contradictory`, builds Findings via `makeFinding`). **EMIT-ONLY — `engine.ts` + `schema/index.ts` are byte-identical to base (blob SHAs unchanged); the model only emits the right classification/confidence and the existing engine Rules 3/4/5/6 + schema do the rest.** Model results are at best `inferred`, capped 0.9, never `confirmed`; `contradictory` when the model verdict conflicts with a deterministic supporting fact; uncited claims discarded. **The credential boundary is the bin only** — `RealModelClient` reads no `process.env`; config is injected at `bin/sugarbee.ts` (spec §10's one permitted egress, SugarBee.ai's OWN model API — NOT provider credentials, SEC-3 intact). `--offline` / model-unconfigured → the model-dependent check emits `unknown` ("model layer disabled"), **closing AT-27's owed half**; the report stays valid + deterministic. Zero new RUNTIME deps.
- **The `sugarbee` scan/eval CLI EXISTS** (P-004, extended in P-005): `run(argv, io): number` — pure, synchronous, returns the exit code; all effects flow through an injected `Io {stdout, stderr, now, cwd, writeFile}`; the bin runs via `tsx` (DEV-only dep). Deterministic exit codes: `ready`/`ready_with_warnings` → 0, `not_ready` → 1, scan/parse error → 2, `not_evaluated` → 3 (unsupported stack → exit 3, closing AT-24 with ZERO human-surface findings). `--json` → `serializeReport` to stdout only, zero files; default → `report.json` + `report.md` + a human banner, writes CONFINED to `--out`. Flags: `scan [path]`, `--json`, `--out`, `--offline`, `--checks`, `--app`; plus `eval`. P-005 additively extended `run.ts` (shared `emitReport` + an async overload; the sync two-arg path unchanged) and the bin (constructs `RealModelClient` from bin-read config; `--offline`/no-model → sync path); `scanner.ts` gained a sync offline `unknown`-emit for LG-006 + a new async `scanWithModel` (the sync two-arg path byte-identical).
- SugarBee.ai now has the S1 proof surface + scan substrate + **NINE working detectors** — **eight deterministic (LG-001/002/003/004/008/010/014/015) + the first Layer D+M (LG-006)** — all wired into the runnable CLI (branch tip `7908d5d`). Source: S1 surface (`src/schema`, `src/checks/registry`, `src/decision/engine`, `src/report/serialize`, `src/eval/harness`) + scan substrate (`src/scan/{collect,redact,scanner}.ts`) + detectors `src/checks/lg00{1,2,3,4,6,8}.ts`, `lg010.ts`, `lg014.ts`, `lg015.ts` + the model layer `src/model/{client,fakeClient,realClient,inference}.ts` + the shared webhook locator `src/scan/webhook.ts` (A-S1b) + CLI (`src/cli/{args,io,run,reportMd,banner}.ts`, `bin/sugarbee.ts`). **253 tests / 25 files** (`npm test`); `npm run eval` → **11 fixtures, 11/11 decisions correct, 0 blocker FPs**. **Zero runtime deps** (tsx is dev-only; `RealModelClient` uses built-in `fetch`, no SDK). Build/test commands `npm test` + `npm run typecheck` in the SugarBee.ai checkout.
- **THE SHARED WEBHOOK LOCATOR EXISTS** (A-S1b, `src/scan/webhook.ts`): one path predicate + `locateWebhookHandlers(fileset)` returning **ALL** matching handlers in deterministic order. **One locator = one contract: all-handlers (`.filter()`) semantics**, adopted by LG-006, already shipped by LG-004. Determinism rests on `collect.ts:270` sorting collected files by path. **LG-005 (A-S2) is its intended third consumer.** NOTE: `lg003.ts:51` still carries a **third byte-identical copy** of the predicate — not yet migrated (residue). **A-S1c added a second shared predicate, `isCodeFile` (exported from `src/scan/collect.ts`), consumed by `webhook.ts` + `lg006.ts`** — but unification is **only at the sites A-S1c touched**: `lg003.ts:35`, `lg015.ts:39`, and `lg010.ts:62` still carry private `CODE_EXT_RE` copies. Do not describe `isCodeFile` as the single source of truth.
- **LG-006 IS NOW A GENUINE D+M DISJUNCTION** (A-S1b): **no cancellation signal anywhere in the repo → Layer D → `fail`/`confirmed`/1.0/blocker, standing under `--offline`**; branch present but downgrade unproven → Layer M → `inferred` (cap 0.9) online / `unknown` ("model layer disabled") offline; no webhook handler → `not_applicable`. The blocker gate is **repo-wide (`hasCancellationSignalAnywhere`)**, not handler-file-scoped — that is what stops it manufacturing a false blocker on a correct handler that *delegates* the branch to another module. **Two load-bearing consequences:** (a) `collect.ts:47`'s **`node_modules` exclusion is load-bearing** — the `stripe` package's own typings name `customer.subscription.deleted`, so a future "scan vendored dependencies" mode would silently kill this blocker on every Stripe repo; (b) the repo-wide scan reads raw content of ALL collected files, so a README / agent spec / commented-out branch / SQL comment merely *naming* the event suppresses the blocker to `unknown` — direction-safe (under-warn only) but wider in SugarBee's AI-built-SaaS target population. **RESOLVED by A-S1c — the online path no longer substitutes model judgment; see the D→M contract fact below.**
- **THE D→M SURFACING CONTRACT EXISTS** (A-S1c, `src/model/inference.ts` + `src/checks/lg006.ts` + `src/scan/scanner.ts` + additive `isCodeFile` in `src/scan/collect.ts`). Three stable rules now bind every Layer D+M check:
  1. **The surface follows the QUESTION's scope, not the file.** `request.excerpts` = every located handler (whole file) **plus** up to **5** additional **code** files elsewhere in the repo carrying the signal, path-sorted, windowed **−10/+30** around the first match, with any elision disclosed **in `request.question`** (NOT in `Evidence.note` — `buildUntrustedDataEnvelope` does not transmit that field). **Only code files may ever be surfaced** — SEC-5 blast radius: zero non-code excerpts reach a prompt.
  2. **`contradictory` forces `outcome: 'fail'`.** Spec §4.2 rule 4 — "the fact wins": the deterministic fact carries `establishesVerdict: 'fail'`, so `fail` IS the fact-wins outcome and `contradictory` is the classification recording "needs human review." Engine rule 5 then fires → `requires_confirmation`, counted as a warning. A contradictory blocker-capable `fail` can **never** manufacture `not_ready`. Choosing `unknown` instead would make rule 5 dead code for every model-derived finding.
  3. **The deterministic layer settles BEFORE the model is consulted at all.** The deterministic disjunct is evaluated first; when the check is settled, `scanWithModel` **skips `model.infer` entirely** (provable: zero `infer` calls). LG-006's layer assignment no longer varies with model configuration — §4.3 satisfied.
  **`fact:lg006.no-cancellation-branch` is RETIRED** (handler-scoped, and simply false on delegating repos) in favour of **`fact:lg006.no-code-cancellation-signal`** (`establishesVerdict: 'fail'`), emitted when no *code* file in the repo carries a cancellation signal. **NOT fully closed:** `isCodeFile` is `/\.(?:ts|tsx|js|jsx|mjs|cjs)$/` — no `mts`/`cts` — so a repo whose handling lives in a `.mts`/`.cts`/`.sql`/`.prisma` file still gets a **false fail-establishing fact plus a withheld disproving file**, and the likely model-agreement branch yields **`not_ready` on a correct repository**. See residue.
- **The WARNING TIER is proven in code** (P-003-S4): LG-010/LG-014 are Warning-severity, so a repo-side problem is outcome `fail` + severity `warning` (severity from the registry via `makeFinding`, never hardcoded, never outcome `warning`, never blocker). The engine routes fail+warning to warnings, so a fixture seeding only such a problem yields `ready_with_warnings` (exit 0), NOT `not_ready`. LG-001/002/003/004/008/015 remain the blocker tier. Still no fixture yields unqualified `ready`.
- **externalVerification obligation is proven in code** for LG-003 (`stripe`), LG-010 (`resend`), LG-014 (`sentry`), and LG-015 (`dns`); LG-008 carries it only on its partial/external branch (`supabase`), never on its confirmed-fail branch. The substrate now also supports it on model-derived findings via `InferencePresentation.externalVerification`. **Among external checks only LG-012 remains pending — it is Layer D+M and lands LAST (A-S7).**
- **Detector coverage: STILL 9 of 15 done — for a SECOND CONSECUTIVE PACKET** (A-S1b added no detector; A-S1c added no detector either — it landed the D→M surfacing contract that LG-005 and the other four D+M checks all inherit). The remaining 6 all ride the now-existing model layer: LG-005/009/011/013 (Layer D+M), LG-007 (Layer M), LG-012 (Layer D+M, external — last, A-S7). There are no remaining pure-D checks.
- **clean-min demonstrates the `ready_with_warnings` ceiling** (flipped in S3): a repo-present-but-unverified external finding yields `ready_with_warnings`, and no fixture yields unqualified `ready`.
- **CEILING INVARIANT (load-bearing) — NOW PROVEN STRUCTURAL, correcting A-S1b's receipt wording:** `hasAppSignal ⊇ scanner.supported` — every supported repo emits an LG-015 finding carrying externalVerification, so decision Rule 7 (`ready`) is unreachable. **A-S1c's qa proved this holds BY CONSTRUCTION, not incidentally:** `supported = hasNextDep || hasAppRouter || hasPages` and `hasAppSignal = supported's disjuncts || vercel.json || metadataBase`, therefore **`supported ⇒ hasAppSignal`** and LG-015 can never be `not_applicable` on a supported stack; Rule 6 always pre-empts Rule 7. **A-S1b's receipt recorded qa's caveat that the guard was "incidental" — that caveat is now WITHDRAWN and corrected here** (receipts are append-only; `A-S1b.md` is not edited — see `build-os/receipts/A-S1c.md`, Corrections #2). qa additionally enumerated **all 32 combinations** of the five structural signals as real directory scans: **0 unqualified `ready`, 0 ceiling violations**. Any slice that broadens `supported` or narrows LG-015's gate must preserve the superset.
- Fixtures on branch: inert `fixtures/unsupported/` (S1) + `fixtures/broken-lg-00{1,2,3,4,8}/`, `fixtures/broken-lg-01{0,4,5}/`, and `fixtures/clean-min/` — all `private: true`, no scripts, excluded by vitest/tsconfig. **A-S1b added `fixtures/broken-lg-006/` (11th fixture, `not_ready`, exit 1) and DELIBERATELY REPAIRED `fixtures/broken-lg-004/`** — a disclosed rebaseline: that fixture carried a real latent LG-006 defect (no cancellation branch at all), so a `customer.subscription.deleted` branch reaching an entitlement downgrade was added; LG-004 still fails and still cites `webhook/route.ts`. **A-S1c moved NO fixture** — the `fixtures/` tree object is byte-identical at `e8f6f99` and `7908d5d`. `eval` runs over these 11 present fixtures; §9.3's 15/15-recall and 18/18-decision thresholds remain a program-level gate owed as golden/, hostile/, and the model-layer broken fixtures (incl. `broken-lg-006`) land.
- **TEST-DATA POLICY in force:** fake provider-key literals in fixtures/tests/receipts/memory must keep short suffixes (<20 contiguous alphanumerics) AND avoid any Sentry-DSN shape, so GitHub secret scanning does not block pushes; never allowlist a secret. P-005's model layer sends the model ONLY already-redacted excerpts wrapped as delimited untrusted data (SEC-5); no CLI or model surface emits a raw fake key.
- AT-17 closed; AT-01/02/04 for LG-001/002/004; AT-03/08/15 for LG-003/008/015; AT-10/14 for LG-010/014; AT-24 closed (P-004); **AT-27 CLOSED (P-005: `--offline`/model-unconfigured → LG-006 `unknown`, deterministic checks unaffected, byte-identical fixed-clock offline runs)**; **AT-06 DETERMINISTIC HALF CLOSED via eval on `broken-lg-006` (A-S1b) — the model-assisted half stays proven only by the `FakeModelClient` integration tests; AT-06 is NOT closed end-to-end by one fixture**; AT-23/25/26 reinforced end-to-end (A-S1b re-proved AT-23 byte-identity on `broken-lg-006` and `broken-lg-004`); AT-20/AT-22 mechanisms unit-proven (hostile-fixture closure deferred); AT-21 SEC-5 mechanism unit-proven (hostile fixture deferred); AT-16 (golden) still deferred.

---
_Updated by the archivist on close of **A-S1c** (2026-07-25). Prior updates: close
of A-S1b (2026-07-25); close
of P-005 (2026-07-23); project section, repo identity, and Build OS location were
reconciled on the 2026-07-25 handoff migration (memory relocated from
ClaudeOrchestrator into this product repo; branch
`claude/launchgraph-product-scope-43pgdx` → `claude/sugarbee-project-handoff-ic47uc`
at the identical tip), which advanced no packet state._
