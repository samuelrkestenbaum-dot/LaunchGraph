# Residue

> What was deferred, left behind, or noted as risk — the stuff that didn't fit in
> the last packet but must not be forgotten. The orchestrator reads this to avoid
> dropping threads; the archivist appends/clears it on close.

## TOP ITEM — surface this first

- **(EV-001) TRAJECTORY — THIRD CONSECUTIVE PACKET WITH ZERO DETECTOR MOVEMENT. THE NEXT PACKET SHOULD BE CODE. NEW TOP ITEM.**
  - **T-001, T-002, EV-001 all shipped zero detector change**, against **four unimplemented detectors (LG-007, LG-011, LG-012, LG-013)** and **two named open product defects**.
  - **Doctrine is now well-capitalized relative to code.** Each of the three packets was individually justified. **The PATTERN is the risk, not any one packet.**
  - **Leading candidate: LG-005's delegated-helper blind spot — `src/checks/lg005.ts:203`, EV-001 §7.1.** A **shipped, repository-authoritative** detector that can **currently manufacture a false blocker**, and **no provider and no probe can fix it** — it sits squarely inside Phase 1's own authority.
  - **Hardening it is a better use of the same effort than a twelfth detector, and it HONOURS the A-S4 pause rather than working around it.**

- **(EV-001) THE CODEX `lg005.ts:205` CITATION IS OFF BY TWO. THE CORRECT SITE IS `src/checks/lg005.ts:203`. FIX THE LINE NUMBER BEFORE ACTING ON THE DEFECT.**
  - Verified at source (tree `258cc70`): **`:203` is `selects:` (the EXCLUDING predicate)**, `:204` is `anchor:`, **`:205` is `prefers:` — a RANKING predicate that excludes nothing.**
  - **The defect is REAL; only the line number was wrong.** The exclusion the Codex finding describes happens at **`selects`**, not at `prefers`.
  - **PROCESS NOTE, AND IT IS THE DURABLE PART: qa flagged this (DC-13 RED) and the reviewer INDEPENDENTLY ASSERTED THE OPPOSITE — that `:205` IS `selects`. Two independent reviews disagreed on a ONE-LINE FACT, and only DIRECT VERIFICATION OF THE SOURCE settled it. Neither report's confidence was evidence.**
  - The wrong citation is carried in this file and in `current_state.md` from T-002 (inherited from Codex). **Receipts are append-only — `T-002.md` is NOT edited; the correction lives in `build-os/receipts/EV-001.md` and here.**

- **(EV-001) NO SECOND EYES ON EV-001. DO NOT LET T-002'S "SECOND EYES OBTAINED" BE READ AS COVERING IT.**
  - `codex` is **absent from PATH**; **no plugin route exists**; the only live Codex channel is the **GitHub PR bot, which requires a push** — and **nothing was pushed**.
  - **STANDING QUALIFIER, from the reviewer's own caveat, and it now travels with every doctrine packet: PROSE REVIEW IS WEAKER EVIDENCE THAN CODE REVIEW.** There is **no test that fails if a sentence is misread**, and the failure mode is **a reading three sessions from now, under delivery pressure.** That is why EV-001's four reviewer findings were treated as required fixes, not style notes.

- **(T-002) THE PIN FAILS OPEN IN BOTH DIRECTIONS — NEW TOP ITEM. READ THIS BEFORE MOVING, RE-CLONING, OR RELOCATING THIS REPO.**
  - The mitigation applied at `c7baff9` is `env.BUILD_OS_SOURCE = "/home/user/LaunchGraph"` — **an ABSOLUTE PATH**. Reviewer-verified, both directions:
  - **(a) PATH GONE.** If `/home/user/LaunchGraph` no longer exists, `is_canonical_src()` fails and `resolve_source()` (`project-bootstrap.sh:63-76`) **falls through** to `/home/user/ClaudeOrchestrator`, `$HOME/ClaudeOrchestrator`, **or `$(dirname TARGET)/ClaudeOrchestrator`**. The overwrite returns **SILENTLY, with the key still present and LOOKING APPLIED.**
  - **(b) RE-CHECKOUT AT A NEW PATH — WORSE THAN THE ORIGINAL HAZARD.** If the repo is checked out at a **new** path while the **old one still exists**, `SRC` = the **stale old checkout** and `TARGET` = the new path, so the bootstrap **installs into the new checkout FROM the abandoned one**, reporting LaunchGraph's own HEAD as "canonical". **The abandoned checkout becomes a de-facto source repo.**
  - **It fails SAFE on a clean machine** (no ClaudeOrchestrator anywhere → the provisional `SRC` is the repo itself → no-op) **and fails OPEN on this one.**
  - **ACTION: include a one-line re-check** (`bash build-os/tools/project-bootstrap.sh --target "$PWD"` must print `nothing to install`) in any session that moves or clones this repo.

- **(T-002) `~/build-os/.canonical-source` IS NOT A VERSION PIN — DO NOT "APPLY THE STAMP" AND BELIEVE THIS CLOSED.**
  - `resolve_source()` order is `$BUILD_OS_SOURCE` → stamp → hard-coded paths → mirror. But **`canonical_sha()` reads `git rev-parse HEAD` LIVE from the resolved path**; `STAMPED_SHA` is used **ONLY** as a fallback when the resolved source has no git history.
  - **THE STAMP PINS THE SOURCE PATH, NEVER THE SOURCE CONTENT.** T-001's residue named it as the mitigation. **That was wrong.**
  - **A FROZEN CANONICAL CLONE ALSO DOES NOT WORK** — it still satisfies `is_canonical_src()`, so `SRC != TARGET` and the full stage→backup→promote path still runs. **Freezing pins WHICH bytes get written, never WHETHER writes happen.** Arm (b) of the three-arm control: **canary REVERTED.**
  - **The ONLY working upgrade form** is `BUILD_OS_SOURCE=/home/user/ClaudeOrchestrator bash build-os/tools/project-bootstrap.sh --target /home/user/LaunchGraph --force`. **`c7baff9`'s commit message says to run the bootstrap from a ClaudeOrchestrator checkout with `--target`; that is WRONG** — `resolve_source():65` consults `$BUILD_OS_SOURCE` **first, regardless of which copy of the script executes**, so it still prints `nothing to install`. Commit messages are immutable history; the correction lives in `build-os/receipts/T-002.md`.

- **(T-001) THE SELF-UPDATING HOOK — NEW TOP ITEM, AND IT IS A STANDING CONSTRAINT ON EVERY FUTURE PACKET.**
  - `.claude/hooks/session-start-build-os.sh:18-31` now runs `project-bootstrap.sh --target "$ROOT"` on **EVERY SessionStart**, with **no `--dry-run` and no `--verify`** — **a live write**, not a check.
  - **The vendored copy does NOT shield this repo:** `resolve_source()` **hard-codes `/home/user/ClaudeOrchestrator` first**.
  - **Steady state is quiet.** The cache gate requires a canonical SHA match **AND** a sha256 match on **all 20 managed files**. **On a cache MISS it overwrites all 20 managed files plus `.claude/settings.json` and the `CLAUDE.md` managed block.**
  - **qa DEMONSTRATED the failure, it is not theoretical:** in a throwaway repo, a simulated drift **silently reverted a local edit to `build-os/memory/tool_router.md` to canonical content.**
  - **THE TRIGGER IS ENTIRELY OUTSIDE THIS REPO** — anyone committing to, or even `git checkout`-ing in, `/home/user/ClaudeOrchestrator`, which currently holds **two branches with DISJOINT HISTORIES** (`claude/sugarbee-project-handoff-tdfylf` `0a63f66` and `claude/add-build-os`, **empty merge-base**).
  - **It does not commit**, so it does not literally breach "hold all `build-os/` commits until the packet closes" — **but a builder running `git add build-os` or `git commit -a` mid-packet sweeps it in**, which is **the same collision class that previously required a reflog repair** (see `--amend` is unsafe, Standing constraints).
  - **MITIGATION (a) APPLIED AT T-002 (`c7baff9`)** — `env.BUILD_OS_SOURCE = "/home/user/LaunchGraph"`: `SRC == TARGET`, `nothing to install`, a **TRUE ZERO-WRITE no-op** (qa proved zero-write by leaving a tampered `source_sha` of 40 zeros in place afterward — an overwrite would have restored it), and it **survives `--force`**. **Keep habit (b) anyway: run `git status` for managed-path churn before any `build-os` commit.**
  - **CORRECTION 1 (T-002) — THE HAZARD IS SELF-TRIGGERING. "THE TRIGGER IS ENTIRELY OUTSIDE THIS REPO" IS FALSE.** The reviewer's control **did not tamper the manifest**: **a canary edit to a managed file ALONE flips the file-hash half of the cache gate** (`project-bootstrap.sh:196-215`). **No external ClaudeOrchestrator commit is needed to fire it.** A clone-pin would have frozen only the `source_sha` half and left the **file-hash half fully live** — which is exactly why the pin points at the project and not at a clone.
  - **CORRECTION 2 (T-002) — "TWO BRANCHES WITH DISJOINT HISTORIES" IS A STALE LOCAL REF.** ClaudeOrchestrator's remote has **SIX** branches: `claude/add-build-os` @ `0a63f66` (its default), `claude/launchgraph-product-scope-43pgdx` @ `ef8a75c` (the G5 target), `claude/orchestrator-tools-list-e0mdaz` @ `0a63f66`, `claude/project-handoff-merge-ramhds` @ `d3d8305`, `claude/repo-verification-cleanup-m9cfsm` @ `0a63f66`, `claude/song-lyrics-docx-ksh0qr` @ `33629b9` (unrelated content on the framework repo). `origin/claude/add-build-os` reads `0f831fd` **locally** and `0a63f66` **on the remote**. Also: **`claude/sugarbee-project-handoff-ic47uc @ 7ef50e8` on ClaudeOrchestrator, named in HANDOFF §5, IS GONE — that caveat is STALE.**
  - **TEST-HYGIENE NEAR-MISS, RECORDED (T-002 reviewer R5):** during the packet an **uncommitted manifest with a zeroed `source_sha`** was briefly present (control residue). **A zeroed `source_sha` guarantees a permanent cache miss**, so committing it would have **PRE-ARMED the exact overwrite the pin defends against.** Restored; tree clean at close. Also: an **unpinned `--force` run rewrote the manifest timestamp and had to be reverted** — precisely the **spurious-diff effect T-001 predicted**.

- **(T-001) MANAGED vs PRESERVED — NEW, LOAD-BEARING, AND IT CHANGES WHERE PROJECT KNOWLEDGE MAY BE WRITTEN.**
  - The manifest (`build-os/.install-manifest.json`) lists **20 MANAGED files**. **`build-os/memory/tool_router.md` AND `build-os/memory/skill_budget.md` ARE NOW MANAGED.**
  - **PRESERVED is ONLY:** `memory/current_state.md`, `memory/residue.md`, `packets/active_packet.md`, `receipts/README.md`.
  - **THEREFORE: any SugarBee-specific routing knowledge written into `tool_router.md` will be SILENTLY REVERTED by the next bootstrap.** **Project-specific routing facts must live in `current_state.md` / `residue.md` instead.**
  - **The router has NO ROW for framework/tooling adoption.** T-001 routed from the **Build** row with reviewer dropped. **The gap is real** — a tooling packet is neither a build nor a design packet, and the router does not say so. **STILL OPEN AT T-002**, and because `tool_router.md` IS MANAGED, **a local row added to it is reverted.**
  - **(T-002) NEW — THERE IS AN UNMANAGED HOME FOR THAT ROW.** Canonical documents a **3-tier fallback**: project `build-os/memory/tool_router.md` → **user-scope `~/build-os/memory/tool_router.md`** → embedded lanes. **The user-scope router is UNMANAGED by this repo's bootstrap and therefore NON-REVERTING** — it is the correct home for a `T-` row. **`~/build-os` DOES NOT EXIST on this host** (`/root/build-os`: no such file or directory), so creating it is a deliberate act, not a repair.

- **(A-S3) R-A — THE SURVIVING CARRIER RESIDUAL ON LG-009, AND IT IS FALSITY, NOT SILENCE. NEW TOP ITEM.**
  - `noRlsFound` scans every `.sql`/`.prisma` file, comment-stripped. RLS expressed in **any other carrier** — `.psql`, `.pgsql`, extensionless, or a `.ts`/`.js` migration template string (Drizzle `db.execute(sql\`…\`)`, Knex `knex.raw`) — is **invisible to the scan**. So **on a repo that declares tables in `.sql`/`.prisma` but applies RLS elsewhere, `fact:lg009.no-rls-in-migrations` is emitted and FALSE.** **Narrower than the carrier-scoped defect it replaced, but the SAME CLASS.**
  - **THE FRAMING WAS PROPOSED BACKWARDS AND qa CORRECTED IT.** The main session proposed recording this as *"a silence-only limit, not falsity."* **That is backwards.** Not scanning a file makes `noRlsFound` **more** likely true, so the fact is **more** likely emitted **and false**. **WIDENING THE SCAN SILENCES; NARROWING FALSIFIES.** Carry the corrected framing, not the original.
  - **Bounded:** no `establishesVerdict`, the statement self-hedges, ceiling `inferred` → Rule 3.
  - **Further widening is NOT free.** Scanning all text files would *silence* the fact on any repo whose README merely mentions the phrase, degrading true context. **Where the scan boundary belongs is a design decision, not a patch.** One-line partial close if wanted: add `psql|pgsql` to the scan regex — **monotone in the safe direction**.
  - **This is defeater class 6 recurring WITHIN A SINGLE ROUND, immediately after the class was identified.** That is why the class sits in Standing constraints below.

- **(A-S3) LG-009 SHIPPED WITH BOTH DETERMINISTIC BRANCHES WITHHELD — the record of WHY, because it constrains every future certifying branch.**
  - **No `confirmed` fail, no `confirmed` pass.** Only `not_applicable`, `unknown`, or model-derived `inferred` capped at 0.9.
  - **Fail branch — withheld at shaping time.** Absence of RLS is not evidence of missing isolation; application-layer scoping is legitimate and more common. Five false-blocker routes FB-1..FB-5; **FB-1 (a Prisma `$extends` app) is the modal architecture of the target population.**
  - **Pass branch — withdrawn mid-packet BY USER DECISION** after **seven false-certification routes** across **four adversarial passes**, each pass finding new ones: (1) `USING (true)` beside a strict policy — *fixed, then moot*; (2) line-commented RLS — *fixed, then moot*; (3) block-commented/unterminated/nested — *fixed, then moot*; (4) later `DISABLE ROW LEVEL SECURITY` — *handled by the withdrawn branch*; (5) later `DROP POLICY` — **never fixed, cannot fire**; (6) later `DROP TABLE` + recreate — **never fixed, cannot fire**; (7) `USING (1=1)` tautology — **never fixed, cannot fire**; (8) **RLS DDL inside a string literal, which certified a repo with ZERO RLS** — **never fixed, cannot fire**; (9) partial child-table coverage — *handled by the withdrawn branch*.
  - **THE DECIDING ARGUMENT:** because the Phase-1 ceiling is **structural**, **`pass` and `unknown` produce the identical decision and exit code** — LG-009 `unknown` → Rule 6 → `ready_with_warnings`, and `pass` → LG-015's pending external still forces Rule 6 → `ready_with_warnings`. The branch bought **a line of report text and a skipped model call**, against the risk of certifying tenant isolation on a repo that has none. What settled it was not the length of the list but **that it kept growing**.
  - **Routes 5–8 are unreachable BY ABSENCE OF THE BRANCH, not by patching. That is the right kind of closure.**

- **(A-S2, STILL OPEN) `prefers` RE-WEIGHTS THE DELEGATED SURFACE; IT DOES NOT CURE IT.** *(Was the top item; demoted by A-S3's R-A, not resolved.)* `prefers` can **demote** a guard that path-ordering would have shown. Three residual shapes: **(1) the INVERSION case** — a generic-band guard at `app/_guard.ts` sorts first under path order but is pushed behind five `lib/telemetry-*.ts` files matching `event.id`; **(2) six specific-band files** with the guard sorting last within the band; **(3) a generic-only guard** using just `ON CONFLICT DO NOTHING` behind five specific-band files. **The honest framing is "re-weighted toward the far more common failure," NOT "cured."** What was fixed is the **bias**: path order correlated with layout (`app/**` before `lib/**`) while guards live in `lib/**`, so a binding cap discarded the exonerating file **systematically**; ranking is now content-derived and layout-independent. What remains is **incompleteness WITHIN a band** — ordinary, and irreducible on any capped surface without import resolution (**not** to be revived; A-S1b's reviewer withdrew it after it reproduced the false blocker on path aliases, barrel re-exports, and two-hop delegation). Bounded: disclosed via the elision note, ceilinged at `inferred`, never `confirmed`; strictly narrower than pre-fix. **Do not describe the delegated surface as complete.** **(A-S3) `elidedByBand` now makes the loss LEGIBLE** — DC-11 keys `unknown` on specific-band elision — but legibility is not cure.
  - **How the hazard was found — record the standard.** The reviewer reproduced **`not_ready` on a correct repository** on the **first realistic repo it wrote**. qa reproduced it independently, and **also** found the committed Trap-9 direction check was **vacuous for its own hazard** (a minimal repo where the cap never binds). **Direction checks must exercise the shape whose name they carry.** *(A-S3 applied this: no `broken-lg-009` was added, because an offline fixture cannot exercise a shape LG-009 has no offline path to.)*

- **~~(A-S2) LG-009 IS THE SHARPEST REMAINING RISK — BINDING SHAPING INPUT FOR A-S3.~~ CONSUMED BY A-S3.** The asymmetry held: the SEC-5 bound restricts `request.excerpts`, **not** what the deterministic layer may read, so LG-009's Layer D read `.sql`/`.prisma` directly and completely with **no new capability, no SEC-5 question, and no change** to `isCodeFile`/`isModelSurfaceableFile`/`PROSE_EXT_RE`. **The "must not ship the `confirmed` branch before the reading is settled" bound was honoured — the reading was settled and the answer was to withhold BOTH branches.** What survives is R-A above.
- **~~(A-S2) `prefers` IS BINARY; LG-009 WILL WANT ≥3 TIERS.~~ CLOSED BY A-S3** — `prefers` is now ordered bands, implemented as successive filters (one-band case byte-identical **by construction**), with `elidedByBand` reporting what the cap dropped.

## Standing constraints (bind every future slice)

- **(EV-001) NEW — PHASE NUMBERS ARE CITATIONS, NOT SHORTHAND. `PRODUCT_SCOPE.md` §35 IS THE AUTHORITY.** **`:1598` Phase 1 Repository auditor · `:1614` Phase 2 AGENT COORDINATOR · `:1626` Phase 3 PROVIDER INSPECTOR AND ROUTER · `:1640` Phase 6 VERIFICATION.** **Writing "Phase 2" for provider or live evidence is WRONG** — Phase 2 is the agent coordinator — **and worse than wrong, it implies the numbering is informal, which is exactly the crack the boundary leak comes through.** The Phase 1 spec's §13 already defers `:646` provider API inspection, **`:647` external-side resolution of LG-003/010/012/014/015**, `:648` DNS/TLS/domain probes and `:651` journey execution + unqualified `Ready` to Phase 3 / Phase 6. **Cite the line; do not paraphrase the phase.**
- **(EV-001) NEW — THE EVIDENCE-ALLOCATION DOCUMENT GRANTS NOTHING, AND ITS NORMATIVE SURFACE IS ENUMERATED.** `specs/evidence-allocation-and-phase-boundaries.md` is normative for **exactly two things — evidence-source allocation and phase ownership — PLUS §4 (FORBIDDEN), §5.3 (GATE) and §6 (LG-009 non-deletion), enumerated as additionally normative and framed as RESTRICTIONS ONLY.** **None of it may be read as permission.** `P1§3` remains the sole authority for what a check MEANS. **§6's release valve is CONJUNCTIVE**: reinstating an LG-009 certifying branch needs **all three** R1 requirements — (1) a real SQL statement parser, (2) a default-deny defeater denylist, (3) evidence the connecting role is subject to the policies — and **Phase-3 provider evidence discharges (3) ALONE and is explicitly NOT grounds for reinstatement.** **§5.3's gate does NOT backstop this**, because reinstating the `confirmed` **fail** branch need not make Rule 7 reachable. **There is no per-check `ready`** — `P1§3` fixes per-check outcomes; `ready` is a global Rule 7 decision, and Rule 7 stays blocked while LG-012's Phase-6 proof is outstanding.
- **(T-001) NEW — SURFACE-VERIFIED TOOL INVENTORY (2026-07-26, Claude Code on the web / remote container). THE ROUTER'S CAPABILITY TABLES ARE HOST-SPECIFIC (P-012, a Mac) AND ARE FALSE ON THIS SURFACE.** Live evidence gathered this session:
  - **CONTRADICTED — the router UNDERSTATES:** `tool_router.md` says *"Not live (out of scope until enabled): Stripe & Cloudflare Developer Platform (installed, need auth)"*. `ListConnectors` here reports **both `connected: true, enabledInChat: true`**.
  - **CONTRADICTED — the router OVERSTATES:** the *"Build accelerators (verified 2026-07 — P-012)"* table marks **Serena / Repomix / ccusage / Trail of Bits / Context Mode ACTIVE**. On this surface `serena`, `repomix`, `ccusage` and `codex` are **all absent from PATH**; `claude plugin list` returns *"No plugins installed"*; there is **no `~/.claude/plugins` and no `~/.claude/settings.json`**. **EVERY "ACTIVE" CLAIM IN THAT TABLE IS FALSE HERE.**
  - **`skill_budget.md` IS INAPPLICABLE HERE.** Its premise — **685 skills / 171,824 chars, 5.7× over the 30,000-char budget; disable `ecc@ecc`** — is a **Mac-host condition**. `build-os/tools/skill-budget-audit.sh --claude-dir ~/.claude 30000` on this surface reports **0 installed / 0 enabled plugins**. **Both the problem and its remediation are absent.**
  - **CONFIRMED ACCURATE:** Google Calendar / Google Drive / Microsoft 365 are installed but `enabledInChat: false`.
  - **ACTUALLY LIVE HERE:** **16 claude.ai connectors** (21st, Apollo.io, Clay, Cloudflare, Docusign, Gmail, Higgsfield, HubSpot, Hugging Face, Netlify, Notion, Otter.ai, Slack, Stripe, Supabase, Zapier); **10 enabled plugins** (design, data, productivity, brand-voice, marketing, sales, small-business, legal, cowork-plugin-management, ui-ux-pro-max); the **`github`** and **Claude Code Remote session** MCPs; **Build OS agents 5/5 discoverable**.
  - **THIS IS THE ROUTER'S OWN "NO-ROUTE-TO-UNVERIFIED" RULE CATCHING THE ROUTER. RE-VERIFY PER SURFACE; DO NOT INHERIT P-012'S HOST VERDICTS.**
- **(T-001) NEW — THE SPECIALIST-HANDOFF GATE IS A SAFETY BOUNDARY AND MUST NOT BE REMOVED CASUALLY.** `env.BUILD_OS_SPECIALIST_HANDOFF = "1"` in `.claude/settings.json` is the recursion guard read at `.claude/hooks/prompt-router.sh:16`. Without it, prompts classifying as **`ecc`** or **`zeroize`** (a) write **`$HOME/.claude/settings.json` and `$HOME/.claude.json`** — *machine-global, outside the product repo* — via `capability-profile.sh:6-8,53-71`, (b) register a **third-party MCP fetched from GitHub** (`uvx --from git+https://github.com/oraios/serena@68884f11…`), and (c) spawn an **autonomous `claude -p` child for up to 900s** via `specialist-handoff.sh:210-307`. That is **external mutation plus an unplanned agent launch without explicit go** — two CLAUDE.md hard gates. **The gate lives in `env` deliberately:** `project-bootstrap.sh:322-350` loads the settings dict, mutates **only** `data["hooks"]`, and writes the rest back, so **the gate survives every canonical refresh** — verified against `project-bootstrap.sh --force`. **Editing `prompt-router.sh` or deleting the tools would NOT survive**; both are managed and would be restored, producing permanent **DRIFT churn**. **And note the classifier is tuned to THIS project's vocabulary** — `CAP_ECC` matches `postgres.*(schema|migration|index|query|performance)`, `eval.*harness`, `architect(ure)?.*review`; measured, `"do an architecture review of the model layer"`, `"review the eval harness results"` and `"Next packet: LG-009 postgres RLS migration schema review"` **all route to `ecc`**, while `"keep going"` and `"Shape A-S4 and pick the next detector"` route to focused.
- **(A-S3) NEW — THE DEFEATER TAXONOMY. Apply to any branch that CERTIFIES (`pass`/`confirmed`) *OR* asserts a deterministic claim into a prompt.**
  1. **A sibling that overrides** — another instance of the same construct nullifies it (permissive RLS policies OR-ing).
  2. **A later statement that revokes** — lifecycle: `DISABLE`, `DROP`, `ALTER`.
  3. **A lexical context that makes it inert** — comments, string literals, dead code.
  4. **A qualification mismatch that hides it** — schema-qualified names, aliases, path prefixes.
  5. **A semantic equivalent unrecognised** — `USING (1=1)` for `USING (true)`.
  6. **A collection boundary that never read it** — carrier predicates, extension gates, caps, `node_modules`.

  **RECORD WITH THE SPLIT: 1–5 are about the text the scan SAW; 6 is about text it NEVER READ.** The builder's self-observation, verbatim, because it names why class 6 is the most likely to be missed: *"it is the one my own polarity argument was blind to, because I reasoned about the text the scan saw and never asked what it never read."* **R-A is that class recurring within a single round, immediately after it was identified.** Class 6 also **retroactively covers A-S1c's `.mts` gap and A-S2's 5-file cap.**
- **(A-S3) NEW — TWO GOVERNING RULES.**
  - **DENYLIST OF DEFEATERS, NOT ALLOWLIST OF POSITIVES.** A missing positive costs a false `unknown` (**safe**); a missing defeater costs a false certification or a false assertion (**unsafe**).
  - **THE WIDENING-DIRECTION RULE.** For any predicate gating a claim, **state which direction widening its input moves the claim, and prefer the widening that can only weaken it.** **Corollary earned at A-S3:** *widen the search for a signal; keep narrow the predicate that gates applicability* — **and parameterize them separately**. `rlsScanText` vs `isSchemaCarrier` is the reference implementation.
  - **RECORD THE COROLLARY AS A PATTERN, NOT A COINCIDENCE — it is the THIRD INDEPENDENT ARRIVAL AT THE SAME SHAPE** (A-S2's `selects`/`anchor` two-parameter contract; A-S2's `prefers` bands; and now `rlsScanText`/`isSchemaCarrier`): **when one predicate is doing two jobs, split it before something silently widens the wrong one.**
- **(A-S3) NEW — THE `ModelCheck` POLARITY PROPERTY IS LOAD-BEARING.** Settledness is **the absence of a request**, not a boolean. `request: undefined` cannot accidentally mean *ask anyway*, where a boolean predicate's plausible default (`() => false`) would ship an `infer` call — repository text off-process — for an already-decided check. `interpret` enforces the same rule independently. `MODEL_CHECKS` is **fixed and append-only** (`['LG-006','LG-005','LG-009']`), **sequential, no `Promise.all`**; `scanWithModel` and `createScanner` iterate the **same** array, which is what stops the AT-27 defect class. Any new D+M check must be added by appending, never reordering.
- **TEST-DATA POLICY** (P-003-S2, from the GitHub push-protection incident; reinforced P-003-S3, P-003-S4, P-004, P-005): fake provider-key literals on EVERY surface — fixtures, tests, receipts, memory — must keep SHORT suffixes (<20 contiguous alphanumerics) AND avoid any Sentry-DSN shape (no Sentry ingest-host string, no long-hex-then-@ pattern), so GitHub secret scanning does not block pushes. A shortened literal still exercises detectors and still redacts correctly. P-004's CLI outputs and P-005's model layer render/transmit ONLY already-redacted evidence excerpts (P-005 additionally fences them as SEC-5 delimited untrusted data), so no CLI or model surface can emit a raw fake key. Resolve push-protection this way — NEVER by allowlisting a secret.
- **MODEL-LAYER SAFETY BOUNDARY** (P-005, binding on every model-touching slice): the credential boundary for SugarBee.ai's OWN model API (spec §10's one permitted egress; NOT provider credentials — SEC-3 intact) lives ONLY at `bin/sugarbee.ts`. `RealModelClient` reads no `process.env`, uses built-in `fetch`, no SDK, and is imported ONLY by the bin. `FakeModelClient` drives ALL tests (no real network, ever). Model/detector code reads no credential/env/network. The model receives ONLY deterministic-surfaced, already-redacted excerpts wrapped as SEC-5 delimited untrusted data; no tools; cite-or-discard; classified at best `inferred`, capped 0.9, `contradictory` on disagreement with a deterministic supporting fact. Engine + schema stay byte-identical (EMIT-ONLY). Any future model-touching slice must preserve all of this.
- **(A-S3) NEW — THE SEC-5 TRUSTED REGION IS A REAL BOUNDARY AND LG-009 WAS THE FIRST CHECK TO TEST IT.** `realClient.ts:37-44` sends `question`, `responseSchema` and `supportingFacts` as **trusted top-level fields**; only `excerpts` go through `buildUntrustedDataEnvelope`. Every statement in the codebase was a hardcoded literal, with integers as the only interpolations. **Any check wanting repository-derived text in `question` or `SupportingFact.statement` must sanitize it**: LG-009 validates identifiers `^[A-Za-z_][A-Za-z0-9_]{0,62}$`, dedupes, sorts, caps at 10, and drops-and-counts failures, proven with a **hostile-identifier test** (Postgres quoted identifiers may contain quotes and newlines). **A false deterministic claim in the trusted region is also a prompt-injection-adjacent hazard, not merely an inaccuracy** — R-A is exactly that.
- **`node_modules` EXCLUSION IS LOAD-BEARING** (A-S1b): LG-006's deterministic blocker survives **only** because `collect.ts:47` excludes `node_modules` — the `stripe` package's own typings name `customer.subscription.deleted`. **If a "scan vendored dependencies" mode is ever added, LG-006's blocker dies silently.** **(A-S1c) twice over** — repo-wide code files enter prompts. **(A-S2) three times over** — `event.id` saturates Stripe's typings, flooding LG-005's delegated surface. **(A-S3) FOUR times over** — vendored packages ship `.sql`/`.prisma` files and `organizationId` columns, which would flood LG-009's applicability *and* its surface. qa re-verified `IGNORED_DIRS` byte-identical at `04a5ea0`.
- **SHARED WEBHOOK LOCATOR — ONE CONTRACT** (A-S1b): `src/scan/webhook.ts` is the single webhook-handler locator, **all-handlers (`.filter()`) semantics**, deterministic via `collect.ts:270`'s path sort. Every future consumer imports it rather than restating the predicate. **`lg003.ts:51` is still an unmigrated third copy** (see Deferred). **(A-S1c) The same rule binds `isCodeFile`** — but unification is only at the sites A-S1c touched; `lg003.ts:35`, `lg015.ts:39`, `lg010.ts:62` still carry private `CODE_EXT_RE` copies. **`isCodeFile` is NOT the single source of truth.**
- **DETECTOR-SLICE externalVerification OBLIGATION** (P-003-S1, still binding): every finding on LG-003/010/012/014/015 must carry `externalVerification` (or an unverified classification) — that is what enforces the §7 Phase-1 ceiling; AT-16 is the backstop. Proven in code for LG-003, LG-010, LG-014, LG-015; LG-008 only on its partial/external branch. LG-005 and LG-009 correctly carry none on any branch. **Among external checks only LG-012 remains pending — Layer D+M, lands LAST (A-S7); it is ALSO the owed structural variant for the `ModelCheck` seam** (see Deferred). The scanner never represents repository evidence as provider-side proof.
- **CEILING INVARIANT — `hasAppSignal ⊇ scanner.supported`** (standing; **STRUCTURAL, not incidental** — A-S1c proved it by construction and enumerated all **32** combinations of the five structural signals as real directory scans: **0 unqualified `ready`, 0 ceiling violations**; A-S1b's caveat is retired, correction in `build-os/receipts/A-S1c.md` Corrections #2, `A-S1b.md` NOT edited). Any future slice that BROADENS `scanner.supported` OR NARROWS LG-015's applicability gate without preserving this superset could open an unqualified-`ready` hole. Add a regression assertion when the `golden/`/AT-16 fixture lands. **(A-S3) THE INVARIANT IS NOW LOAD-BEARING IN A NEW WAY: it is what made LG-009's pass-branch withdrawal FREE** — `pass` and `unknown` produce the identical decision and exit code. Anything that lifts the ceiling changes that calculus and would put the certification question back on the table.
- **DELEGATED-SURFACE SEAM — ONE CONTRACT** (A-S2): `src/scan/surface.ts` is the single D→M excerpt-surfacing seam, consumed by `lg006.ts`, `lg005.ts` and `lg009.ts`, destined for LG-011/013. **`selects` and `anchor` are TWO parameters, not one** — collapsing them silently widens file selection (pinned by a test). **`isModelSurfaceableFile` (= `isCodeFile` ∪ `{.mts,.cts}`) is DISTINCT from `isCodeFile` and must stay distinct.** **Path order is preserved within each `prefers` band**, which keeps AT-23 byte-identity true. **(A-S3) `prefers` is now ORDERED BANDS** implemented as successive filters over the remainder (one-band case byte-identical **by construction**, not by a sort-stability argument), and `DelegatedSurface` carries `elidedByBand` — indexed as `prefers` with a trailing unbanded entry, always summing to `elided`. **The disclosure helpers (`elisionDisclosure`, `withheldNonSourceDisclosure`) now live HERE, once**; zero copies remain in `lg005.ts`/`lg006.ts`. **Both transmitted questions are pinned as full literals** in `tests/model/questionPin.test.ts` — a reword must be a deliberate edit.
- **`PROSE_EXT_RE` IS ADJUDICATION-GATED** (A-S2): the prose allowlist (`.md|.mdx|.markdown|.txt|.rst`) is the **sole** thing between a `.sql`-only repo and a false deterministic `fail`-establishing claim. The allowlist form is correct precisely because it **fails safe on omission**. **Adding an extension silently RE-ENABLES the fail-establishing fact for that file type** and requires the same scrutiny as widening a detector gate. Byte-identical at `04a5ea0`.
- **MODEL-CALL SUPPRESSION IS A SECURITY PROPERTY** (A-S2; **STRUCTURAL since A-S3**): `scanWithModel` makes **zero** `infer` calls for a check excluded by `--checks` — asserted per check id for LG-005, LG-006 and LG-009 individually. This is the difference between an excluded check and an excluded check that **still ships repository text off-process**. Model calls stay **sequential in a fixed, append-only check-id order** — `Promise.all` is forbidden.
- **HASH TABLES MUST PUBLISH BOTH THE ROOT TOKEN AND THE CLOCK** (A-S2, correcting A-S1c): **sha256 of `serializeReport(report)` with `report.repo.root` → the literal `<ROOT>`, first 16 hex, at fixed clock `2026-07-23T00:00:00.000Z`**. Byte length is **clock-invariant**; the digest is **not**. A-S1c's digests are UNDER-SPECIFIED, not wrong; receipts are append-only. **Baseline future comparisons against the A-S3 receipt's 11-row table** — A-S2's is superseded (all 11 moved at A-S3 Commit 2, each gaining one LG-009 `not_applicable`, including `unsupported`).
- **`--amend` IS UNSAFE ON THIS BRANCH** (A-S2): other agents commit concurrently, and a `build-os` commit landing mid-packet was **absorbed by the builder's `--amend`** and had to be repaired via reflog. **Any amend must first verify `HEAD` is the intended commit** — honoured before each of A-S3 Commit 2's four amends. Adopted practice: **hold all `build-os/` commits until the packet closes.**

## Deferred (follow-up packets)

- **(EV-001) SECTION-NUMBER COLLISION ACROSS THE TWO SPEC DOCUMENTS — reviewer item 7, NOT APPLIED, out of budget.** `specs/evidence-allocation-and-phase-boundaries.md` and `specs/phase-1-repository-auditor.md` **both** have a §3, §7 and §8, and bare `§3` / `§7` / `§8` are used **in both senses**. §1 qualifies the path at the one place it matters most, and **`P1§` qualifiers were introduced in the repaired passages only** — a **full sweep is owed**. Until then, **write `P1§3`, never bare `§3`**.
- **(EV-001) THE `:116` "and/or" PARAPHRASE DRIFT ON LG-013 — NOT APPLIED.** The document's paraphrase of the LG-013 row drifts from the spec's `and/or`. Low consequence; sweep with the section-number pass.
- **(EV-001) §9'S AMENDMENTS ARE *PROPOSED*, NOT ENACTED — INCLUDING §9.1 (LG-009's PRIMARY AUTHORITY MOVING TO THE PROVIDER).** **`P1§3:120`'s `External component: No` WINS TODAY.** Both matrix panels and §3.7's title carry the divergence marker. **A future session must not read §3.7 as a decision already taken.** Enacting any of §9 is a **separate packet against `P1§3`/`P1§13`**, not a doc edit.
- **(T-001) THE TOOL ROUTER HAS NO ROW FOR FRAMEWORK / TOOLING ADOPTION.** T-001 routed from the **Build** row with reviewer dropped, then needed a reviewer anyway (it found the central defect). A `T-` row is owed — **but `tool_router.md` is MANAGED**, so the row belongs **upstream in canonical ClaudeOrchestrator**, not as a local edit that the next bootstrap reverts. Record the constraint before someone "fixes" it locally.
- **(T-001) MANIFEST TIMESTAMP — TWO READINGS, BOTH RECORDED.** `build-os/.install-manifest.json` embeds `installed_at` (T-001 moved it `2026-07-26T15:32:55+00:00` → `2026-07-26T15:41:46+00:00`). **Reviewer's read:** the cache gate means it is rewritten **only on a real install**, so a new timestamp is **accurate rather than noise**. **qa's read:** expected one-line churn. **A future session must not read this timestamp as evidence of anything except the time of the last write.**
- **(T-001) INERT BUT ARMED.** `.claude/hooks/session-start-build-os.sh:48-51` **background-launches `$ROOT/install-accelerators.sh` if that file ever appears at the repo root.** It does not exist in LaunchGraph today. **Creating a file with that name at the repo root would silently start executing it on every SessionStart.**
- ~~(P-001) Part II provenance note.~~ **RESOLVED by P-002** (commit 3a5ad85).
- ~~(P-001) Likely next packet: Phase 1 "Repository auditor".~~ **SUPERSEDED by P-002** — the Phase 1 spec now exists.
- (P-002) Phase 1 implementation must be decomposed into ≤2-commit packets at go time — **IN PROGRESS**: P-003-S1/S2/S3/S4, P-004, P-005, A-S1b, A-S1c, A-S2 all closed; **A-S3 (the `ModelCheck` seam + LG-009, Fork Branch A Slice 3) closed 2026-07-26**. **DETECTOR COVERAGE 10 → 11 of 15 — QUALIFIED: 11 implemented, of which 8 can move an offline decision and 3 (LG-005, LG-006's M half, LG-009) are online-only or partly so. LG-009's contribution to the 11/11 eval result is EXACTLY ZERO and it advances §9.3's thresholds not at all. Never record "11 of 15" unqualified.** The model layer exists (emit-only), the shared webhook locator exists, the D→M surfacing contract exists, the delegated-surface seam exists, and **the `ModelCheck` seam now exists**. The spec's AT-01..AT-29 remain the program's done criteria, not one packet's. **NEXT = A-S4** — remaining detectors **LG-007, LG-011, LG-012, LG-013**; see `active_packet.md` for the binding shaping inputs.
- **(A-S3) THE `ModelCheck` SETTLES-`pass` SHAPE IS UNPROVEN AND OWED.** The seam was justified against {settles-fail, settles-nothing, settles-**pass**}; with LG-009's pass branch withheld the true base is **{fail, nothing, nothing}**. **LG-012's `externalVerification`-on-unknown is the next structural variant due (A-S7)** and is the first real chance to re-test the interface against a genuinely different shape. *(Counter-evidence, and it is strong: LG-009 changed settle-shape LATE in the packet and the seam absorbed it with exactly one line plus an import, with `modelCheck.ts`, `surface.ts`, `lg005.ts`, `lg006.ts` byte-identical between C1 and the final C2. An interface that survives its motivating consumer changing shape mid-packet is better evidenced than one that fit three consumers on paper. Record both.)*
- **(A-S3) R1 — REFRAMED, AND THIS IS IMPORTANT. R1 IS NOT A NARROW CARVE-OUT; IT IS THE GENERAL FORM OF THE ONLY PHASE-1-DEFENSIBLE CERTIFICATION.** Reinstating any LG-009 certification needs: **(1)** a real **SQL statement parser** replaying statements in order — which kills the lexical and lifecycle defeater classes **structurally**, not by patching; **(2)** a **default-deny defeater denylist**; **(3)** evidence that the **connecting role is subject to the policies**. **Requirements 1–2 are Phase-1-achievable; requirement 3 is Phase-3 external verification** — which role the runtime connection authenticates as is **not a repository fact** (§1.2), and Supabase's `service_role` bypasses RLS wholesale. Since 3 is unanswerable from a repository, the **only Phase-1-defensible shape is the one where the repository ITSELF constrains the connecting role** (a browser-scoped Supabase anon client querying tenant-owned tables, plus in-repo migrations creating those tables, plus no RLS statement — because in that configuration RLS is the only isolation mechanism that can exist, and application-layer scoping is not isolation at all). **Record as "blocked on Phase 3 unless scoped to R1."**
- **(A-S3) AT-09 IS OWED — recorded exactly as AT-05 was — AND THERE IS NO EVAL PATH TO CLOSE IT.** Eval runs the **offline** scanner and LG-009 has **no offline verdict** beyond `not_applicable`/`unknown`, so a `broken-lg-009` fixture would assert a shape it cannot exercise. **That is precisely the defect qa found in A-S2's committed Trap-9 check** — direction checks must exercise the shape whose name they carry. Closing AT-09 requires either an online eval path or the R1 certification, neither of which exists.
- **(A-S3) R-B (cosmetic).** Stale comment at `src/checks/lg009.ts:377-378` describing carrier ordering and `DISABLE` logic that went out with the withdrawn branch. Zero risk; sweep when the file is next opened.
- **(A-S3 / A-S2) FB-5 VOCABULARY GAP.** A tier list of `tenant_id`/`organization_id` misses `orgId`, `workspaceId`, `teamId`, `accountId`, `companyId`. Under-detection of the **exonerating** signal is what produces a blocker. Carried from A-S2's DC-11 discussion; still open.
- **(A-S3) LG-011 IS NOT THE SAFE OPTION — BINDING SHAPING INPUT.** Examined at A-S3 shaping: §3's LG-011 deterministic half walks straight into the `process.env.X ?? 'http://localhost:3000'` **dev-fallback idiom this program already refused once** (LG-001 source-path scanning, P-003-S2), at **`confirmed` / Rule 2 / `not_ready` with no override** — the same worst-case setting LG-009 carried, on a hazard already documented as real. LG-011 also has its own carrier problem (`.html`/`.mjml`/`.hbs` are not `isModelSurfaceableFile`) with **no** equivalent "the D layer may read it completely" escape, because the answer depends on *resolution* of an env variable, not the presence of a literal. **"LG-011 is lower risk" does not survive contact with §3. It is UNEXAMINED, which reads as lower risk and is not.**
- (P-002) "Later Phase 1 packets" queued behind the auditor core: cost estimation + launch-plan generation; capability graph as versioned asset; recipe selection.
- (P-002) External-side verification of LG-003/010/012/014/015 is owned by Phase 3.
- **(P-005 → A-S1b → A-S1c) UN-MIGRATED PREDICATE COPIES — one folded item.** `lg003.ts:51`'s webhook-predicate copy **plus** three surviving private `CODE_EXT_RE` copies at **`lg003.ts:35`, `lg015.ts:39`, `lg010.ts:62`**. A-S1c unified the code-extension predicate **only at the sites it touched**; it is not repo-wide. Low risk today (the copies are byte-identical) but it is the exact drift the extraction exists to prevent — and widening `isCodeFile` without migrating them would make them diverge in a way that matters. Untouched by A-S2 and A-S3.
- (P-005) **SEC-5 DELIMITER-TOKEN HARDENING** — harden `buildUntrustedDataEnvelope` against an excerpt reproducing the END delimiter token. Low consequence given the structural bounds; do it alongside the AT-21 hostile fixture. Non-blocking.
- (P-003-S1) Spec clarification candidate: rule-5 behavior for contradictory non-fail outcomes is unspecified; consider a validator invariant "contradictory ⇒ outcome ∈ {fail, unknown}".
- (P-003-S1) Harness debt: greedy matching is not optimal assignment for overlapping expected entries; `discoverFixtures` silently skips manifest-less dirs — tighten when the eval surface hardens.
- (P-003-S1) Cheap test strengthener: rule 5 with a warning-severity finding on a blocker-ceiling check.
- (P-003-S2) **LG-001 SOURCE-PATH SCANNING deferred** — §3 lists "hardcoded in source on production paths" as an LG-001 signal; current impl covers config carriers only. Deliberately omitted to avoid the `process.env.X || 'http://localhost'` dev-fallback false-positive idiom (disclosed in the `lg001.ts` docstring). **A-S3 re-affirmed this refusal and extended it to LG-011** — see the LG-011 item above.
- (P-003-S2) AT-16 real `golden/` fixture + AT-20/AT-22 hostile-fixture closure deferred to later slices. `clean-min/` is a subset-scoped negative fixture demonstrating the `ready_with_warnings` ceiling; AT-16's real golden repo is still owed.
- (P-003-S3) **LG-008 warning-outcome enhancement** — for a lone CONCRETE remote DB URL committed in an *unscoped* carrier with no per-environment separation, consider a warning-outcome repo-side finding rather than folding it into the external `unknown`. Non-blocking.
- (P-003-S3) **LG-003 provider-agnostic webhook presence** — `isWebhookRoute` treats any *webhook* route as present. Fine for the deterministic slice; refine later without treading on LG-004. Non-blocking.
- (P-003-S3) **SPEC-DISCREPANCY NOTE** — the P-003-S3 go-instructions mis-attributed LG-011's "localhost / preview host" language (§3) to LG-015. §3 LG-015's only fail conditions are "no canonical production URL configured anywhere, or configured values disagree." The builder's presence-not-correctness handling is spec-faithful. No action — recorded so future slices do not propagate the confusion. **Relevant again now that LG-011 is a near-term candidate.**
- (P-003-S4) **LG-010 domain heuristic (no PSL)** — last-two-labels match, disclosed in the docstring. Can ONLY under-warn. Non-blocking.
- (P-003-S4) **LG-014 implicit-Sentry mechanisms** — does NOT recognize implicit release-injection / dashboard-only DSN mechanisms. Acceptable and spec-faithful. Non-blocking.
- ~~(P-004) **SPEC HEADER STALENESS**~~ **RESOLVED BY EV-001 (`258cc70`)** — the header now reads *"approved scope; implementation underway, one gated packet at a time"* and cross-references the evidence-allocation document. **The "one gated packet at a time" wording is LOAD-BEARING and was a REVIEWER-REQUIRED FIX: the first replacement removed the spec's ONLY explicit-go sentence, leaving the authoritative document with no permission language anywhere — in the very packet whose purpose was to stop permission being read into prose.** Still exactly +2/−2.
- (P-004) **`report.repo.commit` stays null** — the banner's `@<commit>` is cosmetic. Wire it in a later polish pass. Non-blocking.
- (P-004) **BANNER LOCATOR ARTIFACT on fixture scans** — cosmetic, cannot occur on a real repo. Non-blocking.
- (P-004 → A-S1b) **`eval` runs over the 11 present fixtures only** — golden/, hostile/, and the remaining model-layer broken fixtures still don't exist, so **§9.3's 15/15-recall and 18/18-decision thresholds remain a PROGRAM-level gate**. **A-S2 and A-S3 both moved eval not at all** (A-S3 by design — LG-009 is offline-inert). Non-blocking.
- ~~(P-004) AT-27 "mark M-checks unknown" half OWED.~~ **RESOLVED by P-005**; **A-S3 made its defect class structurally harder** — `createScanner` and `scanWithModel` iterate the same `MODEL_CHECKS` array.
- **(A-S1b → A-S1c → A-S2) The "never manufacture a false blocker" guarantee was OFFLINE-ONLY** — closed for the extensions `isCodeFile` matches, and the false `.sql`/`.prisma` fact stopped by the prose-allowlist gate. What survives is the `prefers` re-weighting limit (TOP ITEM) and, newly, **R-A on LG-009** (TOP ITEM).
- **(A-S1b) LG-006 comment-suppression / recall cost.** `SUB_DELETED_RE` runs over **raw file content** and the blocker gate scans **all collected files**. A README, agent spec, commented-out branch, or SQL migration comment merely *naming* `customer.subscription.deleted` flips the blocker to `unknown`. **Direction-safe** (under-warn only) but **WIDER in SugarBee's actual target population**. Disclosed in-code at `lg006.ts:186-192`. **Note A-S3 built a string-aware SQL comment walker for LG-009; LG-006's raw-content scan did NOT get it** — a candidate cheap improvement, but note the widening-direction rule cuts the other way here (stripping comments would *un-suppress* blockers).
- **(A-S1b) LG-006 evidence + fixture nits.** LG-006's fail evidence reuses the single-line anchor rather than the whole-handler excerpt; **AT-26 still passes**. `fixtures/broken-lg-006/app/api/stripe/webhook/route.ts:1-2` imports at five levels where four is correct — harmless. Non-blocking.
- **(A-S1b) AT-06 IS ONLY HALF CLOSED.** Eval on `broken-lg-006` closes the **deterministic** half; the **model-assisted** half stays proven by `FakeModelClient` integration tests. **Do not claim AT-06 closed end-to-end by one fixture.**
- **(A-S1b) FROZEN-STATUS CHANGES, deliberately disclosed.** `lg004.ts` lost its "frozen / byte-identical" status in A-S1b Commit 1; `fixtures/broken-lg-004` was **EDITED** as a disclosed rebaseline. Future regression comparisons baseline against `6509b35`, not the P-005-era assertions.
- **(A-S1c) The elision honesty guard is a REGRESSION PIN, not a semantic guarantee.** It pins the phrasings "plainly handles" / "clearly handles" only; a reword would slip past it. qa confirmed it **discriminates** rather than being vacuous. Treat it as a tripwire on one specific overclaim. **(A-S3) `tests/model/questionPin.test.ts` is a stronger instrument of the same kind** — it carries both questions as full literals, so drift is impossible and a reword must be deliberate.
- **(A-S1c) The 5-file cap's STATED JUSTIFICATION WAS DEMOLISHED.** Six mentions can all be UI copy, so "a repo mentioning cancellation in six code files plainly handles cancellation" does not hold. The cap was kept, but the elision disclosure makes **NO completeness argument**. **Do not reintroduce one.** **(A-S3) The cap is defeater class 6**, and `elidedByBand` + DC-11 are the partial answer: specific-band elision suppresses a model `fail` to `unknown`.
- ~~**(A-S1c) TWO TRAJECTORY SEAMS NAMED FOR A-S2.**~~ **BOTH NOW CLOSED** — (a) `surfaceDelegatedCandidates` extracted in A-S2; (b) the **`ModelCheck` interface extracted in A-S3 Commit 1**.
- **(A-S1c) Corpus regression baselines must be PER-FIXTURE NORMALIZED HASHES, never a whole-corpus aggregate** — an aggregate is scan-path dependent because `repo.root` is embedded in every serialized report. The A-S1c builder's aggregate figure and A-S1b's `c1f187c5…` are both **RETRACTED** as usable baselines. **The authoritative table is now the A-S3 receipt's.**
- **(A-S2) `@unique` / `@@unique` IN `IDEMPOTENCY_SIGNAL_RE` CAN ONLY MATCH NON-SURFACEABLE `.prisma` FILES** — they contribute **only to the withheld-count disclosure**, never to file selection. Harmless; recorded so a future reader does not assume otherwise.
- **(A-S2) AT-05 IS NOT CLOSED — an owed item.** No `broken-lg-005` fixture; eval runs the offline scanner and LG-005 correctly has no deterministic disjunct. Inventing a Layer-D blocker was rejected as **falsifiable on correct repositories**. **AT-09 (A-S3) is the same shape, and worse — it has no eval path at all.**
- **(A-S1c → A-S2 → A-S3) THE `.sql`/`.prisma` PROMPT-SURFACE SEC-5 ADJUDICATION IS STILL OWED.** A `.sql` migration or `.prisma` schema still never enters `request.excerpts`. Admitting structured non-source text into a prompt is a **real security decision**, not something to smuggle into a detector packet. **A-S3 did not need it** — LG-009's Layer D read those carriers directly, with no new capability and no change to `isCodeFile`/`isModelSurfaceableFile`/`PROSE_EXT_RE`. Still owed for `request.excerpts`.
- **(A-S2 → A-S3) PER-CHECK MODEL-ERROR DEGRADATION** (Trap 5) — current propagate-semantics pinned by an explicit test. Still deferred.

- **(T-002, CODEX) P1 `src/checks/lg005.ts:205` — "SURFACE DELEGATED SIDE-EFFECT IMPLEMENTATIONS." A REAL PRODUCT DEFECT ON CLOSED WORK (A-S2). BINDING SHAPING INPUT. — **LINE NUMBER CORRECTED AT EV-001: THE SITE IS `src/checks/lg005.ts:203` (`selects`), NOT `:205` (`prefers`). Codex's citation was off by two; the defect is real. AND THIS IS NOW THE LEADING CANDIDATE FOR THE NEXT PACKET (see TOP ITEM).**
  - When a webhook **delegates** to a helper that is **idempotent by construction** (e.g. a plain state-reconciliation `UPDATE`), the `selects` predicate **EXCLUDES it**, because it carries none of the event-id / upsert / dedup markers.
  - **The model therefore cannot see the code needed to distinguish that safe path from a helper that inserts, or sends mail, on EVERY delivery — yet LG-005's verdict can still become a BLOCKER.**
  - This is **defeater class 6** (a collection boundary that never read it) and a direct instance of the project's own governing rule being violated: **it is an ALLOWLIST OF POSITIVES, not a DENYLIST OF DEFEATERS.**
  - It **SHARPENS** the standing "`prefers` re-weights, it does not cure" item above.
  - **NOT FIXED AT T-002, DELIBERATELY. Findings on CLOSED receipts are input to a FUTURE packet, never a reopening. Receipts are append-only.**
  - **STILL NOT FIXED AT EV-001, DELIBERATELY** — EV-001 §7.1 **LOCATED and OWNER-ASSIGNED it; it repaired nothing.** A doctrine packet may name a defect; it may not quietly fix one.

- **(T-002, CODEX) P1 `src/checks/lg009.ts:424` — "TREAT EVERY ELIDED LG-009 CANDIDATE AS INCOMPLETE." A REAL PRODUCT DEFECT ON CLOSED WORK (A-S3). NEW BINDING SHAPING INPUT FOR A-S4.**
  - When five preferred files fill the cap and an **additional table-name-only candidate holds the real isolation logic** (e.g. a relation predicate not spelling a recognized tenant column), **`incompleteSurface` IGNORES the omitted unmatched/trailing band**, so a model `fail` is accepted as an **inferred blocker** although **the exonerating query was deliberately withheld**.
  - **DC-11 must account for the trailing generic band, or PROVE those candidates cannot contain scoping.**
  - This **extends A-S2's "incompleteness WITHIN a band" to the UNBANDED REMAINDER** — and it is exactly the asymmetry DC-11 was built to guarantee.
  - **BAND COUNT ESTABLISHED FROM SOURCE AT EV-001 — AND IT NARROWS, NOT WIDENS, THE HOLE.** `src/scan/surface.ts:174` sizes `elidedByBand` as **`bands.length + 1`** (the `+1` is the **trailing unbanded slot**); `src/checks/lg009.ts:423` sums **only `[0] + [1]`**. The open worry was that LG-009 ranks across **THREE** bands, in which case `[0]+[1]` would miss **a real band AND the trailing slot**. **IT DOES NOT: `src/checks/lg009.ts:412-415` passes EXACTLY TWO `prefers` predicates** (`SCOPING_MECHANISM_RE`, `TENANT_COLUMN_RE`), so **`bands.length === 2`, `elidedByBand.length === 3`, and `[0]+[1]` omits EXACTLY index 2 — the unbanded remainder, and nothing else.** **"Three ordered bands" was a misreading of `elidedByBand`'s LENGTH, not of the band count.** **The hole is precisely what Codex described.** *(The fix packet should still re-derive this from the code rather than from this note — that is the whole lesson of the `lg005.ts:203` correction above.)*
  - **NOT FIXED AT T-002, DELIBERATELY. STILL NOT FIXED AT EV-001** — EV-001 §7.2 located it only. Same append-only rule.

- **(T-002, CODEX) P2 `build-os/tools/project-bootstrap.sh:209` — "VALIDATE MERGED SETTINGS AND `CLAUDE.md` BEFORE CACHE EXIT." AN UPSTREAM ClaudeOrchestrator DEFECT — OWED UPSTREAM, NOT FIXABLE HERE.**
  - When the canonical SHA and the manifest-listed file hashes match but the managed `CLAUDE.md` block **or** either `.claude/settings.json` hook entry has **DRIFTED**, the **cache path exits before either merge runs**.
  - **Those two managed outputs are ABSENT from the manifest's `files` map**, so a bootstrap **reports the runtime up to date without repairing them.**
  - **DO NOT FIX HERE — NEVER WRITE TO ClaudeOrchestrator.** Record and carry.

## Known risks / debt

- **(T-002) THE §10 COUPLING IS DISSOLVED — AND THE HONEST FINDING IS THAT §10.1 AND §10.2 WERE THE SAME DEFECT, MIS-DIAGNOSED FOR SIX PACKETS.**
  - **SECOND EYES WERE NEVER UNAVAILABLE.** Every prior session checked `codex` **on PATH**, found nothing, and recorded "no second-eyes." **Codex is configured as a GitHub PR REVIEW BOT on this repo** (`chatgpt-codex-connector[bot]`). **It was reachable the whole time — via a PR.**
  - **The PR was never opened because of a FALSE PREMISE:** "this repo has no default branch" (carried **six** times) was **false**. See Open boundaries for the corrected facts.
  - **G2 CLOSED AT T-002 — the first independent, non-Claude review in six packets, and it found TWO REAL PRODUCT DEFECTS on closed work** (LG-005 and LG-009, above) plus **third-party corroboration** that T-002's pin was necessary and correctly designed, plus **one upstream defect**.
  - **THE STANDING LESSON, and it is the durable part:** *checking one delivery channel and concluding a capability is absent is a defeater-class-6 error about our own tooling.* **Six packets of "no second eyes" were an artifact of looking only at `PATH`.**
  - **The old text is preserved below for the record.**
- **~~(T-001) THE §10 COUPLING.~~ SUPERSEDED BY THE ENTRY ABOVE — kept verbatim as history.**
  - **Second-eyes review is STILL unavailable** — `codex` is absent from PATH; the T-001 reviewer confirms it ran as **single-model analysis plus empirical probes**. **FIFTH CONSECUTIVE PACKET.**
  - **BUT `mcp__github__request_copilot_review` IS LIVE ON THIS SURFACE** — a genuinely **independent, non-Claude** reviewer. It is **PR-scoped**.
  - **And this repo has NO DEFAULT BRANCH and nowhere to open a PR** (§10.1, now on its **SIXTH carry-forward**).
  - **THEREFORE §10.1 IS ACTIVELY BLOCKING THE ONLY AVAILABLE MITIGATION FOR §10.2.** They are no longer two independent deferrals; one is the gate on the other.
  - **T-001 — a file-copy packet — NEITHER DISCHARGES NOR WORSENS the second-eyes risk. The count is NOT silently reset. A-S4 is where it must be answered.** **(T-002 ANSWERED IT. The count stops at six.)**
- **(T-002) STATUS RESOLVED AND IT IS WORSE THAN "UNPROVEN": THE RUNTIME IS CONFIGURED-BUT-NOT-FIRING, PROVEN.**
  - **Hooks do not fire here.** The `hook-once` mechanism was **validated** to create `${TMPDIR:-/tmp}/build-os-hook-once` on a `session_id`-bearing payload, and there are **ZERO markers from real events**; `CLAUDE_PROJECT_DIR` is unset; there is no hook banner in the session context; `/tmp` was not cleared (it holds a Jul-23 file).
  - **SEPARATELY AND INDEPENDENTLY: `settings.json` `env` IS NOT DELIVERED TO PROCESSES ON THIS SURFACE.** qa found **no `BUILD_OS_*` in the parent session's `/proc/*/environ`** despite the key being committed since `8652bac`, and the session was launched with `--settings /root/.claude/launcher-settings.json` whose `env` is `None`.
  - **CONSEQUENCE — THE UNCOMFORTABLE ONE, RECORD IT EXPLICITLY: T-001's `BUILD_OS_SPECIALIST_HANDOFF` GATE RESTS ON THE SAME DELIVERY MECHANISM AND MAY ALSO BE INERT HERE.** The `ecc`/`zeroize` routes (machine-global writes, a third-party MCP from GitHub, a 900s autonomous `claude -p` child) are **dormant only because the hooks do not fire — which is NOT the same as being protected.** **Do not treat the gate as proven; treat the surface as quiet.**
  - **Two things this does NOT license:** (i) removing the gate — if the surface ever delivers `env`, the gate is the only defence; (ii) assuming the pin is inert-and-therefore-pointless — the pin is committed configuration and takes effect wherever `env` IS delivered.
- **~~(T-001) STATUS LABEL: THE RUNTIME IS "DURABLY CONFIGURED", NOT "ACTIVE".~~ SUPERSEDED BY THE ENTRY ABOVE — kept verbatim as history.** Installed ≠ activated (the standing status-semantics rule). **Whether Claude Code fires these hooks on this surface is UNPROVEN.** The reviewer's note, recorded because it is uncomfortable and true: **the packet cannot claim self-updating adoption while simultaneously relying on the hook NOT firing for safety — both halves rest on the same unproven premise.** Do not upgrade the label without evidence of a real SessionStart / UserPromptSubmit firing.
- **~~(A-S3) NO SECOND-EYES REVIEW FOR THE FIFTH CONSECUTIVE PACKET.~~ CLOSED AT T-002 — AND THE PREMISE WAS WRONG, NOT JUST THE OUTCOME.** The A-S3 reviewer's MCP inventory concluded *"none is a code-review second-eyes tool"* and *"there is no branch to open a PR against"*. **Both halves were false:** the repo **had** a default branch (`claude/launchgraph-product-scope-43pgdx` @ `a00d194`), and **Codex was configured as a PR review bot on it.** Kept verbatim below as history, because **the shape of the error matters more than the count**: `codex` is not on PATH and no plugin is present. The A-S3 reviewer **inventoried the connected MCP servers** (Apollo, Clay, GitHub, Higgsfield, Hugging Face, Otter, Supabase, Zapier) and **confirmed none is a code-review second-eyes tool**: GitHub MCP could host a PR review, but there is no branch to open a PR against; Supabase MCP is a *live project* tool whose use would be an external-mutation boundary, not a review. **At five packets — and with SEVEN certification defects on LG-009 found by adversarial passes rather than by the suite — record this as a standing process risk requiring a decision.** Every review to date has been single-model analysis plus empirical probes; the proof standard must not be overstated.
- ~~**(A-S1b) The `ready` ceiling is held by an INCIDENTAL guard.**~~ **RESOLVED / CORRECTED by A-S1c** — structural, all 32 combinations enumerated, 0 violations. Correction in `build-os/receipts/A-S1c.md` (Corrections #2); `A-S1b.md` NOT edited. Still add the regression assertion when `golden/`/AT-16 lands.
- ~~**(A-S1c) THE FALSE `fail`-ESTABLISHING FACT ON CORRECT `.mts`/`.cts`/`.sql`/`.prisma` REPOS.**~~ **CLOSED BY A-S2.** **But the CLASS recurred at A-S3 as R-A** (see TOP ITEM) — a false deterministic claim in the trusted prompt region, from a collection boundary. **The class is not closed; only its A-S2 instance is.**
- (P-003-S2, non-blocking) LG-001 `URL_KEY_RE` substrings (`base`/`site`/`origin`) can over-match benign env keys — can only shift pass↔unknown, never manufacture a false blocker.
- (P-003-S2, non-blocking) `vercel.json` evaluation in LG-001 assumes single-line key/value.

## Visibility items (outside Build OS scope)

- **Gravito governance gap** (user-flagged 2026-07-23, explicitly non-blocking, not a Build OS packet): "the live substrate-readiness check still reports Claude, Manus, and surplus_recovery as missing surfaces — remains visible as a distinct Gravito governance gap" (user, 2026-07-23). Recorded for visibility only.
- **Gravito — FIRST NAMED POST-GOLDEN-PATH RECIPE + REAL-WORLD EVAL TARGET** (user-directed, recorded at P-004 close; product-direction item, NOT a Build OS action item): Gravito is a Vite + Express + Fly.io TypeScript SaaS (NOT Next.js/Vercel), so SugarBee.ai's current Next.js-only `detectStack` would honestly return `not_evaluated` on it. An Express/Vite/Fly recipe is the FIRST obvious post-golden-path expansion, and Gravito becomes a named eval target once that recipe exists — but the golden path is completed FIRST. Distinct from the governance-gap item above.

## Open boundaries (awaiting explicit go)

- **(2026-07-25) BUILD OS ADOPTION + MEMORY RELOCATION — RESOLVED.** SugarBee's project memory lives in THIS product repo; ClaudeOrchestrator stays the framework only. Receipt ids preserved. A SessionStart hook (`.claude/hooks/session-start-build-os.sh`) + UserPromptSubmit router are registered in this repo's `.claude/settings.json`. **Still verify on a fresh session that the SessionStart hook actually fires in this environment** — installed ≠ activated (status-semantics rule).
- **(A-S1b, CARRIED FORWARD — FIFTH PACKET FLAGGING IT; NEEDS A USER DECISION, NOT A SIXTH CARRY-FORWARD) THIS REPO HAS NO DEFAULT BRANCH.** There is no `main` or `master`, locally or on the remote — only `origin/claude/launchgraph-product-scope-43pgdx` and `origin/claude/sugarbee-project-handoff-ic47uc`. `git merge-base HEAD origin/main` FAILS; `refs/remotes/origin/HEAD` is not a symbolic ref. So the working-contract "verify the branch base against the base branch" has **no protected base to verify against**, **"never merge without go" currently has NO TARGET**, and an eventual PR has **nowhere to land**. Not a blocker for building; explicitly not any detector packet's work.
- NO merge to any default branch in either repo (ClaudeOrchestrator, SugarBee.ai/LaunchGraph) and NO pull request — both await explicit go. Feature-branch pushes were explicitly authorized and completed (P-001, P-002, P-003-S1/S2/S3/S4, P-004, P-005, A-S1b, A-S1c, A-S2, and **A-S3** — under standing authorization after qa green + reviewer pass + safety grep; **P-005 pushed as `a4ffbf5..fb5663b`; A-S1b as a fast-forward `ed71d6e..6509b35`; A-S1c as `e8f6f99..7908d5d`; A-S2 as `93431ed..48e7b66`; A-S3 as a fast-forward `0e33643..04a5ea0`** — branch tip and code tip both `04a5ea0`). No deploys, no secrets touched/allowlisted, no provider access. **No real model API call occurred in P-005, A-S1b, A-S1c, A-S2, or A-S3** — `FakeModelClient` drove every test; the model layer's runtime egress only activates when a real model is configured at the bin, which did not happen.
- **(A-S3) The A-S3 receipt and these memory updates are NOT committed** — the user handles commit and push. Per the standing constraint, all `build-os/` commits are held until the packet closes.
- **(T-001, 2026-07-26) T-001's COMMIT 2 AND `build-os/receipts/T-001.md` ARE NOT COMMITTED.** The user commits the fix commit together with the receipt and these memory updates. **Nothing was pushed, merged, deployed, published, or allowlisted.** No secrets touched. **No real model API call occurred.** Commit 1 is `4920653` on base `f6a74c0`; ≤2 commits held.
- **(T-001) THE BRANCH-BASE CORRECTION, recorded so it is not re-litigated.** HEAD is on **`claude/sugarbee-project-handoff-tdfylf`**; `current_state.md:9` previously named `…-ic47uc` as primary. Local `tdfylf` was at `a00d194`, a **strict ancestor** of the work tip (**0 ahead / 18 behind**), and was **fast-forwarded to `f6a74c0`**. **`f6a74c0` is ALSO published as `origin/claude/sugarbee-project-handoff-ic47uc`** — so `tdfylf` is a **second ref on the SAME lineage, not a content fork**. `origin/claude/sugarbee-project-handoff-tdfylf` is a **stale local tracking ref for a branch that never existed remotely**. **Every SHA recorded in prior receipts remains valid.**
- **~~(T-001) NO DEFAULT BRANCH — SIXTH PACKET FLAGGING IT.~~ THE PREMISE WAS FALSE. CORRECTED AT T-002.**
  - `git ls-remote --symref origin HEAD` → `ref: refs/heads/claude/launchgraph-product-scope-43pgdx`; the GitHub repo object confirms `default_branch: claude/launchgraph-product-scope-43pgdx` @ `a00d194`.
  - **What was ACTUALLY true, and it is much narrower:** there was no branch *named* `main`/`master`, and **`refs/remotes/origin/HEAD` was unset IN THE LOCAL CLONE ONLY.** `a00d194` is a **strict ancestor** of the tip. **A PR WAS OPENABLE ALL ALONG** — and that false premise is what blocked second eyes for six packets.

- **(T-002) G1 — WHAT WAS DONE, AND UNDER WHAT AUTHORIZATION.**
  - **G1a CLOSED** — `origin/HEAD` set; `git merge-base HEAD origin/HEAD` resolves **for the first time**.
  - **G1b CLOSED** — **PR #1 open**: head `claude/sugarbee-project-handoff-tdfylf`, base `claude/launchgraph-product-scope-43pgdx`, **20 commits**. **IT IS A REVIEW VEHICLE AND IS NOT PROPOSED FOR MERGE.**
  - **G1c CLOSED** — **`main` created at `8652bac`** (all closed, qa-green, reviewer-passed work). **A new name on existing history: EVERY SHA IN EVERY RECEIPT STAYS VALID.**
  - **AUTHORIZATION — ON RECORD, NOT A BREACH.** The reviewer could not see the exchange and flagged a possible hard-gate breach. **The user was asked explicitly and answered: "Both: create main AND PR now."** Recorded so this is not later read as an unauthorized external mutation.
  - **THE `origin/HEAD` REPAIR, AND WHY IT WAS REPAIRED TWICE.** `git remote set-head -a` pointed it at the **repo default** `claude/launchgraph-product-scope-43pgdx`, so `git merge-base HEAD origin/HEAD` → **`a00d194`** — **three commits behind the real base and off this lineage** — handing the next session a **confidently wrong** answer where it previously got an honest failure. **Repointed to `refs/remotes/origin/main`; `git merge-base HEAD origin/HEAD` now → `8652bac`, the actual packet base.** **THIS IS A LOCAL REF AND `git remote set-head -a` WOULD REVERT IT** until the repository default is actually changed.

- **(T-002) G1d — OPEN. USER ACTION ONLY. NOT REACHABLE FROM A SESSION ON THIS SURFACE.**
  - **No `mcp__github__*` tool can set a repository default branch.** `create_branch`, `create_repository`, `fork_repository`, `list_branches` exist; **there is no repo-settings mutation tool.** `gh` is **absent from PATH**.
  - **USER: GitHub → Settings → Branches → set `main`.** The token has admin.

- **(T-002) G5 — OPEN. USER ACTION ONLY. AND IT IS THREE BRANCHES, NOT ONE.**
  - No branch-delete tool; no `gh`; **proxy 403 on ref deletion.**
  - The stale ClaudeOrchestrator branches are per **C2** in `build-os/receipts/T-002.md`; the G5 target is **`claude/launchgraph-product-scope-43pgdx` @ `ef8a75c` ON ClaudeOrchestrator**.
  - **CAUTION, RESTATED AND LOAD-BEARING: LaunchGraph's SAME-NAMED branch is CURRENTLY THAT REPO'S DEFAULT BRANCH — deleting it would remove the default. ONLY EVER DELETE THE ClaudeOrchestrator ONE; VERIFY THE REPO SLUG FIRST.**
  - **The nine-file byte-identity finding stands on the PRIOR session's record — it was NOT re-verified at T-002**, because that would require a `git fetch` into canonical.

- **(T-002, 2026-07-27) `c7baff9` IS LOCAL; the remote branch ref is at `8652bac`. `build-os/receipts/T-002.md` AND THESE MEMORY UPDATES ARE NOT COMMITTED.** The user handles commit and push, per the standing "hold all `build-os/` commits until close" constraint. **≤2 commits held (one commit).** Beyond the explicitly authorized `main` creation and PR #1: **nothing merged, deployed, published, or allowlisted; no secrets touched; NO REAL MODEL API CALL occurred in T-002.**

- **(EV-001, 2026-07-28) `258cc70` IS LOCAL AND UNPUSHED. `origin/main` IS AT `8652bac` — THREE COMMITS ARE UNPUSHED (`c7baff9`, `9706d87`, `258cc70`).** `build-os/receipts/EV-001.md` and these memory updates are **NOT committed**; the user handles commit and push, per the standing "hold all `build-os/` commits until close" constraint. **≤2 commits held (one commit).** **Nothing merged, deployed, published, or allowlisted; no secrets touched; NO REAL MODEL API CALL occurred in EV-001** — the packet ran no scanner and no model client.
- **(EV-001) A-S4 REMAINS *PAUSED BY USER DIRECTIVE*, NOT CANCELLED.** All **eleven binding shaping inputs are PRESERVED, NOT CLEARED**, in `build-os/packets/active_packet.md`. **The thesis is not abandoned; the build sequence is adjusted.** **EV-001 deliberately did NOT consume A-S4's slice number** — a non-detector packet must never take a slice number, and doing so would have made a **pause look like a completion**.

---
_Append-only working notes. Updated by the archivist on close of P-001
(2026-07-23), P-002 (2026-07-23), P-003-S1 (2026-07-23), P-003-S2 (2026-07-23),
P-003-S3 (2026-07-23), P-003-S4 (2026-07-23), P-004 (2026-07-23), P-005
(2026-07-23), A-S1b (2026-07-25), A-S1c (2026-07-25), A-S2 (2026-07-25),
A-S3 (2026-07-26), T-001 (2026-07-26), T-002 (2026-07-27), and **EV-001 (2026-07-28)**._
