# Residue

> What was deferred, left behind, or noted as risk — the stuff that didn't fit in
> the last packet but must not be forgotten. The orchestrator reads this to avoid
> dropping threads; the archivist appends/clears it on close.

## TOP ITEM — surface this first

- **(P-005) LG-006 DETERMINISTIC-DISJUNCT FIDELITY GAP.** Per spec §3 LG-006 ("No handler branch … fails as a blocker") and §4.3 (model judgment may NOT substitute a required deterministic signal), a **FULLY MISSING cancellation branch is a DETERMINISTIC `confirmed` blocker `fail`** and should stand even under `--offline`. Currently that case reports `unknown` offline / routes through the model as `inferred` online — a **false-negative on the canonical LG-006 defect in the deterministic/CI (`--offline`) path**. The deterministic signal IS captured (`fact:lg006.no-cancellation-branch` + the contradiction guard), so this is a **FIDELITY gap, NOT a safety gap.** Fix: emit a deterministic `confirmed` blocker `fail` when NO cancellation branch exists at all; reserve `unknown`/`inferred` for the branch-present-but-downgrade-unconfirmed case. Sequence it with the `broken-lg-006`/AT-06 fixture (and ideally with the shared-locator extraction below) — recommend as an early next slice, naturally bundled with A-S2 (which needs the locator anyway).

## Standing constraints (bind every future slice)

- **TEST-DATA POLICY** (P-003-S2, from the GitHub push-protection incident; reinforced P-003-S3, P-003-S4, P-004, P-005): fake provider-key literals on EVERY surface — fixtures, tests, receipts, memory — must keep SHORT suffixes (<20 contiguous alphanumerics) AND avoid any Sentry-DSN shape (no Sentry ingest-host string, no long-hex-then-@ pattern), so GitHub secret scanning does not block pushes. A shortened literal still exercises detectors (e.g. LG-002's `[A-Za-z0-9]{4,}`) and still redacts correctly; LG-014's DSN-absent defect is proven WITHOUT ever writing a DSN literal. P-004's CLI outputs and P-005's model layer render/transmit ONLY already-redacted evidence excerpts (P-005 additionally fences them as SEC-5 delimited untrusted data), so no CLI or model surface can emit a raw fake key. Resolve push-protection this way — NEVER by allowlisting a secret.
- **MODEL-LAYER SAFETY BOUNDARY** (P-005, now binding on every model-touching slice): the credential boundary for SugarBee.ai's OWN model API (spec §10's one permitted egress; NOT provider credentials — SEC-3 intact) lives ONLY at `bin/sugarbee.ts`. `RealModelClient` reads no `process.env`, uses built-in `fetch`, no SDK, and is imported ONLY by the bin. `FakeModelClient` drives ALL tests (no real network, ever). Model/detector code (`lg006.ts`, `inference.ts`, `client.ts`, `fakeClient.ts`) reads no credential/env/network. The model receives ONLY deterministic-surfaced, already-redacted excerpts wrapped as SEC-5 delimited untrusted data ("not instructions"); no tools; cite-or-discard; classified at best `inferred`, capped 0.9, `contradictory` on disagreement with a deterministic supporting fact. Engine + schema stay byte-identical (EMIT-ONLY): the model only emits classification/confidence; the engine Rules + schema enforce the rest. Any future model-touching slice must preserve all of this.
- **DETECTOR-SLICE externalVerification OBLIGATION** (P-003-S1, still binding): every finding on LG-003/010/012/014/015 must carry `externalVerification` (or an unverified classification) — that is what enforces the §7 Phase-1 ceiling; AT-16 is the backstop. **Proven in code for LG-003 (`stripe`), LG-010 (`resend`), LG-014 (`sentry`), and LG-015 (`dns`)**; LG-008 carries it only on its partial/external branch (`supabase`), never on its confirmed-fail branch. The model-layer substrate now supports it on model-derived findings via `InferencePresentation.externalVerification`. **Among external checks only LG-012 remains pending — it is Layer D+M and lands LAST (A-S7); the substrate already threads its path.** The scanner never represents repository evidence as provider-side proof.
- **CEILING INVARIANT — `hasAppSignal ⊇ scanner.supported`** (P-003-S3 watch-item, standing; undisturbed by P-003-S4, P-004, P-005): the Phase-1 `ready_with_warnings` ceiling holds because LG-015's app-signal gate is a strict SUPERSET of the scanner's `supported` predicate, so every supported repo emits an LG-015 finding carrying externalVerification → decision Rule 7 (`ready`) is unreachable; unsupported repos hit Rule 1 (`not_evaluated`). Any future slice that BROADENS `scanner.supported` OR NARROWS LG-015's applicability gate without preserving this superset could open an unqualified-`ready` hole. Add a regression assertion when the `golden/`/AT-16 fixture lands. (P-005's model layer is emit-only — engine + schema byte-identical, `detectStack`/`supported` and LG-015's gate untouched.)

## Deferred (follow-up packets)

- ~~(P-001) Part II of SugarBee.ai PRODUCT_SCOPE.md retains conversational provenance from its source → candidate packet: add a one-line provenance note if the owner wants the doc fully standalone; not required.~~ **RESOLVED by P-002** (commit 3a5ad85 — Part II made standalone; residue phrases 0 matches at HEAD).
- ~~(P-001) Likely next packet: SugarBee.ai Phase 1 "Repository auditor" per PRODUCT_SCOPE.md section 35 / Part II first milestone.~~ **SUPERSEDED by P-002** — the Phase 1 spec now exists (`specs/phase-1-repository-auditor.md`); implementation tracked below.
- (P-002) Phase 1 implementation must be decomposed into ≤2-commit packets at go time — **IN PROGRESS, being consumed slice by slice**: P-003-S1 (proof surface) closed; P-003-S2 (LG-001/002/004 + composed ScannerFn) closed; P-003-S3 (LG-003/008/015 + externalVerification passthrough + ceiling flip) closed; P-003-S4 (LG-010/014 warning-tier detectors) closed; P-004 (the `sugarbee` scan/eval CLI — Fork Branch B) closed; **P-005 (model-layer substrate + LG-006, Fork Branch A Slice 1) closed 2026-07-23**. **9 of 15 detectors done (LG-001/002/003/004/006/008/010/014/015); the model layer now exists (emit-only).** The spec's AT-01..AT-29 remain the program's done criteria, not one packet's. **NEXT = Branch A continues at A-S2 (LG-005 — non-idempotent webhook, Layer D+M)**, but see the TOP ITEM + shared-locator bundle recommendation — the orchestrator should shape whether A-S2 first lands the shared webhook-locator + the LG-006 deterministic-disjunct fix + a `broken-lg-006` fixture. Remaining after LG-006: LG-005/009/011/013 (D+M), LG-007 (M), LG-012 (D+M, external — last, A-S7).
- (P-002) "Later Phase 1 packets" queued behind the auditor core: cost estimation + launch-plan generation; capability graph as versioned asset; recipe selection.
- (P-002) External-side verification of LG-003/010/012/014/015 is owned by Phase 3.
- **(P-005) EXTRACT THE SHARED WEBHOOK-HANDLER LOCATOR (do BEFORE/WITH A-S2/LG-005).** The webhook-handler locator is currently restated IDENTICALLY (byte-for-byte) in `lg004.ts` and `lg006.ts`, so LG-004/LG-006 agree today — but LG-005 will be a THIRD consumer. Extract it into a shared module (e.g. `src/scan/webhook.ts` or `detectorKit`) with a guard test asserting the locators agree, BEFORE A-S2 lands. **Also note the `.find()` vs `.filter()` multi-handler limitation:** LG-006 currently analyzes only the lexicographically-first webhook handler (`.find()`) whereas LG-004 scans all handlers (`.filter()`) — a slice-1 multi-handler limitation (deterministic; AT-23 holds). The extraction is the natural moment to reconcile the multi-handler behavior.
- (P-005) **SEC-5 DELIMITER-TOKEN HARDENING** — harden `buildUntrustedDataEnvelope` against an excerpt that reproduces the END delimiter token (strip/escape delimiter tokens in excerpts). Low consequence given the structural bounds (no tools, schema-only output, cite-or-discard, contradiction-wins, 0.9 cap); do it alongside the AT-21 hostile fixture. Non-blocking.
- (P-003-S1) Spec clarification candidate: rule-5 behavior for contradictory non-fail outcomes is unspecified; consider a validator invariant "contradictory ⇒ outcome ∈ {fail, unknown}".
- (P-003-S1) Harness debt: greedy matching is not optimal assignment for overlapping expected entries; `discoverFixtures` silently skips manifest-less dirs — tighten when the eval surface hardens.
- (P-003-S1) Cheap test strengthener: rule 5 with a warning-severity finding on a blocker-ceiling check.
- (P-003-S2) **LG-001 SOURCE-PATH SCANNING deferred** — §3 lists "hardcoded in source on production paths" as an LG-001 signal; current impl covers config carriers only (`.env.production*`, `vercel.json`). Source-path scanning was deliberately omitted to avoid the `process.env.X || 'http://localhost'` dev-fallback false-positive idiom (disclosed in the `lg001.ts` docstring). Revisit when the model layer / report surface hardens.
- (P-003-S2) AT-16 real `golden/` fixture + AT-20/AT-22 hostile-fixture closure deferred to later slices. `clean-min/` is a subset-scoped negative fixture — since P-003-S3 it demonstrates the `ready_with_warnings` ceiling (no longer emits unqualified `ready`), but AT-16's real golden repo is still owed.
- (P-003-S3) **LG-008 warning-outcome enhancement** — for the narrow sub-case of a lone CONCRETE remote DB URL committed in an *unscoped* carrier (plain `.env` / a single `vercel.json` entry) with no per-environment separation, consider emitting a warning-outcome repo-side finding rather than folding it into the external `unknown`. Non-blocking.
- (P-003-S3) **LG-003 provider-agnostic webhook presence** — `isWebhookRoute` treats any *webhook* route as present (a non-Stripe webhook route reads as present). Fine for the deterministic slice; refine later without treading on LG-004. Non-blocking.
- (P-003-S3) **SPEC-DISCREPANCY NOTE** — the P-003-S3 go-instructions' challenge-3 premise mis-attributed LG-011's "localhost / preview host" language (§3) to LG-015. §3 LG-015's only fail conditions are "no canonical production URL configured anywhere, or configured values disagree." The builder's presence-not-correctness handling is spec-faithful (a localhost prod URL counts as PRESENT → LG-015 pass/unverified; LG-001 owns the dev-origin blocker). No action — recorded so future slices do not propagate the confusion.
- (P-003-S4) **LG-010 domain heuristic (no PSL)** — the registrable-domain match uses last-two-labels (no Public Suffix List, disclosed in the `lg010.ts` docstring). This can ONLY under-warn: it collapses two distinct registrable domains into a match but never splits one domain apart, so a multi-part public suffix collapses to a match → no false fail; defaulting to pass/unverified on an indeterminable domain is the safe direction for a warning-only, externally-unverified check. Revisit if/when a PSL is introduced. Non-blocking.
- (P-003-S4) **LG-014 implicit-Sentry mechanisms** — the "production-wired" reading is DSN wired AND env/release tagging present (a `process.env` DSN reference counts as wired); it does NOT recognize Sentry's implicit release-injection / dashboard-only DSN mechanisms. Acceptable — those are genuinely absent from repository config, so a warning marked arrival-unverified is spec-faithful and never blocks. Refine later if needed. Non-blocking.
- (P-004) **SPEC HEADER STALENESS** — `specs/phase-1-repository-auditor.md` header still says "not yet implemented." Defensible as the spec's own authorship status, but stale now that implementation is underway (S1-S4 detectors + the CLI + the model layer have landed). Reconcile in a later doc pass. Non-blocking.
- (P-004) **`report.repo.commit` stays null** — the banner's `@<commit>` is cosmetic (no AT requires it); reading `.git/HEAD` statically was deferred to avoid touching the frozen scanner beyond the additive `checks?` option. Wire it in a later polish pass if a real-repo banner wants the commit. Non-blocking.
- (P-004) **BANNER LOCATOR ARTIFACT on fixture scans** — scanning a FIXTURE dir, LG-002 evidence lists the fixture manifest file because the manifest embeds the seeded key string; the excerpt is still redacted. Genuinely cosmetic, cannot occur on a real repo, and would require touching a frozen detector — out of scope. Non-blocking.
- (P-004) **`eval` runs over the 10 present fixtures only** — golden/, hostile/, and the model-layer broken fixtures (incl. `broken-lg-006`) don't exist yet, so §9.3's 15/15-recall and 18/18-decision thresholds remain a PROGRAM-level gate owed as fixtures/detectors land. Non-blocking.
- ~~(P-004) AT-27 "mark M-checks unknown" half OWED to the model-layer packet.~~ **RESOLVED by P-005** — `--offline`/model-unconfigured now marks LG-006 `unknown` ("model layer disabled"); deterministic checks unaffected; two fixed-clock offline runs byte-identical. NOTE the interaction with the TOP ITEM: once the LG-006 deterministic-disjunct fidelity gap is fixed, a fully-missing cancellation branch must be a deterministic `confirmed` blocker `fail` EVEN under `--offline` (the `unknown` is reserved for the branch-present-but-unconfirmed case).

## Known risks / debt

- (P-003-S2, non-blocking hardening from reviewer) LG-001 `URL_KEY_RE` substrings (`base`/`site`/`origin`) can over-match benign env keys — but can only shift pass↔unknown, never manufacture a false blocker.
- (P-003-S2, non-blocking) `vercel.json` evaluation in LG-001 assumes single-line key/value.

## Visibility items (outside Build OS scope)

- **Gravito governance gap** (user-flagged 2026-07-23, explicitly non-blocking, not a Build OS packet): "the live substrate-readiness check still reports Claude, Manus, and surplus_recovery as missing surfaces — remains visible as a distinct Gravito governance gap" (user, 2026-07-23). Recorded for visibility only; carried forward through P-005 as its own distinct item.
- **Gravito — FIRST NAMED POST-GOLDEN-PATH RECIPE + REAL-WORLD EVAL TARGET** (user-directed, recorded at P-004 close; product-direction item, NOT a Build OS action item): Gravito is a Vite + Express + Fly.io TypeScript SaaS (NOT Next.js/Vercel), so SugarBee.ai's current Next.js-only `detectStack` would honestly return `not_evaluated` (unsupported stack) on it. An Express/Vite/Fly recipe is the FIRST obvious post-golden-path expansion, and Gravito becomes a named eval target once that recipe exists — but the golden path (one runnable Next.js path) is completed FIRST; the recipe was deliberately NOT built in P-004/P-005. Distinct from the governance-gap item above.

## Open boundaries (awaiting explicit go)

- **(2026-07-25) BUILD OS ADOPTION + MEMORY RELOCATION — RESOLVED.** The pending "adopt canonical ClaudeOrchestrator at `7ef50e8`" item is closed. The designated branch `claude/sugarbee-project-handoff-ic47uc` in ClaudeOrchestrator was cut from canonical `7ef50e8`, so the framework files arrived with no merge and no conflict. The predicted `P-001…P-005` receipt collision was resolved **structurally, per user decision**: SugarBee's project memory now lives in THIS product repo (`build-os/`, installed by canonical `install-project.sh`), and ClaudeOrchestrator stays the framework only. Receipt ids preserved. A SessionStart hook (`.claude/hooks/session-start-build-os.sh`) + UserPromptSubmit router are registered in this repo's `.claude/settings.json`, so a fresh session here picks Build OS up automatically. **Verify on the next fresh session that the SessionStart hook actually fires in this environment** — installed ≠ activated (status-semantics rule).
- NO merge to any default branch in either repo (ClaudeOrchestrator, SugarBee.ai/LaunchGraph) and NO pull request — both await explicit go. Feature-branch pushes were explicitly authorized and completed (P-001, P-002, P-003-S1/S2/S3/S4, P-004, and **P-005** — under standing authorization after qa green + reviewer pass; **P-005 pushed as a normal fast-forward `a4ffbf5..fb5663b`** after a two-repo pre-push scanner-safety grep). No deploys, no secrets touched/allowlisted, no provider access. **No real model API call occurred in P-005** — the model layer's runtime egress only activates when a real model is configured at the bin, which did not happen.

---
_Append-only working notes. Updated by the archivist on close of P-001
(2026-07-23), P-002 (2026-07-23), P-003-S1 (2026-07-23), P-003-S2 (2026-07-23),
P-003-S3 (2026-07-23), P-003-S4 (2026-07-23), P-004 (2026-07-23), and P-005
(2026-07-23)._
