# Preview configuration. SEEDED DEFECT (LG-008): reuses the SAME DATABASE_URL
# value as .env.production, so preview shares the production database.
DATABASE_URL=postgresql://db.example.com:5432/appdb
