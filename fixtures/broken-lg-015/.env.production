# Production configuration. SEEDED DEFECT (LG-015): NO canonical production
# URL is configured anywhere (no site-URL variable, no metadataBase). LG-001 is
# therefore 'unknown' here (no production URL mapping to evaluate), so LG-015 is
# the only seeded blocker.
NODE_ENV=production
