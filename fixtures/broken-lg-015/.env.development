# Development configuration. DISTRACTOR: a localhost site URL in a dev file is
# correct and must NOT be treated as a production domain by LG-015 (dev files
# are not production-designated carriers).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
