# Production configuration. Clean on LG-001 (canonical https origin) and
# LG-015 (a single consistent production host is configured → pass-unverified).
# NOTE: no SENTRY_DSN is defined here — the seeded LG-014 defect is a DSN that
# is absent from production configuration (no DSN literal is needed).
NEXT_PUBLIC_SITE_URL=https://app.example.com
