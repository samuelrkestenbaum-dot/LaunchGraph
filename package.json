{
  "name": "launchgraph",
  "version": "0.1.0",
  "private": true,
  "description": "LaunchGraph Phase 1 Repository Auditor — proof surface (schemas, check registry, decision engine, fixture harness).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "launchgraph": "bin/launchgraph.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "scan": "tsx bin/launchgraph.ts scan",
    "eval": "tsx bin/launchgraph.ts eval"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "4.19.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
