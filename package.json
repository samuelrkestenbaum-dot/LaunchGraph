{
  "name": "sugarbee",
  "version": "0.1.0",
  "private": true,
  "description": "SugarBee.ai Phase 1 Repository Auditor — proof surface (schemas, check registry, decision engine, fixture harness).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "sugarbee": "bin/sugarbee.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "scan": "tsx bin/sugarbee.ts scan",
    "eval": "tsx bin/sugarbee.ts eval"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "4.19.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
